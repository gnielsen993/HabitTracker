---
phase: 06-polish-f
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - HabitsTrackerTests/ExportImportTests.swift
autonomous: true
requirements: [POL-03]
must_haves:
  truths:
    - "A single bundle containing one of every type (Domain, Habit, DailyEntry, Rule, Collection, CollectionItem, Clip, Idea) round-trips through export→importReplace with all fields + relationships intact at schemaVersion 6"
    - "StatusSet round-trips via the stored statusSet identifier (code catalog, not a DTO)"
    - "A malformed or newer-schema import fails gracefully and does NOT destroy the existing local store"
    - "Honors CONTEXT.md decisions D-14, D-15"
  artifacts:
    - path: "HabitsTrackerTests/ExportImportTests.swift"
      provides: "Consolidated all-types v6 round-trip test + malformed/unsupported-schema safety test"
      contains: "func test"
  key_links:
    - from: "HabitsTrackerTests/ExportImportTests.swift"
      to: "HabitsTracker/Services/ExportImportService.swift"
      via: "exportData(...) → importReplace(data:context:) round-trip on an in-memory ModelContainer"
      pattern: "importReplace\\(data:"
---

<objective>
Verify and harden POL-03: full export/import round-trips ALL types under `schemaVersion` (D-14). The DTOs and per-version tests already exist (`testV3/4/5FieldsSurviveRoundTrip`, `testV6IdeaFieldsSurviveRoundTrip`) and the service stamps `schemaVersion = 6` — POL-03 is round-trip VERIFICATION plus one integration test that exercises all 7 persisted types in a SINGLE bundle (cross-type relationship integrity), plus a safety test proving a malformed/unsupported import never destroys the store. NO schema bump (D-15) — search + a11y are read-side.

Purpose: The "durable" leg of the phase goal — the user's whole hub must survive a backup/restore, and a bad backup file must not nuke their data.
Output: extended `ExportImportTests.swift` (all-types-in-one-bundle + malformed-import-safety). No production DTO/service changes.
</objective>

<decisions_covered>
CONTEXT.md decisions this plan implements:
- D-14: export/import DTOs for all 7 types already round-trip at `schemaVersion = 6`; StatusSet round-trips by stored ID (code catalog, not a DTO). POL-03 is round-trip verification + import-safety hardening, not new DTO work.
- D-15: no cosmetic `schemaVersion` bump — search + a11y are read-side; stays at 6. A bump only happens if a persisted field is genuinely added (playbook territory).
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-f/06-CONTEXT.md
@.planning/phases/06-polish-f/06-PATTERNS.md

<interfaces>
<!-- Confirmed from the repo. -->
ExportImportService (VERIFY ONLY — do not change):
- `schemaVersion = 6`
- `exportData(categories:habits:entries:rules:collections:collectionItems:clips:ideas:) throws -> Data`
- `importReplace(data:context:) throws` — decodes bundle FIRST (line 157), guards `bundle.schemaVersion <= schemaVersion` else throws `ImportError.unsupportedSchema` (161–163), THEN `deleteAll(context:)` (165). Decode/guard failures happen BEFORE any destructive delete — the replace-safety property.
StatusSet: code catalog (StatusSetCatalog.swift) referenced by stored `statusSetID` — round-trips via the identifier, not a DTO.

ExportImportTests analog methods: testV3/4/5FieldsSurviveRoundTrip, testV6IdeaFieldsSurviveRoundTrip.
Harness (lines 27–36): `@MainActor` + in-memory `ModelContainer(for: schema, configurations: [ModelConfiguration(isStoredInMemoryOnly: true)])`, seed → exportData → importReplace → re-fetch → XCTAssertEqual.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add all-types-in-one-bundle round-trip + malformed-import-safety tests</name>
  <files>HabitsTrackerTests/ExportImportTests.swift</files>
  <read_first>
    - HabitsTrackerTests/ExportImportTests.swift (the file being modified — harness lines 27–36; analog assertions testV5FieldsSurviveRoundTrip:164, testV6IdeaFieldsSurviveRoundTrip:220)
    - HabitsTracker/Services/ExportImportService.swift (exportData signature 23–31; importReplace decode-then-guard-then-deleteAll ordering 156–165)
    - .planning/phases/06-polish-f/06-PATTERNS.md (ExportImportTests section, lines 167–171 — incl. the §9.7 toolchain caveat)
    - .planning/phases/06-polish-f/06-CONTEXT.md (D-14, D-15)
    - HabitsTracker/Models/*.swift (field names to assert on round-trip)
  </read_first>
  <behavior>
    - Test A (all-types round-trip): seed one Domain plus one each of Habit (+DailyEntry + HabitState), Rule (filed under the domain, with body + sourceURL), Collection + CollectionItem (with a non-generic statusSetID + note + position fields), Clip (title/url/note/tag/status/domain), Idea (title/note/url, filed + one promoted with promotedToKindRaw/promotedToID). Export → importReplace into a fresh in-memory context → re-fetch every type and XCTAssertEqual counts + key fields + relationships (Rule.domain, Clip.domain, CollectionItem.collection, Idea.domain, Habit↔DailyEntry). Assert the CollectionItem's statusSetID survived (StatusSet round-trips by identifier, not DTO — D-14).
    - Test B (malformed/unsupported import safety): seed a non-empty store, then call importReplace with (b1) syntactically invalid JSON `Data` and (b2) a valid bundle whose `schemaVersion` is 7 (> 6). Both must `XCTAssertThrowsError`, and after each the ORIGINAL seeded data must still be present (re-fetch counts unchanged) — proving decode/guard failure never reached `deleteAll` (the replace-safety property).
  </behavior>
  <action>
    Add two `@MainActor` tests to `ExportImportTests` mirroring the existing harness (in-memory `ModelContainer`, isStoredInMemoryOnly). `testAllTypesSurviveRoundTripV6` implements Behavior A — one bundle covering all 7 persisted types + StatusSet-by-id, asserting fields AND relationships after `service.importReplace`. `testMalformedAndUnsupportedImportPreservesStore` implements Behavior B — seed, snapshot counts, `XCTAssertThrowsError(try service.importReplace(data: garbageData, context: ctx))` and again for a bundle re-encoded with `schemaVersion = 7`, then assert the seeded counts are unchanged after each throw. Do NOT modify `ExportImportService.swift` and do NOT bump `schemaVersion` (D-15) — production code is verify-only. Keep the file coherent with the existing per-version tests (do not delete them).
  </action>
  <acceptance_criteria>
    - `ExportImportTests.swift` contains `func testAllTypesSurviveRoundTripV6` seeding all 7 persisted types in one bundle and asserting fields + relationships after `importReplace`.
    - `ExportImportTests.swift` contains `func testMalformedAndUnsupportedImportPreservesStore` with two `XCTAssertThrowsError` cases (invalid JSON + schemaVersion 7) and post-throw assertions that the seeded store is unchanged.
    - `git diff --stat HabitsTracker/Services/ExportImportService.swift` shows NO changes (production verify-only; no schema bump — D-15).
    - `xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build` exits 0 (compile/build-verify of the persistence test suite).
    - Per §9.7, execution of these `@Model` persistence tests is device-only; the plan does NOT claim they pass on the iOS 26 simulator (they crash the host at 0.000s). Device execution is handled in 06-04.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
    <human-check>ExportImportTests are SwiftData @Model persistence tests — per §9.7 they crash the iOS 26 simulator test host at 0.000s and are BUILD-VERIFY-ONLY here. Actual pass/fail execution is deferred to the owner device run in 06-04. Do NOT report a simulator test pass.</human-check>
  </verify>
  <done>ExportImportTests compiles with an all-7-types single-bundle v6 round-trip test and a malformed/unsupported-import store-preservation test; ExportImportService is unchanged and schemaVersion stays 6; build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline-only, local-first (CLAUDE.md §1) — no network/auth/backend. The ONE real trust boundary in this phase is the JSON **import** path: a backup file is untrusted input that crosses into the local store via `importReplace`.

| Boundary | Description |
|----------|-------------|
| backup file (`fileImporter`) → importReplace → SwiftData store | untrusted JSON that, if mishandled, could corrupt or destroy existing local data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-T | Tampering / Data-loss | importReplace destructive replace | mitigate | `importReplace` decodes + guards `schemaVersion <= 6` BEFORE `deleteAll` (service lines 157–165); Test B proves a malformed/newer-schema file throws and leaves the existing store intact (no partial-destroy) |
| T-06-03-D | Denial of Service | oversized/hostile JSON | accept | Local file the user themselves selected via `fileImporter`; a large file at worst fails to decode and throws — no remote attacker, offline-only, no network/auth surface |
| T-06-03-SC | Supply Chain | package installs | accept | No new package-manager installs in this phase (test-only edit); RESEARCH package audit N/A |
</threat_model>

<verification>
- Build exits 0 on iPhone 17 simulator (test suite compiles).
- ExportImportService unchanged; schemaVersion remains 6 (no cosmetic bump, D-15).
- Round-trip + malformed-import-safety tests present.
- Actual test EXECUTION + in-app backup/restore round-trip verified on device in 06-04 (§9.7).
</verification>

<success_criteria>
- POL-03: full export/import round-trips ALL types (Domain, Habit, Rule, Collection, CollectionItem, Idea, Clip, StatusSet-by-id) under schemaVersion 6, and a bad backup never destroys the store (SC3).
</success_criteria>

<output>
Create `.planning/phases/06-polish-f/06-03-SUMMARY.md` when done.
</output>
