---
phase: 06-polish-f
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02", "06-03"]
files_modified: []
autonomous: false
requirements: [POL-01, POL-02, POL-03, POL-04]
must_haves:
  truths:
    - "On device, cross-domain search returns items across all types and each tapped result opens its own surface"
    - "On device, the search no-results state and all existing empty states render"
    - "On device, the export→import backup round-trips all types with data intact (real execution, not the sim-crashing @Model tests)"
    - "On device, VoiceOver reaches the Collections advance chip and announces the advance; Settings shows schema v6 + version 1.0"
  artifacts:
    - path: ".planning/phases/06-polish-f/06-04-VERIFICATION-EVIDENCE.md"
      provides: "Owner device verification record for SC1–SC4"
  key_links: []
---

<objective>
Final owner device verification of Phase 6 (SC1–SC4). The three Wave-1 plans land the code and pass build-verify on the iOS 26 simulator, but two things are genuinely device-only on this toolchain: (1) VoiceOver reachability of the fixed Collections chip, and (2) actual EXECUTION of the export/import round-trip — `ExportImportTests` are SwiftData `@Model` persistence tests that crash the simulator host at 0.000s (§9.7), so the round-trip must be exercised via the in-app backup/restore flow on a physical device. This plan is the same owner-device-verification pattern that closed Phases 1, 4, and 5.

Purpose: Prove the polish phase is real on-device, not just green in a headless build.
Output: an owner sign-off recorded in the verification evidence file; Phase 6 closes on approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-f/06-CONTEXT.md
@.planning/phases/06-polish-f/06-01-PLAN.md
@.planning/phases/06-polish-f/06-02-PLAN.md
@.planning/phases/06-polish-f/06-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-checkpoint automated gate (build + tokens/print sweeps)</name>
  <files>.planning/phases/06-polish-f/06-04-VERIFICATION-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/06-polish-f/06-01-SUMMARY.md, 06-02-SUMMARY.md, 06-03-SUMMARY.md (what the Wave-1 plans actually delivered)
    - CLAUDE.md §8 (build command), §9.7 (sim caveat), §9.13 (no print())
  </read_first>
  <action>
    Run the full build and record the exact result: `xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build` (must exit 0). Run the runnable engine/logic test suites (they DO run per §9.7) and record pass counts if any logic was touched — note that `ExportImportTests` and other `@Model` persistence suites are build-verify-only here (do not report a sim pass for them). Grep-sweep the Phase-6 surfaces for regressions: no raw `print(`/`debugPrint(` in the modified files (§9.13), and no hardcoded color literals on the touched chip/search/Settings surfaces (§1/§9.4). Write `.planning/phases/06-polish-f/06-04-VERIFICATION-EVIDENCE.md` capturing the build exit code, the grep results, and a checklist of the device steps Task 2 must confirm.
  </action>
  <acceptance_criteria>
    - `xcodebuild ... -quiet build` exit code recorded as 0 in the evidence file.
    - `grep -rn "print(\|debugPrint(" HabitsTracker/Features/Hub/SearchResultsView.swift HabitsTracker/Features/Hub/HubView.swift HabitsTracker/Features/Collections/CollectionItemRow.swift HabitsTracker/Features/Settings/SettingsView.swift` returns nothing (§9.13).
    - `.planning/phases/06-polish-f/06-04-VERIFICATION-EVIDENCE.md` exists with the SC1–SC4 device checklist for Task 2.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <done>Build exits 0, print/token sweeps clean on Phase-6 surfaces, and the device-verification checklist is written to the evidence file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner device walkthrough — SC1–SC4 on iPhone 17</name>
  <what-built>
    Cross-domain search on the Hub tab (POL-01), the search no-results state + confirmed existing empty states (POL-02), export/import round-trip of all types at schemaVersion 6 (POL-03), and the accessibility fixes + Settings schema/version row (POL-04). All build-verified on the simulator; this step confirms the two device-only properties (VoiceOver reachability, actual backup/restore execution) plus the end-to-end feel.
  </what-built>
  <how-to-verify>
    On a physical iPhone (or a device runtime — NOT the sim, which crashes the @Model test host at 0.000s per §9.7):

    1. SC1 — Search: Open the Hub tab. Tap the magnifying glass (it should expand to an inline field — the iOS 26 minimize behavior). Type a term that matches items of several types. Confirm results are grouped into one section per type (Habits / Rules / Collections / Clips / Ideas). Tap a Rule/Collection/Clip result → it PUSHES that item's detail. Tap an Idea result → it opens IdeaCaptureSheet as a sheet. Tap a Habit result → it opens HabitEditorView as a sheet (it must NOT jump to Today). Confirm archived/consumed items do not appear.
    2. SC2 — Empty states: Clear the search field / type gibberish → confirm the search no-results state appears. Spot-check that the Hub empty state, a domain's "Nothing here yet" section, and the inbox empty state still render.
    3. SC3 — Export/import: Settings → Export JSON → save the backup. Change or delete some data. Settings → Import JSON (Replace) → pick the backup → confirm ALL types return intact (domains, habits, rules, collections + items with status, clips, ideas). Optionally try importing a garbage file and confirm it fails gracefully WITHOUT wiping your data.
    4. SC4 — Accessibility + schema row: Enable VoiceOver. Navigate to a Collection's items → confirm the status chip is REACHABLE as a control and announces the current status AND that it advances to the next status (previously silent). Confirm Settings → About shows "Data schema v6" and "Version 1.0". Sanity-check Dynamic Type at a large size on the chips/Hub grid.
    5. Baseline DoD: Today is unchanged, tab bar is still 4 tabs, no debug/placeholder strings.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 6, or describe any issue (which SC + what you saw) to route a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline-only, local-first (CLAUDE.md §1). This plan runs no new code — it verifies the Wave-1 output on device. The only real boundary (the import path) is validated here via the SC3 in-app backup/restore + garbage-file step, executing on-device what §9.7 prevents the simulator from running.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-T | Tampering / Data-loss | on-device import round-trip | mitigate | SC3 step exercises real export→import + a garbage-file import on device, confirming data survives and a bad file fails without destroying the store (the §9.7-blocked execution of T-06-03-T) |
| T-06-04-SC | Supply Chain | package installs | accept | No installs in this phase; verification-only, offline, no network/auth surface |
</threat_model>

<verification>
- Build exits 0; print/token sweeps clean (Task 1).
- Owner confirms SC1–SC4 + baseline DoD on device (Task 2).
</verification>

<success_criteria>
- SC1 (search returns + navigates), SC2 (empty states), SC3 (all-types round-trip), SC4 (a11y + schema visibility) all confirmed on device; owner types "approved".
</success_criteria>

<output>
Create `.planning/phases/06-polish-f/06-04-SUMMARY.md` when done (record the owner sign-off + evidence path).
</output>
