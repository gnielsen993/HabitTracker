---
phase: 06-polish-f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HabitsTracker/Features/Hub/SearchResultsView.swift
  - HabitsTracker/Features/Hub/HubView.swift
  - HabitsTracker/Features/Ideas/InboxView.swift
autonomous: true
requirements: [POL-01, POL-02]
must_haves:
  truths:
    - "User can invoke cross-domain search from the Hub tab (magnifying glass expands to a field)"
    - "Results are grouped into one section per item type (Habits/Rules/Collections/Clips/Ideas), matching title + free-text fields"
    - "Tapping a result opens that item's own existing detail/editor surface"
    - "An empty or no-match query shows a designed no-results state, and existing section/inbox/Hub empty states remain intact"
    - "Honors CONTEXT.md decisions D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-11, D-12"
  artifacts:
    - path: "HabitsTracker/Features/Hub/SearchResultsView.swift"
      provides: "Type-grouped cross-domain search results + no-results state"
      min_lines: 90
    - path: "HabitsTracker/Features/Hub/HubView.swift"
      provides: ".searchable host with .searchToolbarBehavior(.minimize)"
      contains: ".searchable"
  key_links:
    - from: "HabitsTracker/Features/Hub/HubView.swift"
      to: "HabitsTracker/Features/Hub/SearchResultsView.swift"
      via: "SearchResultsView(query: searchText) shown when searchText is non-empty"
      pattern: "SearchResultsView\\(query:"
    - from: "HabitsTracker/Features/Hub/SearchResultsView.swift"
      to: "RuleDetailView / ClipDetailView / CollectionDetailView / IdeaCaptureSheet / HabitEditorView"
      via: "reused NavigationLink push (Rule/Collection/Clip) + sheet (Idea/Habit)"
      pattern: "RuleDetailView\\(rule:|HabitEditorView\\(habit:"
---

<objective>
Deliver cross-domain search (POL-01) and finish the empty-state pass (POL-02). Search lives on the Hub tab root via SwiftUI `.searchable` + iOS 26 `.searchToolbarBehavior(.minimize)` (D-01/D-02) and shows a new `SearchResultsView` — N per-type in-memory-filtered fetches, grouped into one Section per type (D-03), matching title + free-text fields via `.localizedStandardContains` (D-04), excluding archived/consumed items (D-06). Each result reuses the item's already-wired detail/editor surface (D-07/D-08). A dedicated `ContentUnavailableView.search(text:)` covers no-results (D-12); all other empty states are verified as already satisfying §9.3 and left as-is (D-11).

Purpose: Makes the hub feel finished and findable across every type without a new tab (4-tab IA locked) or new detail UI.
Output: `SearchResultsView.swift` (new) + `.searchable` wired into `HubView.swift`.
</objective>

<decisions_covered>
CONTEXT.md decisions this plan implements:
- D-01: cross-domain search lives on the Hub tab root via `.searchable`.
- D-02: invocation via iOS 26 `.searchToolbarBehavior(.minimize)`.
- D-03: results grouped by item type, one Section per type (N per-model fetches).
- D-04: match title + free-text fields via `.localizedStandardContains`.
- D-05: include Habits in search.
- D-06: exclude archived/soft-deleted and consumed/promoted items.
- D-07: tapped result opens the item's own existing detail/editor surface.
- D-08: Habit results open `HabitEditorView` as a sheet, not a jump to Today.
- D-11: reuse existing shared empty states (no bespoke per-section copy).
- D-12: search no-results via `ContentUnavailableView.search(text:)`.
</decisions_covered>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-f/06-CONTEXT.md
@.planning/phases/06-polish-f/06-PATTERNS.md

<interfaces>
<!-- Confirmed init signatures + searchable fields — use directly, no exploration needed. -->
Reused destinations (all take a plain `let` value):
- RuleDetailView(rule: Rule)          // push
- CollectionDetailView(collection: Collection)  // push
- ClipDetailView(clip: Clip)          // push
- IdeaCaptureSheet(idea: Idea)        // sheet (IdeaRow already owns its own tap→edit sheet — reuse IdeaRow directly)
- HabitEditorView(habit: Habit)       // sheet (SearchResultsView owns the .sheet(item:) state; NOT a jump to Today, D-08)
Reused rows (data-driven, own no query — §9.2): RuleRow(rule:), CollectionRow(collection:), ClipRow(clip:), IdeaRow, HabitRow/name display.

Searchable fields (confirmed from Models):
- Rule:           title + body                     ; exclude isArchived
- Clip:           title + url + note? + tag?         ; exclude isArchived
- Idea:           title + note? + url?               ; exclude isArchived && promotedToKindRaw == nil (consumed = promoted, D-06)
- CollectionItem: title + note?                      ; (no isArchived) surface under Collections
- Collection:     title + note?                      ; (no isArchived)
- Habit:          name (title-only)                  ; exclude isArchived (D-05: include habits)

HubView host (lines 24–38): single NavigationStack + navigationTitle("Hub") + Group{ emptyState | grid }. Attach search to the Group.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create SearchResultsView — type-grouped cross-domain results + no-results state</name>
  <files>HabitsTracker/Features/Hub/SearchResultsView.swift</files>
  <read_first>
    - .planning/phases/06-polish-f/06-PATTERNS.md (SearchResultsView section, lines 61–113 — section-loop layout, per-type filter table, reused row+destination wiring, ContentUnavailableView.search)
    - .planning/phases/06-polish-f/06-CONTEXT.md (D-03..D-08, D-12)
    - HabitsTracker/Features/Hub/DomainDetailView.swift (section-loop-with-empty-fallback + the exact NavigationLink{Detail} label:{Row} pairs for Rule/Collection/Clip; header + .isHeader trait)
    - HabitsTracker/Features/Habits/HabitManagerView.swift (searchText + case-insensitive-contains filter idiom, and the .sheet(item: $editingHabit) { HabitEditorView(habit:) } pattern)
    - HabitsTracker/Features/Ideas/IdeaRow.swift (already owns its tap→edit IdeaCaptureSheet(idea:) — reuse as-is)
    - HabitsTracker/Features/AppBootstrap/AppBootstrapView.swift (ContentUnavailableView idiom)
    - HabitsTracker/Models/Rule.swift, Clip.swift, Idea.swift, Collection.swift, CollectionItem.swift, Habit.swift (field names)
  </read_first>
  <action>
    Create `SearchResultsView` taking `let query: String` (via `init(query:)`). Standard theme plumbing: `@EnvironmentObject private var themeManager: DKThemeManager`, `@Environment(\.colorScheme) private var colorScheme`, `let theme = themeManager.theme(for: colorScheme)`.

    Own six `@Query`s (one per type — §9.2, the view owns the queries, rows take values). Apply each type's static exclusion filter in the `#Predicate` where cheap: Rule `!isArchived`, Clip `!isArchived`, Idea `!isArchived && promotedToKindRaw == nil`, Habit `!isArchived`; Collection and CollectionItem need no exclusion filter. Then compute per-type match arrays that filter IN MEMORY by `query` using `.localizedStandardContains` (D-04, diacritic/width-insensitive) across that type's title + free-text fields: Rule → `title`,`body`; Clip → `title`,`url`,`note`,`tag`; Idea → `title`,`note`,`url`; CollectionItem → `title`,`note` (surfaced under a "Collections" section); Collection → `title`,`note`; Habit → `name`. Guard nil optionals (`note`/`tag`/`url`/`sourceURL`) before contains. A blank/whitespace-only `query` yields all-empty match arrays (no results shown).

    Layout: copy DomainDetailView's `ScrollView { VStack(alignment:.leading, spacing: theme.spacing.xl) { ... } .padding(theme.spacing.l) }` section loop. Emit one block per NON-empty match array only, in order Habits, Rules, Collections, Clips, Ideas. Each block header is `Text("<Type>").font(theme.typography.title).foregroundStyle(theme.colors.textPrimary).accessibilityAddTraits(.isHeader)` followed by a `ForEach(matches, id: \.id)` of the REUSED row wrapped in its REUSED destination — do NOT build new rows or detail screens:
      - Rule → `NavigationLink { RuleDetailView(rule: rule) } label: { RuleRow(rule: rule) }.buttonStyle(.plain)`
      - Collection → `NavigationLink { CollectionDetailView(collection: c) } label: { CollectionRow(collection: c) }.buttonStyle(.plain)` (fold CollectionItem hits into this section — route an item hit to its parent `CollectionDetailView(collection: item.collection)`)
      - Clip → `NavigationLink { ClipDetailView(clip: clip) } label: { ClipRow(clip: clip) }.buttonStyle(.plain)`
      - Idea → reuse `IdeaRow` directly (it already owns its tap→edit `IdeaCaptureSheet(idea:)` — D-08)
      - Habit → owns local `@State private var editingHabit: Habit?`; the row sets `editingHabit = habit`; present via `.sheet(item: $editingHabit) { habit in HabitEditorView(habit: habit) }` (D-08: editor sheet, NEVER a jump to Today)

    No-results (D-12/POL-02): when `query` is non-empty AND every match array is empty, render `ContentUnavailableView.search(text: query)` instead of the section VStack. Tokens only, no hardcoded colors (§1, §9.4). Keep the file under the ~400-line cap (§9.1) — this is a lens over existing rows, not new item UI.
  </action>
  <acceptance_criteria>
    - `HabitsTracker/Features/Hub/SearchResultsView.swift` exists with `init(query:)` and compiles.
    - `grep -n "localizedStandardContains" HabitsTracker/Features/Hub/SearchResultsView.swift` returns matches for all six types' filters.
    - File contains `RuleDetailView(rule:`, `CollectionDetailView(collection:`, `ClipDetailView(clip:`, `HabitEditorView(habit:` and reuses `IdeaRow` — no new `*DetailView`/`*Row` struct is declared in this file.
    - File contains `ContentUnavailableView.search(text:` gated on non-empty query + all-empty matches.
    - Idea query predicate contains `promotedToKindRaw == nil` and each type's exclusion filter is present (Rule/Clip/Idea/Habit `!isArchived`).
    - `grep -c "Color(" HabitsTracker/Features/Hub/SearchResultsView.swift` shows no hardcoded UIColor/Color literals (tokens only).
    - `xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build` exits 0.
    - No `X 2.swift` dupe appears in `git status` (§9.6).
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <done>SearchResultsView renders one Section per non-empty type from title + free-text matches, reuses each type's existing row + detail/editor destination, and shows ContentUnavailableView.search on no matches; build exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire .searchable into HubView + verify existing empty states + close inbox POL-02 flag (POL-02)</name>
  <files>HabitsTracker/Features/Hub/HubView.swift, HabitsTracker/Features/Ideas/InboxView.swift</files>
  <read_first>
    - HabitsTracker/Features/Hub/HubView.swift (the file being modified — NavigationStack + Group host, lines 21–39)
    - .planning/phases/06-polish-f/06-PATTERNS.md (HubView section, lines 30–57)
    - .planning/phases/06-polish-f/06-CONTEXT.md (D-01, D-02, D-11)
    - HabitsTracker/Features/Hub/DomainDetailView.swift ("Nothing here yet" fallback), HabitsTracker/Features/Ideas/InboxView.swift, HabitsTracker/Features/Ideas/PromoteToCollectionPicker.swift (existing empty states to verify, D-11)
  </read_first>
  <action>
    In `HubView`, add `@State private var searchText = ""`. Attach `.searchable(text: $searchText)` and `.searchToolbarBehavior(.minimize)` to the existing `Group` inside the single `NavigationStack` (do NOT add a second NavigationStack — the stack already exists at lines 24–38). Change the Group's content branch so search takes precedence: when `!searchText.trimmingCharacters(in: .whitespaces).isEmpty` show `SearchResultsView(query: searchText)`; else keep the current `focusedDomains.isEmpty ? emptyState : grid` behavior unchanged. Preserve the existing `.background(theme.colors.background.ignoresSafeArea())`, `.navigationTitle("Hub")`, and the existing `.navigationDestination(for: Domain.self)`. No new navigationDestination registrations are required because `SearchResultsView` uses closure-based `NavigationLink { Detail }` pushes that resolve against this same stack.

    Empty-state verification (POL-02, D-11): confirm — do NOT rewrite — that HubView's `emptyState` ("Your Hub is empty" + Choose Domains CTA), DomainDetailView's "Nothing here yet" fallback, InboxView's single-line empty state ("Nothing to file right now."), and PromoteToCollectionPicker's empty state all still render. Bespoke per-section copy is explicitly deferred (D-11); the only NEW empty state this phase adds is the search no-results one delivered in Task 1.

    Close the stale POL-02 contract in `InboxView.swift`: the `emptyState(theme:)` doc-comment at lines 46–47 currently reads "Minimal D-04 placeholder — NOT the Phase 6 designed empty state (flag: POL-02 replaces this with a fuller treatment). Single centered line, no heading, no CTA." Per Gabe's Phase 6 decision, D-11 stands — the minimal centered line IS the accepted designed inbox empty state — so this comment is now false and must be removed. Delete those two `///` lines (keep the `// MARK: - Empty state` marker and the `emptyState(theme:)` implementation exactly as-is; only the stale placeholder claim goes). Do not alter the rendered copy or styling.
  </action>
  <acceptance_criteria>
    - `HubView.swift` contains `.searchable(text:` AND `.searchToolbarBehavior(.minimize)`.
    - `HubView.swift` contains `SearchResultsView(query:` gated on non-empty trimmed `searchText`.
    - `grep -c "NavigationStack" HabitsTracker/Features/Hub/HubView.swift` returns 1 (no second stack introduced).
    - Existing `emptyState(theme:)` in HubView is unchanged (still contains "Your Hub is empty" and "Choose Domains").
    - `grep -rn "ContentUnavailableView\|empty" HabitsTracker/Features/Ideas/InboxView.swift HabitsTracker/Features/Hub/DomainDetailView.swift` confirms the existing fallbacks are present (POL-02 verification).
    - InboxView's rendered empty copy is unchanged: `grep -q "Nothing to file right now." HabitsTracker/Features/Ideas/InboxView.swift` succeeds.
    - The stale placeholder comment is gone: `grep -q "NOT the Phase 6 designed empty state" HabitsTracker/Features/Ideas/InboxView.swift` returns non-zero (no match).
    - `xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <done>Hub tab exposes a minimize-behavior search field; non-empty queries show SearchResultsView within Hub's existing stack; all pre-existing empty states remain and satisfy §9.3; build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

HabitsTracker is OFFLINE-ONLY, local-first SwiftUI/SwiftData (CLAUDE.md §1) — no network, no auth, no backend, no remote input. This plan adds a local read-only search surface only.

| Boundary | Description |
|----------|-------------|
| user keyboard → search field | local free-text; feeds `.localizedStandardContains` in-memory filters over the local store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-I | Information Disclosure | SearchResultsView result set | mitigate | Exclude archived/consumed items by default (`!isArchived`, Idea `promotedToKindRaw == nil`, D-06) so dead/hidden items are never surfaced |
| T-06-01-INJ | Injection (n/a) | search input → `.localizedStandardContains` | accept | Local in-memory Swift string compare over a local store; no SQL/predicate string interpolation, no network, no injection surface — offline-only, no auth surface |
</threat_model>

<verification>
- Build exits 0 on iPhone 17 simulator.
- Search invocable from Hub; results grouped by type; each result opens its existing surface.
- No-results state and all pre-existing empty states render.
- Full VoiceOver/device confirmation is deferred to the Wave-2 owner verification plan (06-04).
</verification>

<success_criteria>
- POL-01: cross-domain search returns items across all types and navigates to a tapped result (SC1).
- POL-02: search no-results state added; every existing section/inbox/Hub empty state confirmed present (SC2).
</success_criteria>

<output>
Create `.planning/phases/06-polish-f/06-01-SUMMARY.md` when done.
</output>
