---
phase: 06-polish-f
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - HabitsTracker/Features/Collections/CollectionItemRow.swift
  - HabitsTracker/Features/Settings/SettingsView.swift
  - HabitsTracker/Services/ExportImportService.swift
autonomous: true
requirements: [POL-04]
must_haves:
  truths:
    - "VoiceOver can reach the Collections tap-to-advance chip as a control and hear that it advances to the next status"
    - "Settings shows the current data schema version and app marketing version in a read-only About row"
    - "Chips, buttons, and the Hub grid use tokens-only colors and support Dynamic Type"
  artifacts:
    - path: "HabitsTracker/Features/Collections/CollectionItemRow.swift"
      provides: "VoiceOver-reachable advance chip with label + hint/named action"
      contains: "accessibilityHint"
    - path: "HabitsTracker/Features/Settings/SettingsView.swift"
      provides: "About section: Version + Data schema rows"
      contains: "About"
  key_links:
    - from: "HabitsTracker/Features/Settings/SettingsView.swift"
      to: "HabitsTracker/Services/ExportImportService.swift"
      via: "schema row reads the exposed schemaVersion constant (no divergent literal)"
      pattern: "schemaVersion"
---

<objective>
Close the POL-04 accessibility + schema-visibility debt (the pre-existing "Next 3"). Priority fix: the Collections tap-to-advance status chip is currently an `.onTapGesture` on a `DKBadge` inside an `.accessibilityElement(children: .ignore)` card (CollectionItemRow lines 49–72) — so VoiceOver cannot reach it and the advance is silent (D-10, a correctness bug). Fix it to ClipRow's reachable-Button-chip + label + hint pattern. Add a read-only Settings "About" section surfacing `schemaVersion` (6) + `CFBundleShortVersionString` (1.0) (D-13). Verify chips/buttons/Hub-grid are tokens-only and Dynamic-Type-safe (D-09 fix-as-found, no app-wide audit).

Purpose: Accessibility is a "done" gate (§9.15); silent state changes under VoiceOver are bugs, not polish.
Output: CollectionItemRow chip a11y fix + SettingsView About rows + a readable schemaVersion constant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-f/06-CONTEXT.md
@.planning/phases/06-polish-f/06-PATTERNS.md

<interfaces>
<!-- Confirmed from the repo. -->
CollectionItemRow current chip (lines 49–72): DKBadge(statusLabel).frame(minWidth:44,minHeight:44)
  .onTapGesture { tapCounter += 1; statusIndex = min(statusIndex+1, terminalIndex) }
  .contextMenu { Button("Reset", role:.destructive) { statusIndex = 0 } }
  .sensoryFeedback(.impact(weight:.light), trigger: tapCounter)
Card is .accessibilityElement(children: .ignore) with .accessibilityAction(named: "Reset status").
statusSet = StatusSetCatalog.set(for: collection.statusSetID); terminalIndex = statusSet?.terminalIndex ?? 0.

ClipRow.statusChip (the correct template, lines 57–69): a Button label:{ DKBadge(...) .frame(minWidth:44,minHeight:44) }
  .buttonStyle(.plain) .sensoryFeedback(...) .accessibilityLabel("Status: ...") .accessibilityHint("Toggles between ...").

ExportImportService: `private let schemaVersion = 6` (line 7). Must become readable for SettingsView.
SettingsView: existing `Section("Appearance"/"Management"/"Backup") { ... }` inside a List with
  .scrollContentBackground(.hidden) / .background(theme.colors.background). CFBundleShortVersionString = "1.0".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix CollectionItemRow tap-to-advance chip for VoiceOver (D-10 priority)</name>
  <files>HabitsTracker/Features/Collections/CollectionItemRow.swift</files>
  <read_first>
    - HabitsTracker/Features/Collections/CollectionItemRow.swift (the file being modified — chip at 49–64, card a11y at 68–72)
    - HabitsTracker/Features/Clips/ClipRow.swift (the correct reachable-Button-chip + label + hint template, lines 57–69)
    - .planning/phases/06-polish-f/06-PATTERNS.md (CollectionItemRow section, lines 115–136; VoiceOver chip contract, lines 199–201)
    - .planning/phases/06-polish-f/06-CONTEXT.md (D-09, D-10)
  </read_first>
  <action>
    Make the advance chip a VoiceOver-reachable control. Because the card is `.accessibilityElement(children: .ignore)` (line 68), the chip's own accessibility is otherwise dropped — the fix is to add a named accessibility action AND a hint at the card level describing the advance outcome (the simplest correct path that keeps the single-element card), OR convert the chip to a `Button` and drop `children: .ignore` in favor of `children: .combine` per ClipRow's shape. Prefer the ClipRow shape for consistency: convert the `.onTapGesture` DKBadge into a `Button { tapCounter += 1; let newIndex = min(item.statusIndex + 1, terminalIndex); if item.statusIndex != newIndex { item.statusIndex = newIndex } } label: { DKBadge(statusLabel, theme: theme).frame(minWidth: 44, minHeight: 44) }.buttonStyle(.plain)`, keep `.sensoryFeedback(.impact(weight: .light), trigger: tapCounter)` so terminal taps still buzz (D-08 behavior preserved), and add `.accessibilityLabel("Status: \(statusLabel), \(item.title)")` + `.accessibilityHint("Advances to the next status")`. Keep the existing `contextMenu { Button("Reset", role: .destructive) { item.statusIndex = 0 } }`.

    Adjust the card's accessibility so the chip Button is reachable as a separate control: change the leading title/position text block to its own combined element (like ClipRow's `.accessibilityElement(children: .combine)` on the text VStack) rather than wrapping the whole card in `children: .ignore`. Preserve the existing named "Reset status" action reachability (either keep it on the card element or move it onto the text element / add a second `.accessibilityAction(named:)`). Do NOT change the advance clamp logic (`min(statusIndex+1, terminalIndex)`) or the sensory-feedback keying on `tapCounter`. Tokens only — no hardcoded colors (§1, §9.4).
  </action>
  <acceptance_criteria>
    - `CollectionItemRow.swift` chip is a `Button` (or an `.accessibilityAction(named:)` describing advance exists) — no bare `.onTapGesture` remains as the sole advance affordance.
    - File contains `.accessibilityHint(` describing advancing to the next status AND an `.accessibilityLabel(` exposing the current status.
    - The advance still clamps at `terminalIndex` (`min(item.statusIndex + 1, terminalIndex)` preserved) and `.sensoryFeedback(.impact(weight: .light), trigger: tapCounter)` is retained.
    - The card no longer swallows the chip: `grep -n "children: .ignore" HabitsTracker/Features/Collections/CollectionItemRow.swift` returns nothing OR the chip Button sits outside the ignored element (verified by the Button being a peer, not a child, of the ignored region).
    - The "Reset status" action / Reset context-menu remains present.
    - `xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <done>The Collections status chip is a VoiceOver-reachable Button exposing current status (label) + advance outcome (hint), clamps at terminal, still buzzes on every tap, and keeps Reset; build exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Settings About row (schema + version) + tokens/Dynamic-Type verify sweep</name>
  <files>HabitsTracker/Features/Settings/SettingsView.swift, HabitsTracker/Services/ExportImportService.swift</files>
  <read_first>
    - HabitsTracker/Features/Settings/SettingsView.swift (existing Section rows + List treatment, lines 31–86)
    - HabitsTracker/Services/ExportImportService.swift (schemaVersion at line 7 — currently `private`)
    - .planning/phases/06-polish-f/06-PATTERNS.md (SettingsView section, lines 146–155)
    - .planning/phases/06-polish-f/06-CONTEXT.md (D-13, D-15, D-09)
    - HabitsTracker/Features/Hub/HubView.swift, HabitsTracker/Features/Clips/ClipRow.swift (targets of the tokens/Dynamic-Type verify sweep)
  </read_first>
  <action>
    Expose the schema version so Settings reads the single source of truth, not a divergent literal: in `ExportImportService.swift` change `private let schemaVersion = 6` to a readable form — prefer `static let currentSchemaVersion = 6` and reference it internally where `schemaVersion` is used (keep the instance behavior identical). Do NOT bump the value — search + a11y are read-side, no persisted field is added (D-15, §9.12); the schemaVersion stays 6.

    In `SettingsView.swift`, add a new `Section("About")` (placed after "Backup") using the existing list-row styling — no new screen, no new DesignKit component (D-13): a `LabeledContent("Version", value: Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String ?? "—")` row and a `LabeledContent("Data schema", value: "v\(ExportImportService.currentSchemaVersion)")` row. Keep the existing `.scrollContentBackground(.hidden)` / `.background(theme.colors.background)` on the List.

    Verify sweep (D-09 fix-as-found, NOT an app-wide audit): confirm the status chips (CollectionItemRow after Task 1, ClipRow, IdeaRow pills), the DesignKit buttons, and the Hub grid tiles use theme tokens only (no hardcoded `Color(...)`/hex) and carry Dynamic-Type-safe text (`minimumScaleFactor` already present on the chip titles). Fix any hardcoded color found on those enumerable surfaces; do NOT expand scope beyond chips/buttons/Hub grid.
  </action>
  <acceptance_criteria>
    - `ExportImportService.swift` exposes a readable schema constant (`static let currentSchemaVersion = 6`); value is still 6 (no bump — D-15).
    - `SettingsView.swift` contains a `Section("About")` with a `Version` row sourced from `CFBundleShortVersionString` and a `Data schema` row sourced from `ExportImportService.currentSchemaVersion` (not a hardcoded `6` literal in the view).
    - `grep -rn "Color(red:\|Color(\.sRGB\|#[0-9A-Fa-f]\{6\}" HabitsTracker/Features/Collections/CollectionItemRow.swift HabitsTracker/Features/Clips/ClipRow.swift HabitsTracker/Features/Ideas/IdeaRow.swift HabitsTracker/Features/Hub/HubView.swift` returns no hardcoded colors (tokens-only, §1/§9.4).
    - `xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <done>Settings shows "Version 1.0" + "Data schema v6" sourced from CFBundleShortVersionString and the exposed schemaVersion constant; enumerated chip/button/grid surfaces are tokens-only; no schema bump; build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline-only, local-first (CLAUDE.md §1) — no network, no auth, no backend. This plan is UI-only (an a11y chip fix + a read-only Settings display row + a constant-visibility change). No trust boundary is crossed; `CFBundleShortVersionString` and the schema constant are compile-time/app-bundle values, not user input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-I | Information Disclosure | Settings About row | accept | Only surfaces the app's own marketing version + non-sensitive schema integer; no PII, offline-only, no network/auth surface |
| T-06-02-E | Elevation (n/a) | chip Button action | accept | Advance/reset only mutate the user's own local CollectionItem.statusIndex; no privilege boundary exists in an offline single-user app |
</threat_model>

<verification>
- Build exits 0 on iPhone 17 simulator.
- CollectionItemRow chip is a reachable Button with label + hint; Reset preserved.
- Settings About shows version + schema from the source-of-truth constant.
- VoiceOver-on-device confirmation of chip reachability is verified in the Wave-2 owner plan (06-04).
</verification>

<success_criteria>
- POL-04: Dynamic Type + VoiceOver labels on chips/buttons/grid, tokens-only colors; schema/version visible in Settings (SC4).
</success_criteria>

<output>
Create `.planning/phases/06-polish-f/06-02-SUMMARY.md` when done.
</output>
