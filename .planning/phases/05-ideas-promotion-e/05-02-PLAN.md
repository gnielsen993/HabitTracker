---
phase: 05-ideas-promotion-e
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - HabitsTracker/Services/ExportImportDTOs.swift
  - HabitsTracker/Services/ExportImportService.swift
  - HabitsTracker/Features/Settings/SettingsView.swift
autonomous: true
requirements: [IDEA-01]
decisions: [D-14]
must_haves:
  truths:
    - "ExportImportService.schemaVersion is 6 (bumped from 5, D-14)"
    - "An IdeaDTO: Codable struct exists mirroring ClipDTO's flat/scalar-only shape: id, title, note: String?, url: String?, isArchived: Bool, createdAt: Date, promotedToKind: String?, promotedToID: UUID?, domainID: UUID?"
    - "exportData(...) accepts an ideas: [Idea] parameter and emits an ideas: [IdeaDTO] array wiring domainID = idea.domain?.id (mirrors the clips: map)"
    - "Import creates Idea instances from bundle.ideas, wiring domain via categoryIndex[dto.domainID] (no id-index map needed — nothing references an Idea by id; forward-link fields round-trip as plain scalars)"
    - "deleteAll deletes Idea before Domain (nullify ordering, same rule the Clip comment states)"
    - "The existing import guard bundle.schemaVersion <= schemaVersion still rejects a newer-than-6 bundle and older bundles (no ideas key) import without error — graceful degradation at the boundary"
    - "SettingsView passes ideas: to the exportData(...) call site with its own @Query of all ideas"
  artifacts:
    - path: "HabitsTracker/Services/ExportImportDTOs.swift"
      provides: "IdeaDTO Codable struct"
      contains: "struct IdeaDTO"
    - path: "HabitsTracker/Services/ExportImportService.swift"
      provides: "schemaVersion 6 + Idea export/import/delete round-trip"
      contains: "schemaVersion = 6"
    - path: "HabitsTracker/Features/Settings/SettingsView.swift"
      provides: "ideas: argument threaded into exportData call"
      contains: "ideas: ideas"
  key_links:
    - from: "HabitsTracker/Services/ExportImportService.swift"
      to: "IdeaDTO"
      via: "export map + import loop"
      pattern: "IdeaDTO\\("
    - from: "HabitsTracker/Features/Settings/SettingsView.swift"
      to: "exportData"
      via: "ideas: argument"
      pattern: "ideas: ideas"
---

<objective>
Bump `ExportImportService.schemaVersion` 5→6 and extend the export/import round-trip to cover `Idea` (D-14), so the phase keeps its own backup round-trip green (baseline DoD). Full 8-type export/import completeness stays Phase 6 — this plan only adds what Phase 5 introduces.

Purpose: the baseline DoD requires an export/import round-trip green for any type touched. Idea carries `title`/`note?`/`url?`/`isArchived`/`createdAt`/`domain?` plus the scalar forward-link fields — all must survive an export→wipe→import cycle. The forward-link fields are scalars (not a SwiftData relationship), so they round-trip as plain DTO values, no id-lookup wiring.
Output: `IdeaDTO`, the three service touch-points (export map, import loop, deleteAll ordering), the version bump, and the `SettingsView` call-site update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ideas-promotion-e/05-CONTEXT.md
@.planning/phases/05-ideas-promotion-e/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IdeaDTO</name>
  <files>HabitsTracker/Services/ExportImportDTOs.swift</files>
  <read_first>
    - HabitsTracker/Services/ExportImportDTOs.swift (the `RuleDTO` / `ClipDTO` structs ~lines 75-95 — the flat scalar-only DTO shape to mirror; no nested objects)
    - HabitsTracker/Models/Idea.swift (the fields to serialize — created in 05-01)
    - .planning/phases/05-ideas-promotion-e/05-PATTERNS.md (section "ExportImportDTOs.swift")
  </read_first>
  <action>
    Add `struct IdeaDTO: Codable` with stored `let` fields: `id: UUID`, `title: String`, `note: String?`, `url: String?`, `isArchived: Bool`, `createdAt: Date`, `promotedToKind: String?`, `promotedToID: UUID?`, `domainID: UUID?` — same flat, scalar-only shape as `ClipDTO` (no nested objects). Place it adjacent to `ClipDTO`.
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `ExportImportDTOs.swift` contains `struct IdeaDTO: Codable` with exactly the fields id/title/note/url/isArchived/createdAt/promotedToKind/promotedToID/domainID, all scalar.
    - Build exits 0.
  </acceptance_criteria>
  <done>IdeaDTO exists mirroring ClipDTO's scalar shape; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Idea export/import/delete + schemaVersion 6 + call site</name>
  <files>HabitsTracker/Services/ExportImportService.swift, HabitsTracker/Features/Settings/SettingsView.swift</files>
  <read_first>
    - HabitsTracker/Services/ExportImportService.swift (line 7 `schemaVersion = 5`; the Clip export map ~lines 124-136; the Clip import block ~lines 186-200; the deleteAll ordering ~lines 292-304 incl. the "Clip.domain is .nullify — delete before Domain" comment; the import guard at line 147 `bundle.schemaVersion <= schemaVersion`; the `exportData(` signature at line 23)
    - HabitsTracker/Features/Settings/SettingsView.swift (line 61 the `exportData(...)` call site + the existing @Query declarations for categories/habits/rules/collections/clips to copy an `ideas` @Query alongside)
    - HabitsTracker/Services/ExportImportDTOs.swift (IdeaDTO from Task 1)
    - .planning/phases/05-ideas-promotion-e/05-PATTERNS.md (section "ExportImportService.swift" — the three Clip touch-points to copy)
  </read_first>
  <action>
    In `ExportImportService.swift`: (1) change `private let schemaVersion = 5` to `= 6`. (2) Add an `ideas: [Idea]` parameter to `exportData(...)` (alongside `clips:`) and add an `ideas:` array to the emitted bundle mapping each Idea to `IdeaDTO(id:title:note:url:isArchived:createdAt:promotedToKind: $0.promotedToKindRaw, promotedToID: $0.promotedToID, domainID: $0.domain?.id)` — copy the `clips.map { ClipDTO(...) }` shape. (3) In the import path, after the Clips import block, add a loop over `bundle.ideas` constructing `Idea(...)` with `domain: dto.domainID.flatMap { categoryIndex[$0] }` and setting `promotedToKindRaw = dto.promotedToKind` / `promotedToID = dto.promotedToID`, then `context.insert(idea)` — no id-index map. Guard against an older bundle lacking the key (make `bundle.ideas` decode-optional or default to `[]` so a schemaVersion-≤5 backup imports cleanly). (4) In `deleteAll`, add `try context.delete(model: Idea.self)` immediately BEFORE `try context.delete(model: Domain.self)` (nullify ordering, same rule as Clip). Leave the `bundle.schemaVersion <= schemaVersion` guard unchanged (it now admits ≤6, still rejects newer).
    In `SettingsView.swift`: add an `@Query private var ideas: [Idea]` alongside the existing type queries and pass `ideas: ideas` into the `exportData(...)` call at line 61.
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `ExportImportService.swift` contains `schemaVersion = 6`, an `ideas:` parameter in `exportData`, an `IdeaDTO(` construction in the export map, an `Idea(` construction in the import loop wiring `domainID` via `categoryIndex`, and `try context.delete(model: Idea.self)` positioned before `Domain.self` in `deleteAll`.
    - The bundle model decodes an older backup (no `ideas` key) without throwing — `bundle.ideas` defaults to empty.
    - `SettingsView.swift` contains `ideas: ideas` in the `exportData(...)` call and an `@Query` for ideas.
    - Build exits 0 (all call sites satisfied).
  </acceptance_criteria>
  <done>schemaVersion is 6, Idea round-trips through export/import, deleteAll respects nullify ordering, older backups degrade gracefully, and the SettingsView export call compiles with the new argument.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported JSON backup file → app store | A user-supplied backup file (potentially malformed, older-version, or newer-version) is decoded and replace-imported. This is the phase's one untrusted-input boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering / DoS (malformed input) | ExportImportService import path | mitigate | The existing `bundle.schemaVersion <= schemaVersion` guard rejects a newer-than-6 bundle with a typed `ImportError`; `bundle.ideas` decodes as optional/defaulted so an older (≤5) backup with no ideas key imports without throwing — graceful degradation at the boundary (CLAUDE.md "validate at boundaries, fail gracefully"). Idea DTO fields are scalar Codable — no nested/recursive structure to exploit. |
| T-05-03 | Tampering (data integrity) | deleteAll ordering on replace-import | mitigate | Idea deleted before Domain so the `.nullify` relationship never dangles mid-wipe (same rule proven for Clip in 04-04). |

Offline, single-user, local file only. No network, no new dependency. No high-severity threats.
</threat_model>

<verification>
- `xcodebuild ... build` exits 0.
- Grep: `schemaVersion = 6`, `struct IdeaDTO`, `IdeaDTO(`, `try context.delete(model: Idea.self)` present; `ideas: ideas` present in SettingsView.
- Reasoned round-trip: export emits ideas → deleteAll removes Idea before Domain → import reconstructs Idea + rewires domain via categoryIndex (executor states this reasoning in the SUMMARY; runtime round-trip is exercised in the owner device check bundled with a later phase, matching the Clip 04-04 precedent).
</verification>

<success_criteria>
schemaVersion is 6 and Idea participates in export → wipe → import with domain reference and forward-link scalars preserved; older/newer backups are handled gracefully at the import boundary; app builds clean.
</success_criteria>

<output>
Create `.planning/phases/05-ideas-promotion-e/05-02-SUMMARY.md` when done.
</output>
