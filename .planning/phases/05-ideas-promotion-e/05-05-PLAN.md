---
phase: 05-ideas-promotion-e
plan: 05
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - HabitsTracker/Features/Ideas/IdeaCaptureSheet.swift
  - HabitsTracker/Features/Today/TodayView.swift
autonomous: true
requirements: [IDEA-02]
decisions: [D-01, D-02, D-08]
must_haves:
  truths:
    - "TodayView's NavigationStack gains a native top-trailing '+' toolbar item (net-new chrome) that presents IdeaCaptureSheet() via .sheet — reachable WITHOUT leaving Today and adding NO row to Today's list (D-01/D-02, IDEA-02, SC1)"
    - "IdeaCaptureSheet is a genuinely title-only sheet (D-08): one TextField 'What's the idea?', autofocused; NO note/url/domain fields exposed"
    - "IdeaCaptureSheet(domain: Domain? = nil) create init (global '+' passes nil → unfiled idea) and IdeaCaptureSheet(idea: Idea) edit init (title prefilled) — one shared component (§4 reuse)"
    - "Create-mode is orphan-free (fill-then-commit): Idea(title:, domain:) is inserted + saved ONLY inside the Save action; Cancel inserts/changes nothing (HabitCreateSheet precedent)"
    - "Save CTA reads 'Add Idea' (create) / 'Save Changes' (edit), disabled until trimmedTitle non-empty; validation hint 'Give this a name to continue.' shows when empty"
    - "Edit mode has a destructive 'Delete Idea' row with a confirmationDialog ('Delete this idea?' / 'This can't be undone.' / confirm 'Delete Idea') — hard delete, the mis-capture escape hatch (S2)"
    - "A globally-captured idea (domain == nil, isArchived == false) is exactly what the Hub inbox query surfaces — it does not appear on Today"
  artifacts:
    - path: "HabitsTracker/Features/Ideas/IdeaCaptureSheet.swift"
      provides: "title-only create/edit capture sheet (shared component)"
      contains: "struct IdeaCaptureSheet"
    - path: "HabitsTracker/Features/Today/TodayView.swift"
      provides: "top-trailing capture '+' toolbar item + .sheet"
      contains: "IdeaCaptureSheet"
  key_links:
    - from: "HabitsTracker/Features/Today/TodayView.swift"
      to: "IdeaCaptureSheet"
      via: ".toolbar plus button → .sheet(isPresented: $showingCapture)"
      pattern: "IdeaCaptureSheet\\(\\)"
    - from: "HabitsTracker/Features/Ideas/IdeaCaptureSheet.swift"
      to: "Idea"
      via: "insert-on-save (create) / mutate title (edit)"
      pattern: "modelContext.insert"
---

<objective>
Deliver the capture-first spine's front door: a native top-trailing "+" on Today's NavigationStack (net-new chrome, zero Today-list pollution) that opens a genuinely title-only `IdeaCaptureSheet`, whose Save drops an unfiled `Idea` into the Hub inbox (D-01/D-02/D-08, IDEA-02). This is the shared sheet the in-domain Ideas "+" (05-09) and the row tap-to-edit (05-07) also reuse.

Purpose: IDEA-02 (SC1) — the 11pm "saw a TikTok" path must be near-zero friction: one tap, keyboard already up, title-only, no domain required, and it must NOT add a row to Today (that would violate `DEC-today-is-hero`). Everything heavier (filing, promoting) is deferred to the inbox.
Output: `IdeaCaptureSheet.swift` (new) + the TodayView toolbar item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ideas-promotion-e/05-CONTEXT.md
@.planning/phases/05-ideas-promotion-e/05-UI-SPEC.md
@.planning/phases/05-ideas-promotion-e/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IdeaCaptureSheet (title-only create/edit)</name>
  <files>HabitsTracker/Features/Ideas/IdeaCaptureSheet.swift</files>
  <behavior>
    - Create init with domain nil → Save inserts an Idea(title: trimmed, domain: nil) exactly once and saves; the idea has isArchived == false (surfaces in the inbox query).
    - Create init with a domain → Save inserts Idea(title:, domain: thatDomain) (place-first filed idea).
    - Edit init(idea:) → title field prefilled with idea.title; Save mutates idea.title in place (no new insert).
    - Save disabled while trimmedTitle isEmpty; Cancel inserts/changes nothing.
    - Edit-mode Delete removes the idea after confirmation.
  </behavior>
  <read_first>
    - HabitsTracker/Features/Rules/RuleEditorView.swift (lines 26-60 the EditorMode enum + dual-init pattern; lines 105-119 the titleSection with the verbatim validation-hint copy/token recipe; lines 83-100 + 176-227 the toolbar CTA + destructive confirmationDialog shape)
    - HabitsTracker/Features/Habits/HabitCreateSheet.swift (lines 65-89 + 189-220 the orphan-free insert-only-on-save discipline; lines 87-88 .presentationDetents)
    - HabitsTracker/Features/Clips/ClipEditorView.swift (lines 4-6 the os.Logger header; lines 59/152 the @FocusState mechanism to reuse for autofocus-on-appear)
    - HabitsTracker/Models/Idea.swift (the create/edit target — from 05-01)
    - .planning/phases/05-ideas-promotion-e/05-UI-SPEC.md (S2 + the Copywriting Contract — exact strings) and 05-PATTERNS.md (section "IdeaCaptureSheet.swift")
  </read_first>
  <action>
    Create `HabitsTracker/Features/Ideas/IdeaCaptureSheet.swift` (new `Features/Ideas/` folder — synchronized root group auto-registers, do NOT touch project.pbxproj). Use a `private enum Mode { case create(domain: Domain?); case edit(idea: Idea) }` + `init(domain: Domain? = nil)` and `init(idea: Idea)` (dual-init, RuleEditorView shape). Body: a `NavigationStack` wrapping a single title `TextField("What's the idea?", text: $title)` (`theme.typography.body`) under a "Title" header (`theme.typography.headline`); autofocus via `@FocusState` set true in `.onAppear`. Below: the `if trimmedTitle.isEmpty { Text("Give this a name to continue.") }` hint (verbatim). Toolbar: `.cancellationAction` "Cancel" (dismiss, insert nothing); `.confirmationAction` a save CTA titled "Add Idea" (create) / "Save Changes" (edit), `.disabled(trimmedTitle.isEmpty)`. Save: create-mode constructs `Idea(title: trimmedTitle, domain: mode's domain)`, `modelContext.insert(...)`, `try? modelContext.save()`, `dismiss()`; edit-mode sets `idea.title = trimmedTitle`, save, dismiss. Edit-mode only: a destructive "Delete Idea" row (`Button(role:.destructive)`, `theme.colors.danger`) → `.confirmationDialog` titled "Delete this idea?" message "This can't be undone." confirm "Delete Idea" (deletes the idea + saves + dismisses). navigationTitle "New Idea" (create) / "Edit Idea" (edit). Use tokens only (radii.sheet, spacing.*), ≥44pt tap targets, accessibilityLabel "Idea title" on the field. Use `.presentationDetents([.medium])` (planner's call — keep it light). Add an `os.Logger` header only if an error path needs it. Do NOT expose note/url/domain fields (D-08 literal, UI-SPEC Open Item #1).
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `IdeaCaptureSheet.swift` contains `struct IdeaCaptureSheet`, `init(domain: Domain? = nil)`, `init(idea: Idea)`, a single `TextField("What's the idea?"`, and NO TextField/Picker for note/url/domain.
    - Contains the strings "Add Idea", "Save Changes", "Give this a name to continue.", "Delete this idea?", "This can't be undone.", "New Idea", "Edit Idea" (Copywriting Contract).
    - `modelContext.insert` appears only inside the Save action (grep: no insert in an init or `.onAppear`); Cancel path has no insert.
    - No hard-coded colors/radii/spacing (grep: no `Color(`, no numeric `.padding(` literals except the ≥44 tap-target frames); uses `theme.` tokens.
    - Build exits 0.
  </acceptance_criteria>
  <done>IdeaCaptureSheet is a shared title-only create/edit sheet with orphan-free save, verbatim copy, edit-mode hard-delete confirmation, autofocus, tokens-only styling; builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Today capture '+' toolbar item</name>
  <files>HabitsTracker/Features/Today/TodayView.swift</files>
  <read_first>
    - HabitsTracker/Features/Today/TodayView.swift (line 14 the existing `@State private var saveError`; line 24 the NavigationStack that currently has NO .toolbar; lines 97-100 the existing .alert to sit the new .toolbar/.sheet alongside without disturbing the ScrollView/VStack/.task body)
    - .planning/phases/05-ideas-promotion-e/05-PATTERNS.md (section "TodayView.swift" — the exact .toolbar/.topBarTrailing + .sheet recipe and the "do not touch any other part of the body" instruction)
    - .planning/phases/05-ideas-promotion-e/05-UI-SPEC.md (S1 — accessibilityLabel "Add idea", hint "Opens a quick capture sheet", accentPrimary tint, .font(.system(size:18, weight:.semibold)))
  </read_first>
  <action>
    Add `@State private var showingCapture = false` next to `saveError` (line 14). Attach a `.toolbar { ToolbarItem(placement: .topBarTrailing) { Button { showingCapture = true } label: { Image(systemName: "plus").font(.system(size: 18, weight: .semibold)).foregroundStyle(theme.colors.accentPrimary) }.accessibilityLabel("Add idea").accessibilityHint("Opens a quick capture sheet") } }` and a `.sheet(isPresented: $showingCapture) { IdeaCaptureSheet() }` on the NavigationStack's content. Do NOT modify the ScrollView/VStack content, the `.task` bootstrap, or the existing `.alert` — net-new chrome only (D-01/D-02 "zero overlay/list pollution"). `IdeaCaptureSheet()` passes no domain → the captured idea is unfiled and lands in the inbox, NOT on Today.
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `TodayView.swift` contains `.toolbar`, `placement: .topBarTrailing`, `IdeaCaptureSheet()`, `showingCapture`, accessibilityLabel "Add idea".
    - Grep confirms the ScrollView/VStack row-rendering content and `.task` are unchanged (diff limited to the new @State + .toolbar + .sheet); NO code adds an Idea to Today's habit list.
    - The "+" tints via `theme.colors.accentPrimary` (no hard-coded color), matching the section-header "+" glyph treatment.
    - Build exits 0.
  </acceptance_criteria>
  <done>Today's NavigationStack has a top-trailing capture "+" presenting IdeaCaptureSheet; Today's list is untouched; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | User-typed idea title is free text persisted locally to the user's own store — no network, no other consumer, no privilege change. SwiftData parameterizes storage (no injection surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering (orphan/empty records) | IdeaCaptureSheet create path | mitigate | Fill-then-commit: Idea inserted only on Save with a non-empty trimmed title (Save disabled otherwise); Cancel inserts nothing — no orphan/empty ideas can accumulate (HabitCreateSheet D-04 precedent). |

Offline, single-user, local. No network, no new dependency. No high-severity threats.
</threat_model>

<verification>
- `xcodebuild ... build` exits 0.
- Grep: IdeaCaptureSheet has one idea TextField only, insert only on Save, tokens-only.
- Grep: TodayView diff is additive chrome; no Today-row insertion path added.
</verification>

<success_criteria>
A user can tap "+" on Today without leaving Today, type a title, Save, and the unfiled idea lands in the inbox (not on Today). The capture sheet is title-only, orphan-free, edit-capable with a hard-delete escape hatch, and tokens-only. (Owner on-device visual confirmation of the capture flow rides the phase-end owner check.)
</success_criteria>

<output>
Create `.planning/phases/05-ideas-promotion-e/05-05-SUMMARY.md` when done.
</output>
