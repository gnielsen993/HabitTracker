---
phase: 05-ideas-promotion-e
plan: 09
type: execute
wave: 5
depends_on: [05-05, 05-07]
files_modified:
  - HabitsTracker/Features/Hub/DomainDetailView.swift
autonomous: true
requirements: [IDEA-01, IDEA-03]
decisions: [D-08, D-09]
must_haves:
  truths:
    - "DomainDetailView gains an Ideas section appended to nonEmptySections at the reserved 'Phase E: append Ideas section here' hook (line 90), following the buildClipsSection/content/header trio shape (D-09)"
    - "buildIdeasSection filters domain.ideas.filter { !$0.isArchived }.sorted { $0.createdAt > $1.createdAt } and returns nil (section hidden) when empty — mirrors the Clips section contract exactly"
    - "The section header is 'Ideas' (title/.isHeader) with a trailing '+' (plus glyph, .font(.system(size:18,weight:.semibold)), accentPrimary, ≥44pt) presenting IdeaCaptureSheet(domain: domain) — pre-filed under this domain, place-first, no domain picker (D-09); accessibilityLabel 'Add idea to \\(domain.name)'"
    - "Each idea renders as IdeaRow(idea: idea) with NO NavigationLink/detail-view wrapper (the deliberate deviation from the Clips trio — D-08: IdeaRow owns its own tap-to-edit); File pill is auto-absent on these filed rows (idea.domain != nil), only Promote shows"
    - "A @State creatingIdea flag + a .sheet(isPresented:) { IdeaCaptureSheet(domain: domain) } are added alongside the existing creatingRule/creatingCollection/creatingClip sheets"
  artifacts:
    - path: "HabitsTracker/Features/Hub/DomainDetailView.swift"
      provides: "Ideas domain-section trio + in-domain '+' + creatingIdea sheet"
      contains: "buildIdeasSection"
  key_links:
    - from: "HabitsTracker/Features/Hub/DomainDetailView.swift"
      to: "IdeaRow"
      via: "Ideas section renders rows (no NavigationLink wrapper)"
      pattern: "IdeaRow(idea:"
    - from: "HabitsTracker/Features/Hub/DomainDetailView.swift"
      to: "IdeaCaptureSheet"
      via: "section-header '+' presents place-first capture"
      pattern: "IdeaCaptureSheet(domain: domain)"
---

<objective>
Wire the Ideas section into `DomainDetailView` at the reserved Phase E hook (line 90), following the established per-section trio shape with the one deliberate deviation: idea rows have no NavigationLink/detail-view wrapper (D-08). Add the in-domain Ideas "+" that captures an idea pre-filed under the domain (D-09, IDEA-01/IDEA-03).

Purpose: IDEA-01/IDEA-03 — ideas filed under a domain must be found in that domain's Ideas section (place-first, the in-domain "+" per §3), completing the surface where filed ideas live. Keeping the per-section "+" pattern (not a unified domain "+" picker) is D-09's explicit instruction — the global quick-add is the unifier.
Output: the `buildIdeasSection`/content/header trio + the `creatingIdea` sheet in DomainDetailView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ideas-promotion-e/05-CONTEXT.md
@.planning/phases/05-ideas-promotion-e/05-UI-SPEC.md
@.planning/phases/05-ideas-promotion-e/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ideas section trio + hook registration + in-domain '+'</name>
  <files>HabitsTracker/Features/Hub/DomainDetailView.swift</files>
  <read_first>
    - HabitsTracker/Features/Hub/DomainDetailView.swift (lines 28-30 the creatingRule/creatingCollection/creatingClip @State; lines 56-64 the sibling .sheet modifiers; line 85 the Clips section registration + line 90 the "// Phase E: append Ideas section here." hook; lines 197-248 the buildClipsSection/clipsSectionContent/clipsSectionHeader trio to mirror verbatim — INCLUDING the header "+" button styling at lines ~232-240)
    - HabitsTracker/Features/Ideas/IdeaRow.swift (the row rendered directly — no NavigationLink — from 05-07)
    - HabitsTracker/Features/Ideas/IdeaCaptureSheet.swift (the in-domain "+" destination, place-first init(domain:) — from 05-05)
    - .planning/phases/05-ideas-promotion-e/05-UI-SPEC.md (S5 + Copywriting Contract: header "Ideas", "+" accessibility "Add idea to {domain}") and 05-PATTERNS.md (section "DomainDetailView.swift" — the verbatim trio + the "no NavigationLink" deviation)
  </read_first>
  <action>
    Copy the Clips trio into an Ideas trio: `buildIdeasSection(theme:) -> DomainSection?` filtering `domain.ideas.filter { !$0.isArchived }.sorted { $0.createdAt > $1.createdAt }`, returning nil when empty, else a `DomainSection(id: "ideas", title: "Ideas", content: AnyView(ideasSectionContent(...)))`; `ideasSectionContent(ideas:theme:)` = a `VStack(alignment:.leading, spacing: theme.spacing.m)` with `ideasSectionHeader(theme:)` then `ForEach(ideas, id: \.id) { IdeaRow(idea: $0) }` — render `IdeaRow` DIRECTLY with NO `NavigationLink`/detail wrapper (D-08 deviation; IdeaRow owns tap-to-edit); `ideasSectionHeader(theme:)` = the Clips header shape with `Text("Ideas")` (`theme.typography.title`, `.accessibilityAddTraits(.isHeader)`), Spacer, and the "+" Button (`Image(systemName:"plus")`, `.font(.system(size:18,weight:.semibold))`, `theme.colors.accentPrimary`, `.frame(minWidth:44,minHeight:44)`) setting `creatingIdea = true`, `accessibilityLabel("Add idea to \(domain.name)")`. Register at line 90: `if let ideasSection = buildIdeasSection(theme: theme) { sections.append(ideasSection) }`. Add `@State private var creatingIdea = false` beside the existing creating flags and `.sheet(isPresented: $creatingIdea) { IdeaCaptureSheet(domain: domain) }` beside the other section sheets.
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `DomainDetailView.swift` contains `buildIdeasSection`, `ideasSectionContent`, `ideasSectionHeader`, the section registration `sections.append(ideasSection)` replacing the line-90 hook comment, `@State private var creatingIdea`, and `.sheet(isPresented: $creatingIdea) { IdeaCaptureSheet(domain: domain) }`.
    - `IdeaRow(idea:` is rendered WITHOUT a surrounding `NavigationLink` (grep: no NavigationLink in ideasSectionContent) — the D-08 deviation.
    - Section header is "Ideas" with `.isHeader` and a "+" whose accessibilityLabel is "Add idea to \(domain.name)"; filter/sort is `!isArchived` + createdAt descending.
    - Tokens-only; the "+" reuses the existing `.font(.system(size:18,weight:.semibold))` glyph exception. Build exits 0.
  </acceptance_criteria>
  <done>DomainDetailView renders a filed-ideas section (rows only, no detail wrapper) with an in-domain place-first "+"; hidden when empty; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Read-only rendering of the domain's own ideas + a capture sheet over the local store. No new external input, network, or persistence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering (orphan/empty records) | in-domain "+" capture | mitigate | Reuses IdeaCaptureSheet's fill-then-commit save (05-05) — an idea is inserted only on Save with a non-empty title; the place-first domain is set at construction. No orphan path. |

Offline, single-user, local. No network, no new dependency. No high-severity threats.
</threat_model>

<verification>
- `xcodebuild ... build` exits 0.
- Grep: `buildIdeasSection`, `id: "ideas"`, `IdeaRow(idea:` (no wrapping NavigationLink), `creatingIdea`, `IdeaCaptureSheet(domain: domain)`, `"Add idea to \(domain.name)"`.
</verification>

<success_criteria>
A domain with filed ideas shows an Ideas section (rows with inline Promote, no detail view) and an in-domain "+" that captures an idea pre-filed under that domain; the section hides when empty; build clean. (Owner on-device confirmation rides the phase-end owner check in 05-10.)
</success_criteria>

<output>
Create `.planning/phases/05-ideas-promotion-e/05-09-SUMMARY.md` when done.
</output>
