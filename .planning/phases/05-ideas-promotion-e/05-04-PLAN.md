---
phase: 05-ideas-promotion-e
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .planning/phases/05-ideas-promotion-e/05-UPGRADE-TEST-EVIDENCE.md
autonomous: false
requirements: [IDEA-01]
decisions: [D-13]
must_haves:
  truths:
    - "Upgrading over a Phase-4 store (all prior domains/habits/rules/collections/collectionItems/clips/history) launches on the new Idea-expanded schema with ALL prior data intact and no crash (§9.12 mandatory upgrade test, D-13)"
    - "The new Idea type is available as an empty type post-migration (ZIDEA / Idea table added, no existing rows destroyed)"
    - "Evidence is recorded in 05-UPGRADE-TEST-EVIDENCE.md (mirrors the 04-clips-d/04-UPGRADE-TEST-EVIDENCE.md precedent) OR the owner signs off a device upgrade"
  artifacts:
    - path: ".planning/phases/05-ideas-promotion-e/05-UPGRADE-TEST-EVIDENCE.md"
      provides: "recorded upgrade-test evidence (automated simctl procedure or owner device sign-off)"
      contains: "Idea"
  key_links:
    - from: "Phase-4 on-disk store"
      to: "Idea-expanded schema"
      via: "plan-less inferred lightweight migration"
      pattern: "data intact"
---

<objective>
Prove the D-11/D-13 schema expansion (`Idea` @Model + `Domain.ideas` inverse) migrates a real shipped Phase-4 store with ZERO data loss — the MANDATORY upgrade test the playbook (§9.12) and REQUIREMENTS baseline DoD require before any @Model change merges. This is a device-verification checkpoint per §9.7 (the iOS 26 sim cannot launch the @Model-persistence test host), so it is `autonomous: false`.

Purpose: additive-defaulted migration is only *believed* safe until proven against a store that already holds user data. The CLIP-01 upgrade test (04-01) established an accepted automated path — the simctl migration procedure + sentinel control — recorded in `04-clips-d/04-UPGRADE-TEST-EVIDENCE.md`; reuse it here if feasible, else fall back to owner device verification.
Output: `05-UPGRADE-TEST-EVIDENCE.md` (evidence + verdict).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ideas-promotion-e/05-CONTEXT.md
@Docs/SCHEMA_MIGRATION_PLAYBOOK.md
@.planning/phases/04-clips-d/04-UPGRADE-TEST-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the automated simctl upgrade-migration procedure (reuse the CLIP-01 precedent)</name>
  <files>.planning/phases/05-ideas-promotion-e/05-UPGRADE-TEST-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/04-clips-d/04-UPGRADE-TEST-EVIDENCE.md (the exact automated procedure that RESOLVED CLIP-01: install prior build → seed data → install over with new build → confirm launch + data intact + new empty table added, with a sentinel control)
    - Docs/SCHEMA_MIGRATION_PLAYBOOK.md (the mandatory upgrade-test steps + Forbidden Moves)
    - CLAUDE.md §9.7 (clean-sim reset remedy: `xcrun simctl shutdown all && xcrun simctl erase <device-id>`) and §8 (build command, bundle id `lauterstar.HabitsTracker`)
  </read_first>
  <action>
    Reproduce the 04-clips-d automated upgrade procedure for the Idea expansion: (1) build the LAST pre-Idea commit's app (a Phase-4/schemaVersion-5 binary) or use the existing shipped store, install to a clean iPhone 17 sim, and seed representative data (≥1 domain with habits + rules + a collection with items + a clip); (2) build the current Idea-expanded binary and install OVER the same container (do NOT erase between installs — that is the whole point); (3) launch and confirm no crash + all seeded rows still present; (4) inspect the migrated SQLite store to confirm the Idea table (ZIDEA or equivalent) was added empty and no prior table lost rows. Record every command, the sentinel control, and the PASS/FAIL verdict in `05-UPGRADE-TEST-EVIDENCE.md` using the 04 evidence file as the template. If the simctl procedure is genuinely infeasible on this toolchain, record why and defer to Task 2.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-ideas-promotion-e/05-UPGRADE-TEST-EVIDENCE.md && grep -qi "idea" .planning/phases/05-ideas-promotion-e/05-UPGRADE-TEST-EVIDENCE.md && echo EVIDENCE_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `05-UPGRADE-TEST-EVIDENCE.md` exists and records: the commands run, the seeded pre-migration data, the post-migration launch result, confirmation that prior data is intact, and confirmation the Idea table was added empty.
    - Verdict line is explicit PASS (data intact, no crash) or a recorded infeasibility that hands off to Task 2.
    - No `migrationPlan:` was introduced to make it pass (Forbidden Move — inferred migration only).
  </acceptance_criteria>
  <done>Automated simctl upgrade evidence recorded with an explicit PASS verdict, or a documented infeasibility deferring to the owner checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Idea schema expansion (Idea @Model + Domain.ideas inverse + plan-less container registration) migrating over an existing store. Task 1 attempted the automated simctl migration procedure and recorded evidence.</what-built>
  <how-to-verify>
    Only required if Task 1 recorded infeasibility, or to sign off the automated evidence on real hardware:
    1. Install the current shipped/TestFlight build (Phase-4, schemaVersion 5) on your device; confirm your existing habits/domains/rules/collections/clips are present.
    2. Build+install this Idea-expanded build OVER it (do not delete the app).
    3. Launch. Confirm: the app opens with NO crash, all prior habits/domains/rules/collections/clips/history are intact, Today is unchanged, and the tab bar is still 4 tabs.
    4. Confirm the Hub inbox card is absent (no ideas captured yet) — the schema is present but empty.
  </how-to-verify>
  <resume-signal>Type "approved" (data intact, no crash) or describe what was lost/broken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing shipped on-disk store → upgraded binary | The single highest-value integrity boundary in the phase: a real user's accumulated data opened by the schema-expanded app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering (data integrity / data loss) | plan-less inferred migration over a populated store | mitigate | This plan IS the mitigation for T-05-01 — an empirical upgrade test that fails loudly if any prior data is lost or the store fails to open. Additive-defaulted fields + no `migrationPlan:` keep the migration in the inferred-lightweight lane the playbook proves safe. Blocking checkpoint — cannot silently pass. |

Offline, single-user, local store. No network, no new dependency. This is the one place a bug would look like data loss to a shipped user, hence the blocking gate.
</threat_model>

<verification>
- `05-UPGRADE-TEST-EVIDENCE.md` exists with an explicit PASS verdict (automated) or owner "approved" (device).
- No `migrationPlan:` introduced anywhere (grep the app target).
- Prior-data-intact + Idea-table-added-empty both affirmed in the evidence.
</verification>

<success_criteria>
An existing Phase-4 store upgrades to the Idea-expanded schema with all prior data intact and no crash, evidenced automatically or by owner device sign-off — the mandatory §9.12 gate cleared before Phase 5 UI ships.
</success_criteria>

<output>
Create `.planning/phases/05-ideas-promotion-e/05-04-SUMMARY.md` when done.
</output>
