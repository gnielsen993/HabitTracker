---
phase: 05-ideas-promotion-e
plan: 07
type: execute
wave: 4
depends_on: [05-05, 05-06]
files_modified:
  - HabitsTracker/Features/Ideas/IdeaRow.swift
autonomous: true
requirements: [IDEA-03, IDEA-04, IDEA-05]
decisions: [D-05, D-08, D-10]
must_haves:
  truths:
    - "IdeaRow is a reusable, data-driven presentational row (§9.2, D-05): takes only an Idea value, owns no @Query for its subject; shared by InboxView (05-08) and the Ideas section (05-09)"
    - "Row 1 (title + optional note caption) tapping opens IdeaCaptureSheet(idea: idea) in edit mode — the row's only 'detail' affordance (D-08: NO NavigationLink, NO IdeaDetailView)"
    - "Row 2 shows a File pill (a Menu of all domains) ONLY when idea.domain == nil — tapping a domain sets idea.domain + saves (File keeps it an Idea, IDEA-03/SC2); and a Promote pill (a Menu: Rule / Habit / Collection item) ALWAYS shown (D-07)"
    - "Promote → Rule presents RuleEditorView(promotingIdea: idea); Promote → Habit presents HabitCreateSheet(source: .idea(idea)) with an onSaved that calls PromoteService.archiveAndForwardLink(idea:as:.habit, targetID: habit.id); Promote → Collection item presents PromoteToCollectionPicker(idea:) (IDEA-04/IDEA-05)"
    - "Pills use the DKBadge-style recipe (caption.weight(.semibold), accentPrimary on highlight, radii.chip), ≥44pt tap targets, spacing.s between them; tokens-only, no accentColor(forToken:) per the UI-SPEC Color rule"
    - "Accessibility (§9.15, S6): Row 1 is a combined element labelled '{title}, idea'; the File and Promote Menus stay SEPARATE, individually VoiceOver-reachable ('File idea, choose a domain' / 'Promote idea, choose a type') — never absorbed into the combined label"
  artifacts:
    - path: "HabitsTracker/Features/Ideas/IdeaRow.swift"
      provides: "reusable idea row with tap-to-edit + inline File/Promote menus + promote routing"
      contains: "struct IdeaRow"
  key_links:
    - from: "HabitsTracker/Features/Ideas/IdeaRow.swift"
      to: "IdeaCaptureSheet"
      via: "Row 1 tap → edit sheet"
      pattern: "IdeaCaptureSheet(idea:"
    - from: "HabitsTracker/Features/Ideas/IdeaRow.swift"
      to: "PromoteService"
      via: "habit onSaved consume"
      pattern: "PromoteService.archiveAndForwardLink"
    - from: "HabitsTracker/Features/Ideas/IdeaRow.swift"
      to: "RuleEditorView"
      via: "promote → rule editor"
      pattern: "RuleEditorView(promotingIdea:"
---

<objective>
Build `IdeaRow` — the one reusable, data-driven row (§9.2, D-05) shared by the inbox (05-08) and every domain's Ideas section (05-09). It carries the two one-tap graduations inline: **File** (a domain Menu, shown only when unfiled) and **Promote** (a type Menu routing into the prefilled target editors from 05-06). Row 1 tap opens the title-only edit sheet — the deliberately shallow detail affordance (D-08, no IdeaDetailView).

Purpose: IDEA-03's "two one-tap graduations from the inbox row" — File keeps the item an Idea and gives it a domain (SC2); Promote consumes it into a Rule/Habit/Collection item (SC3) using the editors + PromoteService from 05-06. This is where the inbox actually gets emptied.
Output: `IdeaRow.swift` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ideas-promotion-e/05-CONTEXT.md
@.planning/phases/05-ideas-promotion-e/05-UI-SPEC.md
@.planning/phases/05-ideas-promotion-e/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: IdeaRow layout + tap-to-edit + File pill (domain Menu)</name>
  <files>HabitsTracker/Features/Ideas/IdeaRow.swift</files>
  <read_first>
    - HabitsTracker/Features/Clips/ClipRow.swift (whole file — the exact reusable-row shape to mirror: DKCard surface, sole model prop, @EnvironmentObject/@Environment(\.colorScheme) boilerplate, title Text with .lineLimit(2)/.minimumScaleFactor(0.9), the `.accessibilityElement(children: .combine)` on the text VStack, and the separately-reachable trailing Button/Menu with `.frame(minWidth:44,minHeight:44)` + `.buttonStyle(.plain)`)
    - HabitsTracker/Features/Ideas/IdeaCaptureSheet.swift (the edit-mode init(idea:) presented on Row-1 tap — from 05-05)
    - HabitsTracker/Models/Idea.swift (idea.title, idea.note, idea.domain)
    - .planning/phases/05-ideas-promotion-e/05-UI-SPEC.md (S6 Row layout + the Color/Typography/Spacing tables + Copywriting Contract for the File pill; Accessibility section) and 05-PATTERNS.md (section "IdeaRow.swift")
  </read_first>
  <action>
    Create `HabitsTracker/Features/Ideas/IdeaRow.swift`: `struct IdeaRow: View` with `let idea: Idea`, `@EnvironmentObject themeManager`, `@Environment(\.colorScheme)`, `@Environment(\.modelContext)`, `@Query(sort: \Domain.sortIndex) private var domains: [Domain]` (auxiliary picker data for File — allowed per §9.2 since the row's subject is still the passed-in Idea), and `@State private var editingIdea = false`. Layout: `DKCard` → `VStack(alignment:.leading, spacing: theme.spacing.s)` with Row 1 (title `Text(idea.title)` `theme.typography.headline`/textPrimary, `.lineLimit(2)`, `.minimumScaleFactor(0.9)`; optional single-line `Text(note)` caption/textSecondary when `idea.note` non-empty — rendered defensively even though S2 never writes note) inside a `.accessibilityElement(children:.combine)` block labelled "\(idea.title), idea", wrapped so a tap sets `editingIdea = true`. Present `.sheet(isPresented: $editingIdea) { IdeaCaptureSheet(idea: idea) }`. Row 2: an `HStack(spacing: theme.spacing.s)` holding the File pill FIRST (this task): a `Menu` shown only `if idea.domain == nil`, label a pill (`Image(systemName:"tray.and.arrow.down") + Text("File")` via the DKBadge-style recipe: `theme.typography.caption.weight(.semibold)`, `theme.colors.accentPrimary` on `theme.colors.highlight`, `theme.radii.chip`, `.frame(minHeight:44)` + `.contentShape(Rectangle())`), whose content is `ForEach(domains) { d in Button(d.name) { idea.domain = d; try? modelContext.save() } }`. Menu `accessibilityLabel` "File idea, choose a domain". Leave a clearly-marked placeholder in Row 2 for the Promote pill (added in Task 2). Tokens-only; do NOT use accentColor(forToken:) (UI-SPEC Color rule — ideas stay domain-agnostic).
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `IdeaRow.swift` contains `struct IdeaRow`, `let idea: Idea`, a Row-1 tap presenting `IdeaCaptureSheet(idea: idea)`, and a File `Menu` gated on `idea.domain == nil` that sets `idea.domain` + saves.
    - `.accessibilityElement(children: .combine)` labels Row 1 "{title}, idea"; the File Menu has its own accessibilityLabel "File idea, choose a domain".
    - Pills use tokens (caption.weight(.semibold), accentPrimary/highlight, radii.chip) with `.frame(minHeight:44)`; NO `Color(`, NO `accentColor(forToken`, NO literal radii/spacing.
    - Build exits 0.
  </acceptance_criteria>
  <done>IdeaRow renders title+optional note in a DKCard, opens the edit sheet on tap, and offers a File domain-Menu when unfiled — tokens-only, accessible; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Promote pill (Menu → Rule / Habit / Collection routing)</name>
  <files>HabitsTracker/Features/Ideas/IdeaRow.swift</files>
  <read_first>
    - HabitsTracker/Features/Ideas/IdeaRow.swift (the Row-2 HStack + File pill from Task 1 — add the Promote pill beside it)
    - HabitsTracker/Features/Rules/RuleEditorView.swift (the `init(promotingIdea:)` from 05-06); HabitsTracker/Features/Habits/HabitCreateSheet.swift (the `.idea(Idea)` source + `onSaved` completion from 05-06); HabitsTracker/Features/Ideas/PromoteToCollectionPicker.swift (from 05-06)
    - HabitsTracker/Services/PromoteService.swift (archiveAndForwardLink — for the habit route's onSaved consume)
    - .planning/phases/05-ideas-promotion-e/05-UI-SPEC.md (S6 Promote pill + S7 routing + Copywriting Contract: pill "Promote", menu items "Rule"/"Habit"/"Collection item"; accessibility "Promote idea, choose a type")
  </read_first>
  <action>
    Add the Promote pill to Row 2's HStack (always shown, after the conditional File pill), same DKBadge-style recipe with `Image(systemName:"arrow.up.forward.circle") + Text("Promote")`, `accessibilityLabel` "Promote idea, choose a type". Make it a `Menu` with three `Button`s — "Rule", "Habit", "Collection item" — each setting an enum `@State private var promoteRoute: PromoteRoute?` (`.rule`/`.habit`/`.collection`). Drive presentation off that state (e.g. `.sheet(item:)` or three `.sheet(isPresented:)` bound to the route): `.rule` → `RuleEditorView(promotingIdea: idea)`; `.habit` → `HabitCreateSheet(source: .idea(idea), onSaved: { habit in PromoteService.archiveAndForwardLink(idea: idea, as: .habit, targetID: habit.id); try? modelContext.save() })` (Rule/Collection self-consume in their editors per 05-06; only Habit needs the row to invoke the consume); `.collection` → `PromoteToCollectionPicker(idea: idea)`. After any successful promote, the idea's `isArchived` flips true and it leaves the active-list queries automatically (no manual list mutation needed here). Keep the Promote Menu a separate VoiceOver element from Row 1 (never absorbed).
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `IdeaRow.swift` contains a Promote `Menu` with Buttons "Rule", "Habit", "Collection item" and presents `RuleEditorView(promotingIdea:`, `HabitCreateSheet(source: .idea(idea)`, and `PromoteToCollectionPicker(idea:` respectively.
    - The Habit route passes an `onSaved` closure calling `PromoteService.archiveAndForwardLink(idea: idea, as: .habit, targetID:`; Rule/Collection routes rely on their editors' own consume (no duplicate consume in the row).
    - The Promote Menu carries accessibilityLabel "Promote idea, choose a type" and stays separate from the combined Row-1 element.
    - Build exits 0.
  </acceptance_criteria>
  <done>The Promote Menu routes into the three prefilled editors; the habit route consumes the idea via onSaved+PromoteService; a promoted idea vanishes from active lists; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | IdeaRow presents existing editors over local models. No new external input, network, or persistence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering (data integrity) | habit-promote consume timing | mitigate | The idea is consumed only inside the Habit sheet's `onSaved` completion (fires post-save), so cancelling HabitCreateSheet leaves the idea un-consumed and still in the inbox. PromoteService's already-archived guard blocks a double-consume if a route is somehow triggered twice. |

Offline, single-user, local. No network, no new dependency. No high-severity threats.
</threat_model>

<verification>
- `xcodebuild ... build` exits 0.
- Grep: `struct IdeaRow`, `IdeaCaptureSheet(idea:`, File Menu gated on `idea.domain == nil`, Promote Menu with "Rule"/"Habit"/"Collection item", `RuleEditorView(promotingIdea:`, `HabitCreateSheet(source: .idea`, `PromoteToCollectionPicker(idea:`, `PromoteService.archiveAndForwardLink`.
- Grep: no `Color(`, no `accentColor(forToken`, no literal radii/spacing tokens.
</verification>

<success_criteria>
IdeaRow gives every idea one-tap File (unfiled only) and Promote graduations inline, opens the title-only edit sheet on tap, routes promote into the prefilled editors, consumes the idea on success, and is fully accessible with File/Promote reachable as separate VoiceOver elements. (Owner on-device confirmation of the File/Promote flows rides the phase-end owner check.)
</success_criteria>

<output>
Create `.planning/phases/05-ideas-promotion-e/05-07-SUMMARY.md` when done.
</output>
