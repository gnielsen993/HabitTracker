---
phase: 05-ideas-promotion-e
plan: 10
type: execute
wave: 6
depends_on: [05-04, 05-05, 05-07, 05-08, 05-09]
files_modified: []
autonomous: false
requirements: [IDEA-02, IDEA-03, IDEA-04, IDEA-05]
decisions: [D-01, D-06, D-07]
must_haves:
  truths:
    - "SC1: the global '+' on Today opens capture without leaving Today, and a captured idea adds NO row to Today's list but appears in the Hub inbox"
    - "SC2: File assigns a domain and the item stays an Idea (moves into that domain's Ideas section, leaves the inbox)"
    - "SC3: Promote → Rule/Habit/Collection item opens the prefilled editor; on Save the idea is archived (leaves the active list) and the result carries no backref; promote-to-habit reuses Phase 2's HabitCreateSheet"
    - "SC4: an unfiled idea's promote prompts for a domain (Rule route) and promote-to-collection prompts for the target list"
    - "Baseline DoD: schemaVersion-6 export/import round-trip is green; Today is visually unchanged aside from the '+'; 4 tabs hold; tokens-only, Dynamic Type + VoiceOver labels hold on the new surfaces (§9.15)"
  artifacts:
    - path: ".planning/phases/05-ideas-promotion-e/05-10-SUMMARY.md"
      provides: "owner full-flow verification record"
      contains: "SC1"
  key_links:
    - from: "Phase 5 UI surfaces"
      to: "the 4 ROADMAP success criteria"
      via: "owner device walkthrough"
      pattern: "approved"
---

<objective>
Owner device verification of the complete Phase 5 capture → inbox → File / Promote spine on real hardware — the standing §9.7 gate (the iOS 26 sim cannot launch the UI/@Model host, so the human-observable behavior of SC1–SC4 + baseline DoD is confirmed on device). Mirrors the owner full-flow checkpoints that closed Phases 1–4 (e.g. 04-05 Task 2, CLIP-03).

Purpose: every prior surface plan built + build-verified its slice; this plan proves the assembled flow behaves per the four ROADMAP success criteria and does not regress Today/the 4-tab IA. It is `autonomous: false` — a blocking owner checkpoint.
Output: `05-10-SUMMARY.md` recording the walkthrough verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ideas-promotion-e/05-CONTEXT.md
@.planning/phases/05-ideas-promotion-e/05-UI-SPEC.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gates</name>
  <files>.planning/phases/05-ideas-promotion-e/05-10-SUMMARY.md</files>
  <read_first>
    - CLAUDE.md §6 (RC smoke test steps) and §8 (build/test commands, bundle id)
    - .planning/phases/05-ideas-promotion-e/05-UI-SPEC.md (all surfaces + Copywriting Contract — the visual/copy contract the owner checks against)
  </read_first>
  <action>
    Before handing off to the owner, run the automatable gates and record results in the SUMMARY: (1) `xcodebuild ... build` exits 0; (2) the runnable PromoteServiceTests suite passes (`-only-testing:HabitsTrackerTests/PromoteServiceTests`); (3) grep sweeps confirm tokens-only on the new Ideas surfaces (no `Color(`, no literal radii/spacing outside the ≥44 tap-target frames) and zero `print(` in new files (§9.13). Summarize which of {build, engine tests, grep gates} passed so the owner walkthrough is the only remaining item.
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:HabitsTrackerTests/PromoteServiceTests -parallel-testing-enabled NO test</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records: build exit 0, PromoteServiceTests green, tokens/print grep sweeps clean on the new Ideas files.
    - Any failure is recorded and blocks the owner handoff until fixed.
  </acceptance_criteria>
  <done>Automated gates (build, engine tests, token/print greps) recorded green; only the owner walkthrough remains.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Phase 5 spine: Today "+" capture → Hub inbox card → InboxView → File (domain menu) and Promote (Rule/Habit/Collection item) graduations, plus the in-domain Ideas section. All slices build-verified; automated gates recorded in Task 1.</what-built>
  <how-to-verify>
    On your iPhone 17 (device — the sim can't launch this host, §9.7):
    1. SC1 — On Today, tap the top-trailing "+". Confirm capture opens WITHOUT leaving Today, type a title, Save. Confirm NO new row appeared on Today's list, and the Hub now shows an "Ideas to file" card with the right count.
    2. SC1 — Tap the inbox card → InboxView lists your captured idea(s), most-recent first.
    3. SC2 — On an inbox idea, tap File → pick a domain. Confirm it leaves the inbox and now appears under that domain's Ideas section (Hub → domain), still an Idea.
    4. SC3 (Rule) — On an idea, Promote → Rule. Confirm RuleEditorView opens prefilled (title from the idea). Save. Confirm the idea disappears from the active list and the new Rule shows NO "promoted from" backref.
    5. SC3 (Habit) — Promote → Habit. Confirm the SAME Phase-2 HabitCreateSheet opens prefilled (title + domain editable). Save. Confirm the new habit appears on Today and the idea is consumed (gone from the inbox), with no backref.
    6. SC3 (Collection) + SC4 — Promote → Collection item. Confirm "Choose a List" prompts for the target collection, then the item editor opens prefilled; Save consumes the idea.
    7. SC4 — Capture a fresh UNFILED idea, Promote → Rule. Confirm Save stays disabled until you pick a domain.
    8. Baseline DoD — Settings → export a backup, then import it back; confirm the round-trip succeeds and ideas survive. Confirm Today is visually unchanged aside from the "+", the tab bar is still 4 tabs, and VoiceOver reaches the File and Promote actions as separate elements.
    9. Escape hatch — tap an idea's title (edit sheet), delete it via "Delete Idea"; confirm the hard-delete confirmation and removal.
  </how-to-verify>
  <resume-signal>Type "approved" if SC1–SC4 + baseline DoD all hold, or describe what failed (which step, observed vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assembled flow on a real user's device | The integrated capture/promote flow acting on the user's real store — the point where any data-loss or consume-timing bug would first be observable end-to-end. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering (data integrity) | end-to-end promote/consume + export round-trip | mitigate | This blocking owner walkthrough is the final integrity check — it exercises promote-consume timing (idea vanishes only on Save), the no-backref rule, and the schemaVersion-6 export/import round-trip against real data, catching any cross-plan regression the per-slice build-verify could not. |

Offline, single-user, local. No network, no new dependency. No high-severity threats.
</threat_model>

<verification>
- Task-1 automated gates recorded green (build, PromoteServiceTests, token/print greps).
- Owner "approved" covering SC1–SC4 + baseline DoD (round-trip green, Today unchanged, 4 tabs, VoiceOver separateness), or a recorded defect list.
</verification>

<success_criteria>
The owner confirms on device that all four ROADMAP Phase-5 success criteria hold and the baseline DoD (schemaVersion-6 round-trip green, Today unchanged, 4 tabs, accessibility) is met — closing Phase 5.
</success_criteria>

<output>
Create `.planning/phases/05-ideas-promotion-e/05-10-SUMMARY.md` when done.
</output>
