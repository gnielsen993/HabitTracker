---
phase: 05-ideas-promotion-e
plan: 08
type: execute
wave: 5
depends_on: [05-07]
files_modified:
  - HabitsTracker/Features/Hub/HubView.swift
  - HabitsTracker/Features/Ideas/InboxView.swift
autonomous: true
requirements: [IDEA-03]
decisions: [D-03, D-04, D-05]
must_haves:
  truths:
    - "HubView pins an inbox card ABOVE the domain grid, inside the same ScrollView/VStack (not a separate region), shown ONLY when the unfiled (domain == nil, !isArchived) idea count > 0 (D-03/D-04); zero ideas → card entirely absent (no ghost/zero state — Phase 6 owns the designed empty state)"
    - "The card is a DKCard: leading tray.full icon (accentPrimary), 'Ideas to file' (headline/textPrimary), Spacer, DKBadge('{N} to file'), trailing chevron.right (textTertiary); the whole card is one ≥44pt NavigationLink → InboxView, .buttonStyle(.plain); accessibilityLabel '{N} ideas to file, opens inbox'"
    - "InboxView is data-driven (§9.2, D-05): owns @Query(filter: domain == nil && !isArchived, sort: createdAt desc); renders ForEach of IdeaRow in a VStack(spacing: .m); navigationTitle 'Inbox'; declares NO NavigationStack of its own (nests under HubView's stack, DomainDetailView precedent)"
    - "InboxView shows the minimal placeholder 'Nothing to file right now.' (body/textSecondary, spacing.xxl top) if the query goes to zero while open (D-04 minimal — NOT the Phase 6 designed empty state)"
    - "Filing or promoting an idea from an inbox IdeaRow removes it from the inbox live (both surfaces filter the same predicate) — the inbox exists to be emptied"
  artifacts:
    - path: "HabitsTracker/Features/Hub/HubView.swift"
      provides: "inbox card pinned above the domain grid"
      contains: "InboxView"
    - path: "HabitsTracker/Features/Ideas/InboxView.swift"
      provides: "data-driven list of unfiled ideas"
      contains: "struct InboxView"
  key_links:
    - from: "HabitsTracker/Features/Hub/HubView.swift"
      to: "InboxView"
      via: "inbox card NavigationLink"
      pattern: "InboxView()"
    - from: "HabitsTracker/Features/Ideas/InboxView.swift"
      to: "Idea"
      via: "unfiled-ideas @Query"
      pattern: "domain == nil"
    - from: "HabitsTracker/Features/Ideas/InboxView.swift"
      to: "IdeaRow"
      via: "ForEach row rendering"
      pattern: "IdeaRow(idea:"
---

<objective>
Surface the Hub inbox: a "N to file" card pinned above the domain grid in `HubView` (visible only when unfiled ideas exist) that opens `InboxView`, a data-driven list of unfiled ideas rendered as `IdeaRow`s (D-03/D-04/D-05, IDEA-03). This is where captured ideas land and get filed or promoted out.

Purpose: IDEA-03 (SC1) — the Hub inbox is the surface where unfiled ideas live until filed or promoted; it lives in the Hub, never on Today. The card's presence is itself the signal ("you have N to file"); it disappears when the inbox is empty (the whole point is an inbox that gets emptied).
Output: the HubView card + `InboxView.swift` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ideas-promotion-e/05-CONTEXT.md
@.planning/phases/05-ideas-promotion-e/05-UI-SPEC.md
@.planning/phases/05-ideas-promotion-e/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: InboxView (data-driven list of unfiled ideas)</name>
  <files>HabitsTracker/Features/Ideas/InboxView.swift</files>
  <read_first>
    - HabitsTracker/Features/Hub/DomainDetailView.swift (lines 22-65 the ScrollView+VStack content shape, nests under caller's stack, navigationTitle/.inline; lines 266-278 the emptyState treatment to SIMPLIFY from — S4 wants a single centered line, not the two-line heading+body)
    - HabitsTracker/Features/Ideas/IdeaRow.swift (the row this view renders — from 05-07)
    - HabitsTracker/Models/Idea.swift (the @Query subject)
    - .planning/phases/05-ideas-promotion-e/05-UI-SPEC.md (S4 + Copywriting Contract "Inbox", "Nothing to file right now.") and 05-PATTERNS.md (section "InboxView.swift")
  </read_first>
  <action>
    Create `HabitsTracker/Features/Ideas/InboxView.swift`: `struct InboxView: View` owning `@Query(filter: #Predicate<Idea> { $0.domain == nil && !$0.isArchived }, sort: \Idea.createdAt, order: .reverse) private var unfiledIdeas: [Idea]`, the DKThemeManager/colorScheme boilerplate, and a `ScrollView { VStack(alignment:.leading, spacing: theme.spacing.m) { ForEach(unfiledIdeas) { IdeaRow(idea: $0) } } }` padded `theme.spacing.l`, background `theme.colors.background.ignoresSafeArea()`, `navigationTitle("Inbox")`. Do NOT wrap in its own NavigationStack (it's pushed from HubView's stack). When `unfiledIdeas.isEmpty`, show a single centered `Text("Nothing to file right now.")` (`theme.typography.body`, `theme.colors.textSecondary`) with `theme.spacing.xxl` top padding — no heading, no illustration, no CTA (D-04 minimal; flag POL-02 replaces it in a code comment).
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `InboxView.swift` contains `struct InboxView`, the `@Query` filtering `domain == nil && !$0.isArchived` sorted by createdAt reverse, `ForEach(...) { IdeaRow(idea:`, `navigationTitle("Inbox")`, and NO nested `NavigationStack`.
    - Empty branch shows exactly "Nothing to file right now." (body/textSecondary, spacing.xxl top), no heading/CTA; a comment flags POL-02.
    - Tokens-only (no `Color(`/literals). Build exits 0.
  </acceptance_criteria>
  <done>InboxView is a data-driven, self-querying list of unfiled ideas rendering IdeaRow, nesting under HubView's stack, with the minimal D-04 placeholder; build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Hub inbox card (pinned above the grid, count-gated)</name>
  <files>HabitsTracker/Features/Hub/HubView.swift</files>
  <read_first>
    - HabitsTracker/Features/Hub/HubView.swift (lines 15-16 the existing focusedDomains @Query idiom to copy for an unfiledIdeas @Query; lines 40-61 grid(theme:); lines 46-57 the NavigationLink-wraps-DomainTile idiom; lines 75-85 the empty-state CTA card for a full-card component reference; the ScrollView/VStack container the grid renders into)
    - HabitsTracker/Features/Ideas/InboxView.swift (the navigation destination — Task 1)
    - HabitsTracker/Models/Idea.swift
    - .planning/phases/05-ideas-promotion-e/05-UI-SPEC.md (S3 layout: DKCard, tray.full icon, "Ideas to file", DKBadge "{N} to file", chevron; visibility D-04; accessibility "{N} ideas to file, opens inbox") and 05-PATTERNS.md (section "HubView.swift")
  </read_first>
  <action>
    In `HubView.swift`: add `@Query(filter: #Predicate<Idea> { $0.domain == nil && !$0.isArchived }) private var unfiledIdeas: [Idea]` alongside `focusedDomains`. Inside the same ScrollView/VStack that renders `grid(theme:)`, PREPEND (above the grid's LazyVGrid) an `if !unfiledIdeas.isEmpty { inboxCard(theme:) }` block. `inboxCard` is a `NavigationLink { InboxView() } label: { DKCard(theme:) { HStack(spacing: theme.spacing.s) { Image(systemName:"tray.full").foregroundStyle(theme.colors.accentPrimary); Text("Ideas to file").font(theme.typography.headline).foregroundStyle(theme.colors.textPrimary); Spacer(); DKBadge("\(unfiledIdeas.count) to file", theme: theme); Image(systemName:"chevron.right").foregroundStyle(theme.colors.textTertiary) } } }.buttonStyle(.plain)` with `.frame(minHeight:44)` and `accessibilityLabel("\(unfiledIdeas.count) ideas to file, opens inbox")`. Use "{N} to file" verbatim for all N (no singular/plural this phase). Do NOT alter the grid or the "Your Hub is empty" state — the card is purely additive above the grid.
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `HubView.swift` contains an `unfiledIdeas` `@Query`, an `if !unfiledIdeas.isEmpty` gate, a `DKCard` with `tray.full` + "Ideas to file" + `DKBadge("\(unfiledIdeas.count) to file"` + `chevron.right`, and a `NavigationLink { InboxView() }`.
    - accessibilityLabel is "{N} ideas to file, opens inbox"; card is ≥44pt, `.buttonStyle(.plain)`; tokens-only.
    - The card renders ABOVE the grid and is absent when count is 0 (grep the gate); the grid/empty-state code is unchanged.
    - Build exits 0.
  </acceptance_criteria>
  <done>The Hub shows a count-gated "N to file" inbox card above the domain grid that opens InboxView; absent when empty; grid untouched; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Read-only @Query surfaces over the local store; navigation only. No new external input, network, or persistence boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information disclosure (none realistic) | inbox surfaces | accept | The inbox displays only the single local user's own captured ideas on their own device; there is no multi-user or network exposure. No mitigation required beyond the offline-only constraint already in force. |

Offline, single-user, local, read-only surfaces. No network, no new dependency. No high-severity threats.
</threat_model>

<verification>
- `xcodebuild ... build` exits 0.
- Grep: `struct InboxView`, InboxView `@Query` `domain == nil`, `IdeaRow(idea:`, `navigationTitle("Inbox")`; HubView `unfiledIdeas`, `if !unfiledIdeas.isEmpty`, `InboxView()`, `DKBadge("\(unfiledIdeas.count) to file"`.
- Grep: no `Color(`/literals in the new/edited code.
</verification>

<success_criteria>
When unfiled ideas exist the Hub shows a "N to file" card above the domain grid that opens a data-driven InboxView list of IdeaRows; filing/promoting empties the inbox live; the card and list follow tokens + the D-04 minimal-placeholder posture. (Owner on-device visual confirmation rides the phase-end owner check.)
</success_criteria>

<output>
Create `.planning/phases/05-ideas-promotion-e/05-08-SUMMARY.md` when done.
</output>
