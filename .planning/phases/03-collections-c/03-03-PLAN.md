---
phase: 03-collections-c
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - HabitsTracker/Features/Collections/CollectionRow.swift
  - HabitsTracker/Features/Collections/CollectionPresetPickerSheet.swift
  - HabitsTracker/Features/Collections/CollectionDetailView.swift
  - HabitsTracker/Features/Hub/DomainDetailView.swift
autonomous: true
requirements: [COLL-02, COLL-06, COLL-07]
decisions: [D-13, D-15, D-17, D-18]
must_haves:
  truths:
    - "A domain's collections appear as a Collections section inside DomainDetailView (rendered only when domain.collections is non-empty, D-15), each row a CollectionRow inside a NavigationLink to CollectionDetailView, sorted by sortIndex ascending"
    - "The Collections section header has a '+' that presents CollectionPresetPickerSheet pre-scoped to the domain (D-13)"
    - "CollectionPresetPickerSheet lists all 9 CollectionPresetCatalog presets (generic first); tapping one creates a Collection from that preset filed under the domain, dismisses, and the new collection appears in the section"
    - "CollectionRow shows the collection title, a StatusSet sub-label, and — when showsAggregate is true — a trailing rollup: \"X/Y\" (caption) for completionist or \"$NNN\" (monoNumber) for cost-flavored, computed via CollectionRollupEngine; no trailing label when showsAggregate is false"
    - "CollectionDetailView shows a header (domain glyph accent-tinted + name + rollup + StatusSet sub-label), an items list (empty for now — CollectionItemRow arrives in 03-04), and the 'Nothing in this list yet' empty state; declares no NavigationStack"
    - "All new views are data-driven (§9.2), token-only (§9.4), carry empty/VoiceOver states (§9.3/§9.15), and stay under ~400 lines (§9.1)"
    - "Traceability: implements CONTEXT decisions D-13, D-15, D-17, D-18 (see 03-CONTEXT.md)"
  artifacts:
    - path: "HabitsTracker/Features/Collections/CollectionRow.swift"
      provides: "data-driven collection row with StatusSet sub-label + trailing rollup"
      contains: "struct CollectionRow"
    - path: "HabitsTracker/Features/Collections/CollectionPresetPickerSheet.swift"
      provides: "preset picker that creates a Collection from a CollectionPreset filed under the domain"
      contains: "struct CollectionPresetPickerSheet"
    - path: "HabitsTracker/Features/Collections/CollectionDetailView.swift"
      provides: "collection read surface: header + rollup + items list + empty state + Add-item toolbar"
      contains: "struct CollectionDetailView"
    - path: "HabitsTracker/Features/Hub/DomainDetailView.swift"
      provides: "Collections section appended to nonEmptySections"
      contains: "buildCollectionsSection"
  key_links:
    - from: "HabitsTracker/Features/Hub/DomainDetailView.swift"
      to: "CollectionDetailView"
      via: "NavigationLink from a CollectionRow"
      pattern: "CollectionDetailView"
    - from: "HabitsTracker/Features/Collections/CollectionPresetPickerSheet.swift"
      to: "Collection"
      via: "modelContext.insert on preset tap, filed under the domain"
      pattern: "modelContext.insert"
    - from: "HabitsTracker/Features/Collections/CollectionRow.swift"
      to: "CollectionRollupEngine"
      via: "rollup computed for the trailing label"
      pattern: "CollectionRollupEngine"
---

<objective>
Build the collection-level surfaces: the Collections section in `DomainDetailView` (mirrors the Rules section, D-15), `CollectionPresetPickerSheet` (the "+" → pick a preset → create a filed collection, D-13), `CollectionRow` (title + StatusSet sub-label + trailing rollup, D-17), and `CollectionDetailView` (header + rollup + items list + empty state). This delivers success criterion 1's "create a collection from a preset" and criterion 3's row/header rollup rendering.

Purpose: COLL-07 ships the presets as pickable content; COLL-02 makes the generic preset creatable and default; COLL-06 renders X/Y (ring-optional) and cost-sum (never a ring) on the row and header.
Output: 3 new view files + the DomainDetailView section edit. Item rows/detail/editor land in 03-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-collections-c/03-CONTEXT.md
@.planning/phases/03-collections-c/03-UI-SPEC.md
@.planning/phases/03-collections-c/03-PATTERNS.md
@.planning/phases/03-collections-c/03-01-SUMMARY.md
@.planning/phases/03-collections-c/03-02-SUMMARY.md

<interfaces>
<!-- From the codebase + 03-01/03-02 outputs — use directly. -->

CollectionRollupEngine (from 03-02):
```swift
enum CollectionRollupEngine {
    enum Result: Equatable { case count(x: Int, y: Int); case costSum(total: Double); case none }
    nonisolated static func rollup(collection: Collection, items: [CollectionItem]) -> Result
}
```
StatusSetCatalog (from 03-02): `StatusSetCatalog.set(for: String) -> StatusSet?` where `StatusSet { id; states: [String]; terminalIndex: Int }`.
CollectionPresetCatalog (from 03-02): `CollectionPresetCatalog.all: [CollectionPreset]` where `CollectionPreset { id; name; statusSetID; progressTemplate; showsAggregate }`.
Collection (from 03-01): `title, statusSetID, progressTemplate, showsAggregate, sortIndex, note?, items: [CollectionItem], domain: Domain?`.

RuleRow full shape to mirror (HabitsTracker/Features/Rules/RuleRow.swift): DKCard { VStack(spacing: theme.spacing.xs) { title headline/textPrimary; secondary caption/textSecondary } }.frame(minHeight:44).accessibilityElement(children:.ignore).accessibilityLabel(...).

RuleDetailView header + toolbar shape (HabitsTracker/Features/Rules/RuleDetailView.swift): ScrollView { VStack(spacing: theme.spacing.xl) { header (domain glyph accentColor(forToken:) + title) ... } }.background(theme.colors.background.ignoresSafeArea()).toolbar { trailing Button }; no NavigationStack.

RuleEditorView sheet scaffold (HabitsTracker/Features/Rules/RuleEditorView.swift): NavigationStack { Form/List }.navigationTitle(...).toolbar { cancellationAction Button("Cancel"){dismiss()} }; uses @Environment(\.dismiss) + @Environment(\.modelContext).

DomainDetailView hook (HabitsTracker/Features/Hub/DomainDetailView.swift): nonEmptySections(theme:) appends buildRulesSection; @State creatingRule; .sheet(isPresented:$creatingRule){ RuleEditorView(domain:) }.

Domain accent glyph idiom: HabitsTracker.accentColor(forToken: domain.colorToken, scheme: colorScheme).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CollectionRow + CollectionPresetPickerSheet</name>
  <files>HabitsTracker/Features/Collections/CollectionRow.swift, HabitsTracker/Features/Collections/CollectionPresetPickerSheet.swift</files>
  <read_first>
    - HabitsTracker/Features/Rules/RuleRow.swift (the full DKCard row shape + accessibilityElement/label pattern to mirror)
    - HabitsTracker/Features/Rules/RuleEditorView.swift (the NavigationStack sheet scaffold + @Environment(\.modelContext)/(\.dismiss) + cancel toolbar to mirror for the picker)
    - .planning/phases/03-collections-c/03-UI-SPEC.md sections "S1 — Collections section" (row layout + trailing rollup rules) and "S2 — CollectionPresetPickerSheet" (title "Choose a type", preset row copy, cancel, accessibility) and "Copywriting Contract" (exact strings)
    - .planning/phases/03-collections-c/03-PATTERNS.md sections "CollectionRow.swift" and "CollectionPresetPickerSheet.swift"
    - .planning/phases/03-collections-c/03-02-SUMMARY.md (final catalog/engine signatures)
  </read_first>
  <action>
    Create `Features/Collections/CollectionRow.swift`. `struct CollectionRow: View` taking `let collection: Collection`; `@EnvironmentObject themeManager`, `@Environment(\.colorScheme)`. Body: `DKCard` wrapping an `HStack` — leading `VStack(alignment:.leading, spacing: theme.spacing.xs)` with title (`theme.typography.headline`/textPrimary, 1 line) + a StatusSet sub-label (`theme.typography.caption`/textSecondary) built from `StatusSetCatalog.set(for: collection.statusSetID)` rendering e.g. "Shows — watched" (title-cased preset name derived from the set's terminal label, or the collection name — keep it a caption sub-label). `Spacer()`. Trailing: when `collection.showsAggregate`, compute `CollectionRollupEngine.rollup(collection: collection, items: collection.items)` and render per the Rollup Format Spec — `.count(x,y)` where y>0 → `Text("\(x)/\(y)")` caption/textSecondary; `.costSum(total)` → `Text("$\(formatted)")` monoNumber/textSecondary; `.none` or `.count` with y==0 → no trailing label. Whole card `.frame(minHeight: 44)`. `.accessibilityElement(children:.ignore)` + composed label per UI-SPEC S1 ("\(collection.name), \(itemCount) items, \(rollupIfPresent)"). Data-driven — no @Query (§9.2).

    Create `Features/Collections/CollectionPresetPickerSheet.swift`. `struct CollectionPresetPickerSheet: View` taking `let domain: Domain`; `@Environment(\.dismiss)`, `@Environment(\.modelContext)`, theme env. Body: `NavigationStack { List/VStack }` titled "Choose a type" (D-13). One row per `CollectionPresetCatalog.all` (generic first): preset name (`headline`/textPrimary) + a description sub-label (`caption`/textSecondary) showing the state flow from `StatusSetCatalog.set(for: preset.statusSetID)` joined with " → " (e.g. "to-watch → watching → watched"). Row tap: create `Collection(title: preset.name, statusSetID: preset.statusSetID, progressTemplate: preset.progressTemplate, showsAggregate: preset.showsAggregate)`; set `collection.domain = domain`; set `collection.sortIndex = (domain.collections.map(\.sortIndex).max() ?? -1) + 1`; `modelContext.insert(collection)`; `try? modelContext.save()`; `dismiss()`. Toolbar: only a `cancellationAction` "Cancel" (no confirm — tapping a row creates + dismisses). Row `.accessibilityLabel("\(preset.name), \(description)")`; Cancel label "Cancel, no collection created". Sheet radius via `theme.radii.sheet` where applicable. No empty state (catalog always populated).
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `CollectionRow.swift` contains `struct CollectionRow`, `DKCard`, `CollectionRollupEngine`, `.accessibilityElement(children: .ignore)`; no `@Query`.
    - `CollectionPresetPickerSheet.swift` contains `struct CollectionPresetPickerSheet`, `CollectionPresetCatalog.all`, `modelContext.insert`, the literal `"Choose a type"`, and a `sortIndex` max+1 assignment.
    - `grep -c "Color(hex\|Color(red\|\.font(\.system(size" HabitsTracker/Features/Collections/CollectionRow.swift HabitsTracker/Features/Collections/CollectionPresetPickerSheet.swift` returns 0 (tokens only; the ≥44pt `.frame(minWidth:44,minHeight:44)` is the only allowed non-token dimension).
    - Both files stay under ~400 lines (§9.1).
    - Build exits 0.
  </acceptance_criteria>
  <done>CollectionRow renders title + StatusSet sub-label + the correct trailing rollup, and the preset picker creates a domain-filed collection from any of the 9 presets and dismisses.</done>
</task>

<task type="auto">
  <name>Task 2: CollectionDetailView + wire the Collections section into DomainDetailView</name>
  <files>HabitsTracker/Features/Collections/CollectionDetailView.swift, HabitsTracker/Features/Hub/DomainDetailView.swift</files>
  <read_first>
    - HabitsTracker/Features/Rules/RuleDetailView.swift (ScrollView + VStack(spacing:.xl) block structure, header block with accent-tinted domain glyph, toolbar trailing button, no NavigationStack)
    - HabitsTracker/Features/Hub/DomainDetailView.swift (self — nonEmptySections + buildRulesSection + rulesSectionContent + rulesSectionHeader as the exact templates; the @State creatingRule + .sheet pattern)
    - .planning/phases/03-collections-c/03-UI-SPEC.md sections "S3 — CollectionDetailView" (header block, rollup display, items list, empty state, toolbar) and "Rollup Format Specification" and "Copywriting Contract" (empty-state + Add-item strings)
    - .planning/phases/03-collections-c/03-PATTERNS.md sections "CollectionDetailView.swift" and "DomainDetailView.swift (edit)"
  </read_first>
  <action>
    Create `Features/Collections/CollectionDetailView.swift`. `struct CollectionDetailView: View` taking `let collection: Collection`; theme + colorScheme env; `@State private var addingItem = false`. Body: `ScrollView { VStack(alignment:.leading, spacing: theme.spacing.xl) { headerBlock; itemsOrEmpty } }.background(theme.colors.background.ignoresSafeArea())`; declares NO NavigationStack (nests under Hub). Header block: `HStack` with the owning-domain glyph (`Image(systemName: collection.domain?.iconName ...)`, accent-tinted via `HabitsTracker.accentColor(forToken: collection.domain?.colorToken, scheme:)`, size 28) + collection name (`theme.typography.title`/textPrimary); below it a rollup row when `collection.showsAggregate` per the Rollup Format Spec: completionist → `DKProgressRing(progress: Double(x)/Double(y), ...)` at ≤56pt with a `"\(x) of \(y) \(terminalStateLabel)"` body/textSecondary fallback beneath (when y==0 render "0 items" body/textSecondary and NO ring — avoid divide-by-zero); cost → `"$\(formatted)"` monoNumber/textPrimary (never a ring, D-18); tracker (showsAggregate false) → no rollup block. Optional Row 3 StatusSet sub-label (caption/textSecondary). Items list: `ForEach(collection.items.sorted { $0.sortIndex < $1.sortIndex })` — for THIS plan render a lightweight placeholder row (title + status label text) OR leave the ForEach emitting a minimal read-only row; the interactive `CollectionItemRow` + `NavigationLink` to `CollectionItemDetailView` are wired in 03-04, so keep the list body a small private `@ViewBuilder` that 03-04 will replace. Empty state (`collection.items.isEmpty`): heading "Nothing in this list yet" (title/textPrimary) + body "Tap + to add your first item." (body/textSecondary). Toolbar: trailing "+" (`Image(systemName:"plus")`, accentPrimary, ≥44pt, `accessibilityLabel("Add item to \(collection.name)")`) toggling `addingItem`; `.sheet(isPresented:$addingItem)` presenting a placeholder for now (03-04 swaps in `CollectionItemEditorSheet(collection:)` — leave a clearly-marked TODO stub that compiles, e.g. an EmptyView-backed sheet, so 03-04 only edits the sheet body).

    Edit `DomainDetailView.swift`: add `@State private var creatingCollection = false` next to `creatingRule`. Add a `buildCollectionsSection(theme:)` mirroring `buildRulesSection` — guard `!domain.collections.isEmpty` (D-15), sort by `sortIndex` ascending, build `DomainSection(id:"collections", title:"Collections", content:)`. Add a `collectionsSectionContent(collections:theme:)` + `collectionsSectionHeader(theme:)` mirroring the rules equivalents — header "Collections" (title/textPrimary, `.accessibilityAddTraits(.isHeader)`) + "+" button (accentPrimary, ≥44pt, `accessibilityLabel("Add collection to \(domain.name)")`) setting `creatingCollection = true`; each collection row = `NavigationLink { CollectionDetailView(collection: collection) } label: { CollectionRow(collection: collection) }.buttonStyle(.plain)`. Append to `nonEmptySections` after the rules block: `if let s = buildCollectionsSection(theme: theme) { sections.append(s) }`. Add `.sheet(isPresented: $creatingCollection) { CollectionPresetPickerSheet(domain: domain) }` alongside the existing rules sheet. Keep DomainDetailView under ~400 lines — if it approaches the cap, extract the Collections section builders into a private extension in the same file (§9.1).
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `CollectionDetailView.swift` contains `struct CollectionDetailView`, `DKProgressRing`, the literals `"Nothing in this list yet"` and `"Tap + to add your first item."`, and does NOT contain `NavigationStack`.
    - `DomainDetailView.swift` contains `buildCollectionsSection`, `CollectionRow(collection:`, `CollectionDetailView(collection:`, `CollectionPresetPickerSheet(domain:`, and `"Add collection to \(domain.name)"`.
    - `grep -c "NavigationStack" HabitsTracker/Features/Collections/CollectionDetailView.swift` returns 0 (nests under the Hub stack — no doubled nav bar).
    - The Collections section append sits after the Rules append inside `nonEmptySections`; the section renders only when `domain.collections` is non-empty (D-15 — grep for the `!domain.collections.isEmpty` guard).
    - Build exits 0.
  </acceptance_criteria>
  <done>Tapping "+" in a domain's Collections section picks a preset, creates a filed collection that appears as a CollectionRow, and tapping the row opens CollectionDetailView with its header/rollup/empty-state under a single nav bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user "+" create action → SwiftData insert | User-initiated collection creation writes to the local store; the only untrusted value is the (catalog-sourced) preset, which is code-defined |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service (crash) | CollectionDetailView rollup ring when y == 0 | mitigate | Guard `y == 0` → render "0 items" text and NO DKProgressRing (no `Double(x)/Double(0)` divide) per the Rollup Format Spec |
| T-03-06 | Information Disclosure | domain-filed create path | accept | Local-only app, no network/auth; preset values are code-defined, not user-authored free text at the schema level |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs — DesignKit local path + SF Symbols only; no new third-party dependency (UI-SPEC Registry Safety) |
</threat_model>

<verification>
- App builds clean on iPhone 17.
- Collections section renders only when domain.collections is non-empty (D-15); "+" presents the preset picker; picking creates a filed collection.
- CollectionRow shows the correct trailing rollup (X/Y vs $NNN vs none); CollectionDetailView shows header + rollup + empty state with no NavigationStack.
- All new views token-only (grep), data-driven, under ~400 lines.
</verification>

<success_criteria>
- Create-from-preset works and the new collection appears in its domain (COLL-02, COLL-07).
- Row + header rollups render X/Y (ring-optional) and cost-sum (never a ring) (COLL-06).
</success_criteria>

<output>
Create `.planning/phases/03-collections-c/03-03-SUMMARY.md` when done.
</output>
