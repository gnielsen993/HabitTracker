---
phase: 03-collections-c
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - HabitsTracker/Services/StatusSetCatalog.swift
  - HabitsTracker/Services/CollectionPresetCatalog.swift
  - HabitsTracker/Services/CollectionRollupEngine.swift
  - HabitsTrackerTests/CollectionRollupEngineTests.swift
autonomous: true
requirements: [COLL-01, COLL-02, COLL-06]
decisions: [D-01, D-04, D-12, D-16, D-18, D-19, D-20]
must_haves:
  truths:
    - "StatusSetCatalog is a code-only enum exposing a StatusSet value type (id, states, terminalIndex) with the generic set (to-collect -> collected) plus the 8 curated sets whose labels match the UI-SPEC StatusSet Catalog table exactly; no @Model, no SwiftData import"
    - "The generic StatusSet (id \"generic\", states [\"to-collect\", \"collected\"], terminalIndex 1) exists and is the default statusSetID for user-created collections (COLL-02 prerequisite)"
    - "CollectionPresetCatalog is a code-only enum exposing the 9 presets (generic first, then Shows, Movies, Albums, Concerts, Books, Clothes, Want to spend on, Planes/places) each carrying name, statusSetID, progressTemplate, and a showsAggregate default"
    - "CollectionRollupEngine is a pure nonisolated enum returning .count(x,y) / .costSum(total) / .none — X counts only items where statusIndex == terminalIndex (mid-step items excluded), cost sums only non-nil costs, and showsAggregate == false returns .none; imports Foundation only"
    - "CollectionRollupEngineTests ships in the same plan/commit covering completionist happy path, empty list, mid-step-excluded, mixed nil/non-nil cost, and tracker-off -> .none"
    - "Traceability: implements CONTEXT decisions D-01, D-04, D-12, D-16, D-18, D-19, D-20 (see 03-CONTEXT.md)"
  artifacts:
    - path: "HabitsTracker/Services/StatusSetCatalog.swift"
      provides: "code-only ordered StatusSet catalog keyed by String id"
      contains: "enum StatusSetCatalog"
    - path: "HabitsTracker/Services/CollectionPresetCatalog.swift"
      provides: "code-only 9-preset catalog (the SPEC §5 curated set + generic)"
      contains: "enum CollectionPresetCatalog"
    - path: "HabitsTracker/Services/CollectionRollupEngine.swift"
      provides: "pure rollup engine returning .count / .costSum / .none"
      contains: "enum CollectionRollupEngine"
    - path: "HabitsTrackerTests/CollectionRollupEngineTests.swift"
      provides: "5 unit tests shipped with the engine (§9.5)"
      contains: "final class CollectionRollupEngineTests"
  key_links:
    - from: "HabitsTracker/Services/CollectionRollupEngine.swift"
      to: "StatusSetCatalog"
      via: "terminalIndex lookup to decide X in .count"
      pattern: "StatusSetCatalog"
    - from: "HabitsTracker/Services/CollectionPresetCatalog.swift"
      to: "StatusSetCatalog"
      via: "each preset's statusSetID references a catalog set id"
      pattern: "statusSetID"
---

<objective>
Build the three code-only sources of truth that the chip, preset picker, and rollups consume: `StatusSetCatalog` (ordered states + terminal, D-01), `CollectionPresetCatalog` (the 9 curated presets, D-12), and the pure `CollectionRollupEngine` (X/Y vs cost-sum vs none, D-16–D-20) — the engine paired with its unit tests in the same commit (§9.5).

Purpose: COLL-01 reads "a StatusSet model exists" as a typed value model (D-01 — non-editability is structural because labels live in code). COLL-02's generic preset is the default. COLL-06's rollup semantics are deterministic and testable, so they live in a pure engine, not a view.
Output: Three service files (no persistence, no @Model) + one test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-collections-c/03-CONTEXT.md
@.planning/phases/03-collections-c/03-UI-SPEC.md
@.planning/phases/03-collections-c/03-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase — use directly, no exploration needed. -->

Pure engine idiom (HabitsTracker/Services/WeeklyGoalEngine.swift):
```swift
import Foundation

enum WeeklyGoalEngine {
    nonisolated static func remainingSessions(target: Int, completed: Int) -> Int {
        max(target - completed, 0)
    }
    nonisolated static func isTargetMet(target: Int, completed: Int) -> Bool {
        completed >= target
    }
}
```

Test file idiom (HabitsTrackerTests/EngineTests.swift):
```swift
import XCTest
@testable import HabitsTracker

final class EngineTests: XCTestCase {
    func testWeeklyGoalRemainingNeverNegative() { ... }
}
```

StatusSet catalog table (from 03-UI-SPEC.md + 03-PATTERNS.md — labels are the source of truth):
| id | states | terminalIndex |
| "generic" | ["to-collect", "collected"] | 1 |
| "shows" | ["to-watch", "watching", "watched"] | 2 |
| "movies" | ["to-watch", "watching", "watched"] | 2 |
| "albums" | ["to-listen", "listening", "listened"] | 2 |
| "concerts" | ["to-attend", "attended"] | 1 |
| "books" | ["to-read", "reading", "read"] | 2 |
| "clothes" | ["want", "bought"] | 1 |
| "spending" | ["considering", "purchased"] | 1 |
| "places" | ["to-visit", "visited"] | 1 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatusSetCatalog + CollectionPresetCatalog (code-only)</name>
  <files>HabitsTracker/Services/StatusSetCatalog.swift, HabitsTracker/Services/CollectionPresetCatalog.swift</files>
  <read_first>
    - HabitsTracker/Services/WeeklyGoalEngine.swift (the pure enum + nonisolated pattern to mirror — Foundation-only, no instantiation)
    - .planning/phases/03-collections-c/03-UI-SPEC.md section "StatusSet Catalog — Visual State Labels" (the exact state strings + terminal indices) and "S2 — CollectionPresetPickerSheet" (preset order + description sub-label format)
    - .planning/phases/03-collections-c/03-PATTERNS.md sections "Services/StatusSetCatalog.swift" and "Services/CollectionPresetCatalog.swift" (the id/states/terminal table + preset value-type fields)
  </read_first>
  <action>
    Create `StatusSetCatalog.swift` (`import Foundation` only). Define a `struct StatusSet { let id: String; let states: [String]; let terminalIndex: Int }`. Define `enum StatusSetCatalog` exposing `static let all: [StatusSet]` populated with the 9 entries from the interfaces table EXACTLY (labels are the chip's rendered source of truth — no paraphrasing), and `static func set(for id: String) -> StatusSet?` doing a `first(where:)` lookup, plus a `static var generic: StatusSet` convenience returning the "generic" set. No SwiftData import, no @Model, no `print()` (§9.13).

    Create `CollectionPresetCatalog.swift` (`import Foundation` only). Define a `struct CollectionPreset { let id: String; let name: String; let statusSetID: String; let progressTemplate: String; let showsAggregate: Bool }`. Define `enum CollectionPresetCatalog` exposing `static let all: [CollectionPreset]` in this order (D-12 / UI-SPEC S2): generic, shows, movies, albums, concerts, books, clothes, spending, places. Set each preset's fields per the SPEC-derived architectural defaults:
    - generic: name "My List", statusSetID "generic", progressTemplate "none", showsAggregate true
    - shows: name "Shows", statusSetID "shows", progressTemplate "seasonEpisode", showsAggregate true
    - movies: name "Movies", statusSetID "movies", progressTemplate "none", showsAggregate true
    - albums: name "Albums", statusSetID "albums", progressTemplate "none", showsAggregate true
    - concerts: name "Concerts", statusSetID "concerts", progressTemplate "none", showsAggregate true
    - books: name "Books", statusSetID "books", progressTemplate "counter", showsAggregate true
    - clothes: name "Clothes to buy", statusSetID "clothes", progressTemplate "none", showsAggregate true
    - spending: name "Want to spend on", statusSetID "spending", progressTemplate "none", showsAggregate true (cost-flavored — rollup derives .costSum from item costs per D-20, no stored money flag)
    - places: name "Planes / places", statusSetID "places", progressTemplate "none", showsAggregate true
    Do NOT introduce a preset→domain mapping (presets are not domain-locked, D-13). Do NOT make progressTemplate anything outside none/counter/seasonEpisode (scope guard).
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `StatusSetCatalog.swift` contains `enum StatusSetCatalog`, `struct StatusSet`, `terminalIndex`, and the literal strings `"to-collect"`, `"collected"`, `"to-watch"`, `"watching"`, `"watched"`, `"to-listen"`, `"to-attend"`, `"to-read"`, `"want"`, `"bought"`, `"considering"`, `"purchased"`, `"to-visit"`, `"visited"`.
    - `StatusSetCatalog.all` has exactly 9 entries; `StatusSetCatalog.set(for: "generic")?.terminalIndex == 1` and `.set(for: "shows")?.terminalIndex == 2` (verifiable via a scratch assertion or by inspection).
    - `CollectionPresetCatalog.swift` contains `enum CollectionPresetCatalog`, `struct CollectionPreset`, `static let all`; `all` has exactly 9 entries with `all.first?.id == "generic"`.
    - `grep -c "import SwiftData\|@Model\|import DesignKit" HabitsTracker/Services/StatusSetCatalog.swift HabitsTracker/Services/CollectionPresetCatalog.swift` returns 0 (pure code, no persistence/UI).
    - Build exits 0.
  </acceptance_criteria>
  <done>Both catalogs are pure Foundation code, the generic set is present and default-eligible, and the 9 presets match the SPEC order and StatusSet labels exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CollectionRollupEngine + its unit tests (same commit, §9.5)</name>
  <files>HabitsTracker/Services/CollectionRollupEngine.swift, HabitsTrackerTests/CollectionRollupEngineTests.swift</files>
  <read_first>
    - HabitsTracker/Services/WeeklyGoalEngine.swift (nonisolated static func + Foundation-only idiom)
    - HabitsTrackerTests/EngineTests.swift (XCTestCase structure + in-line object construction style — no makeInMemoryContext needed for a pure engine)
    - .planning/phases/03-collections-c/03-CONTEXT.md decisions D-16, D-18, D-19, D-20 (X = terminal-only; cost never a ring; the 5 required test cases; the .costSum-vs-.count derivation)
    - .planning/phases/03-collections-c/03-PATTERNS.md section "Services/CollectionRollupEngine.swift" (the Result enum shape) and "HabitsTrackerTests/CollectionRollupEngineTests.swift" (the 5 named test cases)
    - .planning/phases/03-collections-c/03-UI-SPEC.md section "Rollup Format Specification" (how each Result maps to UI — informs the enum contract)
  </read_first>
  <behavior>
    - completionist: 3-state set, 5 items where 2 are at statusIndex == terminalIndex -> .count(x: 2, y: 5).
    - mid-step excluded: a 3-state set with an item at statusIndex 1 (not terminal) -> that item is NOT counted in x.
    - empty list: showsAggregate true, zero items -> .count(x: 0, y: 0) (y == 0 handled, no divide).
    - cost-flavored: showsAggregate true, some items have non-nil cost and the list has no meaningful completion signal per D-20 -> .costSum summing ONLY non-nil costs (nil costs ignored, not treated as 0 in the count path).
    - tracker: showsAggregate == false -> .none regardless of items.
  </behavior>
  <action>
    Create `CollectionRollupEngine.swift` (`import Foundation` only — never SwiftData/DesignKit). Define `enum CollectionRollupEngine` with a nested `enum Result: Equatable { case count(x: Int, y: Int); case costSum(total: Double); case none }` and a single `nonisolated static func rollup(collection: Collection, items: [CollectionItem]) -> Result`. Logic (D-20 derivation, stated crisply):
    1. `guard collection.showsAggregate else { return .none }`.
    2. Cost-flavored branch: if any item has a non-nil `cost`, return `.costSum(total:)` where total = sum of `items.compactMap(\.cost)` (nil costs excluded, D-19 mixed-nil case). Cost is ALWAYS plain-text downstream, never a ring (D-18) — that is a UI concern; the engine just returns the sum.
    3. Otherwise completionist branch: look up `terminalIndex` via `StatusSetCatalog.set(for: collection.statusSetID)?.terminalIndex`; `x = items.filter { $0.statusIndex == terminalIndex }.count` (strictly terminal, D-16 — a mid-step item is NOT counted); `y = items.count`; return `.count(x: x, y: y)`. If the statusSetID is unknown, treat terminalIndex as the last state index defensively (`states.count - 1`) or return `.count(0, items.count)` — pick the defensive path and document it inline (do not crash).

    Create `CollectionRollupEngineTests.swift` (`import XCTest; @testable import HabitsTracker`) with the 5 required cases from §9.5, constructing `Collection`/`CollectionItem` in-memory and passing directly (no ModelContext): `testCompletionistHappyPath`, `testEmptyList`, `testMidStepItemNotCounted`, `testCostSumWithMixedNilCosts`, `testTrackerShowsAggregateOff`. Assert against the `Result` enum via `XCTAssertEqual` (Result is Equatable).
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet test -only-testing:HabitsTrackerTests/CollectionRollupEngineTests</automated>
  </verify>
  <acceptance_criteria>
    - `CollectionRollupEngine.swift` contains `enum CollectionRollupEngine`, `case count(x: Int, y: Int)`, `case costSum(total: Double)`, `case none`, and `nonisolated static func rollup`.
    - `grep -c "import SwiftData\|import DesignKit\|print(" HabitsTracker/Services/CollectionRollupEngine.swift` returns 0.
    - `CollectionRollupEngineTests.swift` contains all 5 method names: `testCompletionistHappyPath`, `testEmptyList`, `testMidStepItemNotCounted`, `testCostSumWithMixedNilCosts`, `testTrackerShowsAggregateOff`.
    - The `-only-testing:HabitsTrackerTests/CollectionRollupEngineTests` run exits 0. (If the host aborts in NSStagedMigrationManager, `xcrun simctl uninstall booted gn.HabitsTracker` then retry — §9.7.)
  </acceptance_criteria>
  <done>The rollup engine is pure Foundation, terminal-only X, non-nil-only cost sum, .none on tracker; all 5 unit tests ship in the same commit and pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted statusSetID / statusIndex → catalog lookup | A stored statusSetID may not resolve (e.g. imported/older data); a statusIndex may be out of range for its set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service (crash) | CollectionRollupEngine terminalIndex lookup on an unknown/stale statusSetID | mitigate | Engine treats an unresolved statusSetID defensively (last-state fallback or .count(0, y)) rather than force-unwrapping — no crash on malformed/imported data (covered by the unknown-set path) |
| T-03-04 | Tampering (integrity) | rollup math (X/Y, cost sum) | accept | Pure deterministic function with unit tests pinning the 5 documented cases; no external input beyond in-memory model instances |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Foundation + XCTest only; no new third-party dependency |
</threat_model>

<verification>
- App target builds clean; both catalogs are Foundation-only (grep confirms no SwiftData/@Model/DesignKit).
- `CollectionRollupEngineTests` passes all 5 cases.
- `StatusSetCatalog.all` == 9 entries with labels matching the UI-SPEC table; `CollectionPresetCatalog.all` == 9 presets, generic first.
</verification>

<success_criteria>
- Generic StatusSet exists and is default-eligible (COLL-02).
- Rollup engine returns terminal-only .count, non-nil .costSum, and .none on tracker (COLL-06), pinned by tests.
- The 9 SPEC §5 presets ship as code (COLL-07 catalog source of truth).
</success_criteria>

<output>
Create `.planning/phases/03-collections-c/03-02-SUMMARY.md` when done.
</output>
