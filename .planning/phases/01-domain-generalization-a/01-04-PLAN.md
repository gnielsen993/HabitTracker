---
phase: 01-domain-generalization-a
plan: 04
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - HabitsTracker/Features/RootTabView.swift
  - HabitsTracker/Features/Progress/ProgressDashboardView.swift
  - HabitsTracker/Features/Calendar/CalendarMonthHeatmapView.swift
autonomous: false
requirements: [DOM-06]
must_haves:
  truths:
    - "The tab bar shows exactly Today, Progress, Settings with no Calendar tab (a Hub tab slot is freed for plan 01-05)"
    - "Progress shows a Charts vs Calendar segmented control; selecting Calendar renders the month heatmap and the day-detail sheet still works"
    - "Today is visually unchanged"
  artifacts:
    - path: "HabitsTracker/Features/Progress/ProgressDashboardView.swift"
      provides: "Charts vs Calendar segmented control hosting the folded calendar"
      contains: "pickerStyle(.segmented)"
  key_links:
    - from: "HabitsTracker/Features/Progress/ProgressDashboardView.swift"
      to: "CalendarMonthHeatmapView"
      via: "Calendar segment renders the heatmap inside Progress's NavigationStack"
      pattern: "CalendarMonthHeatmapView"
---

<objective>
Recompose the tab bar to free a slot for the Hub: remove the Calendar tab from RootTabView and fold CalendarMonthHeatmapView (+ its DayDetailSheet) into ProgressDashboardView behind a Charts vs Calendar segmented control. Strip the calendar view's own NavigationStack + navigationTitle("Calendar") so it nests under Progress's single stack. Today stays untouched.

Purpose: DOM-06 — keep 4 tabs. This plan reduces the bar to Today/Progress/Settings (removing Calendar); plan 01-05 adds the Hub tab to reach the target Today/Hub/Progress/Settings. Calendar density and discoverability are preserved via the segmented control (D-13/D-14), and Today is visually unchanged (D-15).
Output: RootTabView without the Calendar tab, Progress with the segmented control, CalendarMonthHeatmapView with its inner stack stripped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-generalization-a/01-RESEARCH.md
@.planning/phases/01-domain-generalization-a/01-UI-SPEC.md

<interfaces>
RootTabView.swift (current — 4 tabs: Today, Calendar, Progress, Settings):
  TabView { TodayView().tabItem{...}; CalendarMonthHeatmapView().tabItem{...}; ProgressDashboardView().tabItem{...}; SettingsView().tabItem{...} }.tint(theme.colors.accentPrimary)
  THIS PLAN: remove the CalendarMonthHeatmapView tabItem. Leaves Today/Progress/Settings. Plan 01-05 inserts the Hub tab.

ProgressDashboardView.swift (current):
  NavigationStack { ScrollView { VStack(spacing: theme.spacing.l) { ...charts body... } }.navigationTitle("Progress") }
  THIS PLAN: add @State progressTab enum {charts, calendar}; a Picker(.segmented) at top with horizontal padding theme.spacing.l; switch renders the extracted charts body or CalendarMonthHeatmapView(). Default .charts. No accent on the control (system tint).

CalendarMonthHeatmapView.swift (current):
  NavigationStack { VStack {...} .navigationTitle("Calendar") .sheet(item: $selectedDay){ DayDetailSheet(date:) } }  (lines 19, 56, 57)
  extension Date: @retroactive Identifiable (line 101 — MUST survive the edit)
  THIS PLAN: remove the wrapping NavigationStack (line 19) and .navigationTitle("Calendar") (line 56); keep the VStack body, the .background, and the .sheet(item: $selectedDay) attached to the VStack. DayDetailSheet has its own NavigationStack and is unaffected (D-14, Research Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip CalendarMonthHeatmapView's inner NavigationStack and fold it into Progress behind a segmented control</name>
  <read_first>
    - HabitsTracker/Features/Calendar/CalendarMonthHeatmapView.swift (NavigationStack at line 19, navigationTitle at 56, sheet at 57, retroactive Identifiable at 101)
    - HabitsTracker/Features/Progress/ProgressDashboardView.swift (the NavigationStack + charts body to host the picker)
    - .planning/phases/01-domain-generalization-a/01-RESEARCH.md (Pattern 3 + Pitfall 4 — two-stack nesting)
    - .planning/phases/01-domain-generalization-a/01-UI-SPEC.md (S5 — segmented control spec, default Charts, theme.spacing.l padding, no accent)
  </read_first>
  <files>HabitsTracker/Features/Calendar/CalendarMonthHeatmapView.swift, HabitsTracker/Features/Progress/ProgressDashboardView.swift</files>
  <action>
    CalendarMonthHeatmapView.swift: remove the wrapping `NavigationStack {` (line 19) and its matching close brace, and remove `.navigationTitle("Calendar")` (line 56). The body becomes the bare `VStack(spacing: theme.spacing.m) { ... }` with its existing `.background(theme.colors.background.ignoresSafeArea())` and `.sheet(item: $selectedDay) { day in DayDetailSheet(date: day) }` attached directly to the VStack (sheets present from anywhere in the hierarchy — Pitfall 4). DO NOT delete `extension Date: @retroactive Identifiable` (line 101) — it is required for `.sheet(item:)`. Keep the month nav buttons, the LazyVGrid, and DayCell unchanged.

    ProgressDashboardView.swift: introduce a private enum `ProgressTab { case charts, calendar }` and `@State private var progressTab: ProgressTab = .charts`. Extract the current charts content (the DKSectionHeader/DKCard chart blocks inside the ScrollView VStack) into a `chartsBody` computed view or @ViewBuilder. Inside the existing `NavigationStack` (Progress owns the single stack), place at the top a `Picker("", selection: $progressTab) { Text("Charts").tag(ProgressTab.charts); Text("Calendar").tag(ProgressTab.calendar) }.pickerStyle(.segmented)` with `.padding(.horizontal, theme.spacing.l)` and no accent tint (system default). Below it, `switch progressTab { case .charts: chartsBody; case .calendar: CalendarMonthHeatmapView() }`. Keep `.navigationTitle("Progress")` on the stack. Default selection is Charts.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && grep -q "pickerStyle(.segmented)" HabitsTracker/Features/Progress/ProgressDashboardView.swift && grep -q "CalendarMonthHeatmapView()" HabitsTracker/Features/Progress/ProgressDashboardView.swift && grep -q "@retroactive Identifiable" HabitsTracker/Features/Calendar/CalendarMonthHeatmapView.swift && ! grep -q navigationTitle..Calendar.. HabitsTracker/Features/Calendar/CalendarMonthHeatmapView.swift</automated>
  </verify>
  <acceptance_criteria>
    - CalendarMonthHeatmapView.swift no longer contains a wrapping NavigationStack or a navigationTitle of "Calendar", but still contains the `.sheet(item: $selectedDay)` and the `@retroactive Identifiable` extension.
    - ProgressDashboardView.swift contains a `.pickerStyle(.segmented)` Picker with Charts/Calendar segments and renders `CalendarMonthHeatmapView()` in the Calendar branch, with default `.charts`.
    - Progress retains a single NavigationStack with `.navigationTitle("Progress")`.
  </acceptance_criteria>
  <done>Calendar is folded into Progress behind a segmented control with one NavigationStack; the day-detail sheet still presents.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the Calendar tab from RootTabView</name>
  <read_first>
    - HabitsTracker/Features/RootTabView.swift (the four tabItems)
    - .planning/phases/01-domain-generalization-a/01-CONTEXT.md (D-12 — Calendar tab removed; 4-tab target Today/Hub/Progress/Settings)
  </read_first>
  <files>HabitsTracker/Features/RootTabView.swift</files>
  <action>
    Remove the `CalendarMonthHeatmapView().tabItem { Label("Calendar", systemImage: "calendar") }` entry from the TabView. The TabView now contains exactly three tabItems: Today, Progress, Settings, in that order. Keep `.tint(theme.colors.accentPrimary)`. Do NOT add the Hub tab here — plan 01-05 inserts the Hub tab (which depends on HubView existing) between Today and Progress to reach the final 4-tab Today/Hub/Progress/Settings IA. Leave a brief comment noting the Hub tab slot is added by 01-05.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && ! grep -q Label..Calendar HabitsTracker/Features/RootTabView.swift && grep -c tabItem HabitsTracker/Features/RootTabView.swift | grep -qx 3</automated>
  </verify>
  <acceptance_criteria>
    - RootTabView.swift contains no `Label("Calendar"...)` tabItem.
    - RootTabView.swift contains exactly 3 `tabItem` occurrences (Today, Progress, Settings).
    - `.tint(theme.colors.accentPrimary)` is retained.
  </acceptance_criteria>
  <done>The Calendar tab is removed; three tabs remain pending the Hub tab added by 01-05.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify tab recomposition + Today unchanged + Calendar reachable</name>
  <action>Build and run on iPhone 17 and visually confirm the tab recomposition per <how-to-verify>: no Calendar tab, Charts/Calendar segmented control works with a single nav bar, the day-detail sheet presents, and Today is visually unchanged.</action>
  <what-built>
    The Calendar tab was removed and the month heatmap folded into Progress behind a Charts vs Calendar segmented control (D-12/D-13/D-14). Today was not edited (D-15).
  </what-built>
  <how-to-verify>
    Build and run on iPhone 17: xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build, then launch.
    1. Confirm the tab bar shows Today, Progress, Settings and NO Calendar tab (Hub arrives in a later plan).
    2. Open Progress: a Charts/Calendar segmented control appears at the top, default Charts; tap Calendar — the month heatmap renders with a single nav bar (no doubled "Calendar" title).
    3. Tap a day in the calendar — the DayDetailSheet still presents correctly.
    4. Open Today — visually identical to before (no layout/spacing/copy change).
  </how-to-verify>
  <acceptance_criteria>
    - Tab bar has no Calendar tab and no doubled nav bars in the Calendar segment.
    - Charts/Calendar segmented control toggles content; day-detail sheet presents.
    - Today is visually unchanged.
  </acceptance_criteria>
  <verify>
    <human-check>Tab bar has no Calendar tab; Charts/Calendar segmented control toggles content with one nav bar; day-detail sheet presents; Today unchanged.</human-check>
  </verify>
  <done>Tab bar (no Calendar), segmented Charts/Calendar, day sheet, and unchanged Today all verified on device.</done>
  <resume-signal>Type "approved" if the tab bar, segmented control, day sheet, and unchanged Today all check out, or describe the issue.</resume-signal>
</task>

</tasks>

<verification>
- Build succeeds: xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build.
- Manual: tab bar without Calendar, segmented Charts/Calendar works, day sheet presents, Today unchanged.
</verification>

<success_criteria>
- DOM-06 (partial): Calendar tab removed, calendar density preserved under Progress, Today unchanged. (Final 4-tab assertion completes in 01-05 when Hub is added.)
</success_criteria>

<output>
Create `.planning/phases/01-domain-generalization-a/01-04-SUMMARY.md` when done.
</output>
