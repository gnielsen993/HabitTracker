---
phase: 01-domain-generalization-a
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - HabitsTracker/Models/Category.swift
  - HabitsTracker/Models/Domain.swift
  - HabitsTracker/Models/Habit.swift
  - HabitsTracker/HabitsTrackerApp.swift
  - HabitsTracker/Services/ExportImportService.swift
  - HabitsTracker/Services/SeedDataService.swift
  - HabitsTracker/Features/Settings/CategoryManagerView.swift
  - HabitsTracker/Features/Settings/HabitEditorView.swift
  - HabitsTracker/Features/Settings/HabitManagerView.swift
  - HabitsTracker/Features/Settings/SettingsView.swift
  - HabitsTracker/Features/Today/TodayView.swift
autonomous: false
requirements: [DOM-01, DOM-02]
must_haves:
  truths:
    - "After upgrading over an existing store, all prior categories appear as Domains with their habits and history intact"
    - "Each Domain carries isFocused (default false) plus name, iconName, colorToken, sortIndex, isSeeded, seedVersion"
    - "Export/Import round-trips at schemaVersion 2 including isFocused"
    - "The app target compiles with zero @Model references to Category (only the originalName string remains)"
  artifacts:
    - path: "HabitsTracker/Models/Domain.swift"
      provides: "renamed @Model class Domain with isFocused"
      contains: "final class Domain"
    - path: "HabitsTracker/Services/ExportImportService.swift"
      provides: "schemaVersion 2 + DomainDTO + isFocused"
      contains: "DomainDTO"
  key_links:
    - from: "HabitsTracker/HabitsTrackerApp.swift"
      to: "Domain"
      via: "modelContainer type list"
      pattern: "Domain.self"
    - from: "HabitsTracker/Models/Habit.swift"
      to: "Domain"
      via: "category relationship retyped to Domain?"
      pattern: "var category: Domain"
---

<objective>
Rename the `@Model class Category` to `Domain` (plan-less, via the migration playbook), add the additive defaulted `isFocused: Bool = false` field, retype `Habit.category` to `Domain?` (keeping the property name `category` to minimize migration surface), update the container type list and ALL ~10 reference sites — including the mechanical Category to Domain substitution in SeedDataService so the app target compiles green — and bump ExportImport to schemaVersion 2 with `DomainDTO` + `isFocused`. Gate the whole change behind the DOM-01 manual upgrade test as a blocking merge gate.

Purpose: This is the schema foundation every later phase files into. The class rename under inferred migration is the phase's #1 empirical risk (Research A1/Pitfall 1) — the upgrade test is the only proof rows survive. This plan delivers a fully compiling, Domain-named app; plan 01-03 layers the NEW seed logic (seedVersion bump, new domains, focus backfill, marker, color resolver) on top.
Output: Domain.swift, retyped Habit, updated container + all reference sites (compile-clean), schemaVersion-2 ExportImport, and a passed (or failed-then-pivoted) upgrade test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Docs/SCHEMA_MIGRATION_PLAYBOOK.md
@Docs/UPGRADE_TEST_RUNBOOK.md
@.planning/phases/01-domain-generalization-a/01-RESEARCH.md
@.planning/phases/01-domain-generalization-a/01-CONTEXT.md

<interfaces>
Reference sites to update (from grep). The Swift property Habit.category stays named `category`; only its TYPE changes from Category? to Domain?. This avoids a property-rename migration (Research A3/Q2).

Container — HabitsTracker/HabitsTrackerApp.swift:
  .modelContainer(for: [Category.self, Habit.self, DailyEntry.self, HabitState.self])

Habit relationship — HabitsTracker/Models/Habit.swift:
  @Relationship var category: Category?   (retype to Domain?, keep property name)

@Query sites (sort by \Category.sortIndex) become (sort by \Domain.sortIndex):
  CategoryManagerView.swift:10, HabitEditorView.swift:11, HabitManagerView.swift:11, SettingsView.swift:11, TodayView.swift:10
TodayView.swift:116 grouped(required:) returns (category: Category, habits: [Habit]) which becomes (category: Domain, ...)

SeedDataService — HabitsTracker/Services/SeedDataService.swift:
  FetchDescriptor<Category>; defaultCategories() -> [Category]; defaultHabits(categoryByName: [String: Category]).
  THIS PLAN: mechanical Category to Domain substitution ONLY (keep seedVersion = 1, the same 12 default domains, the existing restoreMissingDefaults body). New domains + seedVersion bump + backfill belong to 01-03.

ExportImport — HabitsTracker/Services/ExportImportService.swift:
  private let schemaVersion = 1  (becomes 2)
  struct CategoryDTO  (becomes DomainDTO, gains isFocused)
  exportData(categories:habits:entries:)  importReplace(...)  deleteAll uses delete(model: Category.self)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename Category to Domain @Model with isFocused; retype Habit.category; update container</name>
  <read_first>
    - HabitsTracker/Models/Category.swift (class to rename — exact field set)
    - HabitsTracker/Models/Habit.swift (the category: Category? relationship + inverse)
    - HabitsTracker/HabitsTrackerApp.swift (container type list)
    - Docs/SCHEMA_MIGRATION_PLAYBOOK.md (Step 1 additive-field rule, Step 3 originalName, Forbidden moves)
  </read_first>
  <behavior>
    - Test: DomainMigrationTests.testIsFocusedDefaultsFalse — a Domain built without isFocused fetches back with isFocused == false.
    - Test: DomainMigrationTests.testDomainCarriesAllPriorFields — Domain exposes name/iconName/colorToken/sortIndex/isSeeded/seedVersion/isFocused/habits.
  </behavior>
  <files>HabitsTracker/Models/Domain.swift, HabitsTracker/Models/Category.swift, HabitsTracker/Models/Habit.swift, HabitsTracker/HabitsTrackerApp.swift</files>
  <action>
    Create HabitsTracker/Models/Domain.swift containing `@Model final class Domain` with the identical field set from Category (`@Attribute(.unique) var id: UUID`, name, iconName, colorToken, sortIndex, isSeeded, seedVersion), PLUS the new additive defaulted field `var isFocused: Bool = false` (playbook Step 1 — non-optional WITH default; existing rows backfill to false on inferred migration). Apply `@Attribute(originalName: "name")`-style annotations defensively per the playbook on the carried-over stored properties. Keep the inverse `@Relationship(deleteRule: .nullify, inverse: \Habit.category) var habits: [Habit]`. Update the initializer to accept `isFocused: Bool = false` (defaulted, placed last among the value params, before habits).

    DELETE HabitsTracker/Models/Category.swift entirely (do not leave a Category-dupe or a stub — §9.6: a leftover dupe is an invalid redeclaration that breaks the target).

    In Habit.swift, retype the relationship from `var category: Category?` to `var category: Domain?` — KEEP the Swift property name `category` (this is a type change of a relationship target, not a stored-attribute rename, so no originalName is needed on Habit and migration surface stays minimal per Research A3). Update the init parameter type `category: Category?` to `category: Domain?`.

    In HabitsTrackerApp.swift, change the container type list entry `Category.self` to `Domain.self`; keep it plan-less (NO migrationPlan argument — Forbidden).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && test ! -f HabitsTracker/Models/Category.swift && grep -q "var isFocused: Bool = false" HabitsTracker/Models/Domain.swift && grep -q "Domain.self" HabitsTracker/HabitsTrackerApp.swift && grep -q "var category: Domain" HabitsTracker/Models/Habit.swift && ! grep -q migrationPlan HabitsTracker/HabitsTrackerApp.swift</automated>
  </verify>
  <acceptance_criteria>
    - HabitsTracker/Models/Domain.swift contains `final class Domain` with `var isFocused: Bool = false` and all six carried fields.
    - HabitsTracker/Models/Category.swift does not exist; no duplicate Models file with a trailing " 2.swift" exists.
    - HabitsTracker/Models/Habit.swift contains `var category: Domain` (property name unchanged).
    - HabitsTracker/HabitsTrackerApp.swift contains `Domain.self` and contains no `migrationPlan` substring.
  </acceptance_criteria>
  <done>Domain @Model exists with isFocused, Category.swift deleted with no dupe, Habit retyped, container lists Domain plan-less.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update all reference sites (compile-clean) + bump ExportImport to schemaVersion 2 with DomainDTO + isFocused</name>
  <read_first>
    - HabitsTracker/Services/ExportImportService.swift (CategoryDTO, schemaVersion=1, exportData/importReplace/deleteAll)
    - HabitsTracker/Services/SeedDataService.swift (Category fetch + defaultCategories + defaultHabits)
    - HabitsTracker/Features/Today/TodayView.swift (lines 10, 45, 48, 116, 118 — Category query + grouped())
    - HabitsTracker/Features/Settings/CategoryManagerView.swift, HabitEditorView.swift, HabitManagerView.swift, SettingsView.swift (Category @Query + Category construction)
  </read_first>
  <behavior>
    - Test: ExportImportTests round-trip exports a Domain with isFocused: true, importReplace restores it, and the fetched Domain has isFocused == true at schemaVersion 2.
  </behavior>
  <files>HabitsTracker/Services/ExportImportService.swift, HabitsTracker/Services/SeedDataService.swift, HabitsTracker/Features/Today/TodayView.swift, HabitsTracker/Features/Settings/CategoryManagerView.swift, HabitsTracker/Features/Settings/HabitEditorView.swift, HabitsTracker/Features/Settings/HabitManagerView.swift, HabitsTracker/Features/Settings/SettingsView.swift</files>
  <action>
    ExportImportService.swift — make these coupled edits in one pass (Research Pitfall 5): set `private let schemaVersion = 2`; rename `struct CategoryDTO` to `struct DomainDTO` and add `let isFocused: Bool`; change the bundle field type `categories: [CategoryDTO]` to `[DomainDTO]` (keep the JSON key `categories` to avoid a wider rename; keep export and import symmetric); update `exportData(categories:...)` to map each Domain into `DomainDTO(... isFocused: $0.isFocused)`; update `importReplace` to build `Domain(... isFocused: dto.isFocused)`; update deleteAll to `try context.delete(model: Domain.self)` (was Category.self). The importReplace schema guard `bundle.schemaVersion == schemaVersion` now hard-rejects anything not equal to 2.

    SeedDataService.swift — MECHANICAL substitution ONLY so the target compiles: `FetchDescriptor<Category>` to `FetchDescriptor<Domain>`; `defaultCategories() -> [Category]` to `-> [Domain]` returning `Domain(...)` instances; `defaultHabits(categoryByName: [String: Category])` to `[String: Domain]`; the inner `func c(_:) -> Category?` to `-> Domain?`. KEEP `seedVersion = 1`, the SAME 12 default domains (no new ones), and the existing restoreMissingDefaults body unchanged. Do NOT add a seedVersion bump, new domains, a persisted marker, or any backfill — those are owned by 01-03 (which depends on this plan and edits this file next).

    Reference views — replace every Category symbol with Domain:
    - CategoryManagerView.swift: `@Query(sort: \Domain.sortIndex) private var categories: [Domain]`; the `Category(name:...)` construction becomes `Domain(name:...)` (curated default icon "square.grid.2x2", colorToken "forest" kept). Leave the file NAME CategoryManagerView.swift as-is this plan (the focus picker that supersedes it lands in 01-06).
    - HabitEditorView.swift: `@Query(sort: \Domain.sortIndex) private var categories: [Domain]`; the Picker get/set on habit.category?.id is unchanged (property name stays category).
    - HabitManagerView.swift: `@Query(sort: \Domain.sortIndex) private var categories: [Domain]`.
    - SettingsView.swift: `@Query(sort: \Domain.sortIndex) private var categories: [Domain]`; update the fileExporter defaultFilename from "habittracker-backup-v1" to "habittracker-backup-v2".
    - TodayView.swift: `@Query(sort: \Domain.sortIndex)`; grouped(required:) return type `(category: Domain, habits: [Habit])`; the inner type annotation at line ~116. Do NOT change any visible layout, copy, spacing, or styling — Today stays visually unchanged (D-15).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && grep -q "private let schemaVersion = 2" HabitsTracker/Services/ExportImportService.swift && grep -q DomainDTO HabitsTracker/Services/ExportImportService.swift && grep -q backup-v2 HabitsTracker/Features/Settings/SettingsView.swift && ! grep -rIl "FetchDescriptor<Category>" HabitsTracker/Services && ! grep -rIl "Category.self" HabitsTracker/Features HabitsTracker/Services</automated>
  </verify>
  <acceptance_criteria>
    - ExportImportService.swift contains `schemaVersion = 2`, `DomainDTO`, an `isFocused` DTO field, and `delete(model: Domain.self)`.
    - SettingsView.swift fileExporter defaultFilename contains `backup-v2`.
    - No `FetchDescriptor<Category>` remains in Services; no `Category.self` remains under Features or Services.
    - TodayView.swift grouped return tuple uses `category: Domain`.
  </acceptance_criteria>
  <done>All app reference sites use Domain (target compiles); ExportImport is schemaVersion 2 with DomainDTO+isFocused; Today layout untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: DOM-01 manual upgrade-test gate (data integrity across the class rename)</name>
  <action>Run the DOM-01 manual upgrade test per Docs/UPGRADE_TEST_RUNBOOK.md (see <how-to-verify>). This is the blocking merge gate for the Category-to-Domain class rename; do not advance past this checkpoint until rows are confirmed to survive the rename (or the relabel-only pivot is invoked).</action>
  <what-built>
    The Category to Domain @Model class rename under plan-less inferred migration, with the additive `isFocused: Bool = false` field and schemaVersion-2 Export/Import. The single empirically-unverified risk (Research A1/Pitfall 1) is whether SwiftData preserves existing rows when the @Model class name changes.
  </what-built>
  <how-to-verify>
    Run the committed runbook Docs/UPGRADE_TEST_RUNBOOK.md against gn.HabitsTracker on the iPhone 17 simulator:
    1. git checkout f564d15; xcrun simctl uninstall booted gn.HabitsTracker; build + install + launch the OLD app.
    2. In the OLD app: create a category, toggle a few habits, log a day. Terminate.
    3. git checkout the current branch; build + install + launch the NEW app over the same store (do NOT uninstall between builds — the store must persist).
    4. Confirm: app launches (PID greater than 0) AND all prior categories appear as Domains with their habits and the logged day visible.
    5. Run the full automated suite: xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' test -quiet — DomainMigrationTests + ExportImportTests green.
  </how-to-verify>
  <acceptance_criteria>
    - App launches over the old store without crashing (PID greater than 0).
    - All prior categories-as-Domains + their habits + the logged day are visible (no data loss).
    - DomainMigrationTests and ExportImportTests pass at schemaVersion 2.
    - If rows do NOT survive: STOP — do not merge; pivot D-01 to relabel-only per the runbook FAIL action (keep @Model class named Category, label "Domain" in UI) before any Hub UI is built.
  </acceptance_criteria>
  <verify>
    <human-check>App launches over the old store (PID > 0) with all prior data visible; DomainMigrationTests + ExportImportTests green at schemaVersion 2.</human-check>
  </verify>
  <done>The upgrade test passed (no data loss) and the full suite is green, OR the relabel-only pivot was invoked per the runbook FAIL action.</done>
  <resume-signal>Type "approved" if rows survived and the suite is green, or describe the data-loss observed (triggers the relabel-only pivot).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing on-device store -> new build | The only store-incompatible move this phase: the @Model class rename runs against persisted rows |
| imported JSON file -> app | User-supplied backup file decoded by importReplace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering/Availability | Category to Domain class rename under inferred migration | mitigate | Mandatory DOM-01 upgrade test (Task 3) as a blocking merge gate; Export/Import JSON as the always-available recovery net (D-03) |
| T-01-02 | Tampering | importReplace decoding a malformed/old backup | mitigate | schemaVersion == 2 hard guard already rejects mismatched bundles; error surfaced in SettingsView |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | N/A — this phase installs zero external packages (Research Package Legitimacy Audit: N/A); pure Apple SDK + local-path DesignKit |
</threat_model>

<verification>
- Full suite green: xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' test -quiet.
- Manual DOM-01 upgrade test passed per Docs/UPGRADE_TEST_RUNBOOK.md (the hard merge gate).
- Today is visually unchanged (manual glance — no layout/copy/spacing edits in TodayView).
</verification>

<success_criteria>
- DOM-01: upgrade over an old store preserves all categories-as-Domains + habits + history (upgrade test green) OR the relabel-only pivot is invoked.
- DOM-02: Domain carries isFocused (default false) plus all prior fields.
- ExportImport round-trips at schemaVersion 2 including isFocused.
- App target compiles; zero Category @Model references remain except the originalName string.
</success_criteria>

<output>
Create `.planning/phases/01-domain-generalization-a/01-02-SUMMARY.md` when done.
</output>
