---
phase: 01-domain-generalization-a
plan: 06
type: execute
wave: 4
depends_on: [01-02, 01-03]
files_modified:
  - HabitsTracker/Features/Settings/DomainFocusPicker.swift
  - HabitsTracker/Features/Settings/DomainIconPicker.swift
  - HabitsTracker/Features/Settings/DomainColorSwatchRow.swift
  - HabitsTracker/Features/Settings/DomainCreateSheet.swift
  - HabitsTracker/Features/Settings/SettingsView.swift
autonomous: false
requirements: [DOM-04, DOM-05]
must_haves:
  truths:
    - "A focus picker lists all domains with a per-row focus toggle; toggling flips isFocused and never deletes content"
    - "Newly merge-added domains show a 'New' badge + a one-line hint caption in the picker"
    - "A user can create a custom domain (name + curated SF Symbol + one of 5 color tokens) that persists and appears in the catalog"
    - "The color choice is closed to the 5 accent tokens (no wheel/hex) and a custom domain's delete preserves its habits"
  artifacts:
    - path: "HabitsTracker/Features/Settings/DomainFocusPicker.swift"
      provides: "catalog with focus toggles + New hint"
      contains: "isFocused"
    - path: "HabitsTracker/Features/Settings/DomainIconPicker.swift"
      provides: "curated ~30 SF Symbol grid"
      contains: "LazyVGrid"
    - path: "HabitsTracker/Features/Settings/DomainColorSwatchRow.swift"
      provides: "closed 5-token swatch row"
      contains: "accentColor"
  key_links:
    - from: "HabitsTracker/Features/Settings/DomainFocusPicker.swift"
      to: "Domain.isFocused"
      via: "Toggle bound to each domain's isFocused, save on change"
      pattern: "isFocused"
    - from: "HabitsTracker/Features/Settings/SettingsView.swift"
      to: "DomainFocusPicker / DomainCreateSheet"
      via: "NavigationLink + New Domain entry point"
      pattern: "DomainFocusPicker"
---

<objective>
Build the focus picker and custom-domain creation (DOM-04 + DOM-05): DomainFocusPicker lists all domains with a per-row focus Toggle (flips isFocused; never deletes content) and surfaces a "New" DKBadge + hint caption on merge-added domains (D-10); DomainCreateSheet collects a trimmed name + a curated ~30-symbol DomainIconPicker grid (D-16) + a closed 5-token DomainColorSwatchRow (D-17), persisting a valid-by-construction custom domain. Wire both into SettingsView (replacing the Manage Categories link).

Purpose: DOM-04 (focus add/remove a Hub tile without data loss) + DOM-05 (custom domain with name+symbol+token persists). Restraint is structurally enforced — the user cannot pick an off-brand glyph (curated set) or color (closed swatch), so colorToken is always a real DesignKit accent token.
Output: Features/Settings/{DomainFocusPicker,DomainIconPicker,DomainColorSwatchRow,DomainCreateSheet}.swift + SettingsView wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-generalization-a/01-UI-SPEC.md
@.planning/phases/01-domain-generalization-a/01-RESEARCH.md

<interfaces>
Domain @Model (from 01-02): id, name, iconName, colorToken, sortIndex, isSeeded, seedVersion, isFocused, habits.
Resolver (from 01-03): accentColor(forToken: String, scheme: ColorScheme) -> Color (HabitsTracker/Utilities/AccentTokenColor.swift).
Existing create pattern (CategoryManagerView, from 01-02) — mirror this insert/save:
  let domain = Domain(name:, iconName:, colorToken:, sortIndex: (domains.last?.sortIndex ?? -1) + 1)
  modelContext.insert(domain); try? modelContext.save()
Theme access: @EnvironmentObject themeManager; @Environment(\.colorScheme); @Environment(\.modelContext).
DesignKit: DKBadge(_:theme:), DKButton, DKSectionHeader, theme.colors.fillSelected, theme.colors.danger, theme.colors.border.

The 5 accent tokens (closed set, D-17): ["forest", "navy", "maroon", "walnut", "stone"].
"New" hint (D-10): a domain qualifies as newly merge-added when isSeeded == true && isFocused == false && seedVersion == 2 (the merge-add tag from 01-03). Show a DKBadge "New" inline + a caption header "New domains are available — focus any to add it to your Hub." (theme.typography.caption / textSecondary).

UI-SPEC copy (binding):
  Create entry button: "New Domain"; sheet save CTA: "Add Domain".
  Name validation: "Give this domain a name to continue." (Add Domain disabled until trimmed non-empty).
  Delete confirmation (custom domains only): "Delete '<name>'? Habits filed here won't be deleted — they'll just lose this domain." Confirm "Delete" (danger), cancel "Cancel".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DomainIconPicker (curated ~30 SF Symbols) + DomainColorSwatchRow (closed 5 tokens)</name>
  <read_first>
    - .planning/phases/01-domain-generalization-a/01-UI-SPEC.md (S4 — icon grid + swatch row specs, selected ring, >=44pt cells, accessibility names)
    - HabitsTracker/Utilities/AccentTokenColor.swift (swatch colors from accentColor)
    - HabitsTracker/Services/SeedDataService.swift (existing seeded iconNames to keep the curated set consistent)
  </read_first>
  <files>HabitsTracker/Features/Settings/DomainIconPicker.swift, HabitsTracker/Features/Settings/DomainColorSwatchRow.swift</files>
  <action>
    DomainIconPicker.swift: a data-driven view taking a `@Binding var selection: String` (the chosen iconName) plus theme. Define a static `let curatedSymbols: [String]` of ~30 hand-picked, lifestyle-relevant SF Symbols consistent with the seeded glyphs (include the existing seeded ones like checklist, book, sun.max, heart, figure.run, person.2, brain, house, dollarsign.circle, paintbrush, briefcase, tray.full, plus additions such as tshirt, fork.knife, banknote, play.rectangle, music.note, airplane, cart, gym/dumbbell, leaf, camera, gamecontroller, fork.knife.circle, etc. — finalize the ~30, Claude's discretion per CONTEXT). Render a `LazyVGrid` (cell gap theme.spacing.m) of `Image(systemName:)` cells, each >= 44pt; the selected cell is backed by `theme.colors.fillSelected` with an accent ring. Tapping a cell sets the binding. Accessibility: each cell labels the symbol's human name + selected state. NO full system symbol browser, NO third-party dependency (D-16).

    DomainColorSwatchRow.swift: a data-driven view taking `@Binding var selection: String` (the chosen colorToken) plus theme + colorScheme. Render a horizontal row (item gap theme.spacing.s) of exactly 5 swatches over `["forest","navy","maroon","walnut","stone"]`, each filled via `accentColor(forToken: token, scheme:)`; the selected swatch carries an accent ring (selected state, D-17). Tapping sets the binding to that token. NO color wheel, NO hex field — colorToken is valid-by-construction. Accessibility: each swatch labels its color name (e.g. "Forest") + selected state, tap target >= 44pt.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && grep -q "LazyVGrid" HabitsTracker/Features/Settings/DomainIconPicker.swift && grep -q "curatedSymbols" HabitsTracker/Features/Settings/DomainIconPicker.swift && grep -q "accentColor" HabitsTracker/Features/Settings/DomainColorSwatchRow.swift && grep -q "forest" HabitsTracker/Features/Settings/DomainColorSwatchRow.swift && grep -q "stone" HabitsTracker/Features/Settings/DomainColorSwatchRow.swift && ! grep -q "ColorPicker\|Color(hex" HabitsTracker/Features/Settings/DomainColorSwatchRow.swift</automated>
  </verify>
  <acceptance_criteria>
    - DomainIconPicker.swift has a static curated `[String]` of SF Symbols in a LazyVGrid with >=44pt selectable cells, bound to a selection, no system browser/third-party dep.
    - DomainColorSwatchRow.swift renders exactly the 5 accent tokens via accentColor with a selected ring, bound to a colorToken; contains no ColorPicker / Color(hex.
    - Both views are data-driven (take bindings, no @Query) and use only theme tokens.
  </acceptance_criteria>
  <done>The curated icon grid and the closed 5-token swatch row exist, both data-driven and brand-constrained.</done>
</task>

<task type="auto">
  <name>Task 2: DomainCreateSheet + DomainFocusPicker (with New hint) + wire into SettingsView</name>
  <read_first>
    - .planning/phases/01-domain-generalization-a/01-UI-SPEC.md (S3 focus picker + New hint + delete confirmation; S4 create sheet CTA + name validation)
    - HabitsTracker/Features/Settings/CategoryManagerView.swift (insert/save pattern + onDelete to mirror)
    - HabitsTracker/Features/Settings/SettingsView.swift (Management section to rewire)
  </read_first>
  <files>HabitsTracker/Features/Settings/DomainCreateSheet.swift, HabitsTracker/Features/Settings/DomainFocusPicker.swift, HabitsTracker/Features/Settings/SettingsView.swift</files>
  <action>
    DomainCreateSheet.swift: a sheet (theme.radii.sheet) with `@Environment(\.modelContext)` and three inputs — a Name TextField (trim + length-guard like CategoryManagerView), the `DomainIconPicker(selection:)`, and the `DomainColorSwatchRow(selection:)`. State holds `name`, `iconName` (default a neutral curated symbol e.g. "square.grid.2x2"), `colorToken` (default "forest"). The "Add Domain" CTA is disabled until the trimmed name is non-empty (show "Give this domain a name to continue." inline when empty). On save: build `Domain(name:iconName:colorToken:sortIndex: (existing max sortIndex)+1)` with `isFocused: true` (a domain you deliberately create belongs in your Hub) and `isSeeded: false`; `modelContext.insert` + `try? modelContext.save()`; dismiss. The new domain appears in the catalog (DOM-05).

    DomainFocusPicker.swift: a List/Form over `@Query(sort: \Domain.sortIndex) private var domains: [Domain]` (parent owns the query). Each row: glyph (accent-tinted via accentColor) + name (theme.typography.headline) + a `Toggle` bound to that domain's `isFocused` (on change, `try? modelContext.save()`) — toggling NEVER deletes content (unfocus only hides the Hub tile, D-04). For domains where `isSeeded && !isFocused && seedVersion == 2`, show an inline DKBadge "New"; if any such domain exists, show the caption header "New domains are available — focus any to add it to your Hub." (D-10). Custom domains (isSeeded == false) support swipe-to-delete with the confirmation dialog ("Delete '<name>'? Habits filed here won't be deleted — they'll just lose this domain." / Delete in danger / Cancel) — deletion relies on the `.nullify` rule so habits survive. A "New Domain" toolbar button presents `DomainCreateSheet`. Empty state (§9.3) if there are zero domains. navigationTitle "Domains".

    SettingsView.swift: in the Management section, replace `NavigationLink("Manage Categories") { CategoryManagerView() }` with `NavigationLink("Manage Domains") { DomainFocusPicker() }`. Leave the rest of Settings (Appearance, Backup, Restore Defaults) unchanged.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && grep -q "DomainFocusPicker" HabitsTracker/Features/Settings/SettingsView.swift && grep -q "Add Domain" HabitsTracker/Features/Settings/DomainCreateSheet.swift && grep -q "isFocused" HabitsTracker/Features/Settings/DomainFocusPicker.swift && grep -q "New domains are available" HabitsTracker/Features/Settings/DomainFocusPicker.swift && grep -q "won't be deleted" HabitsTracker/Features/Settings/DomainFocusPicker.swift</automated>
  </verify>
  <acceptance_criteria>
    - DomainCreateSheet.swift gates "Add Domain" on a non-empty trimmed name, uses DomainIconPicker + DomainColorSwatchRow, and inserts a Domain with isFocused: true + isSeeded: false.
    - DomainFocusPicker.swift toggles isFocused per row (saving), shows a "New" badge + hint for seedVersion-2 unfocused seeded domains, and confirms custom-domain delete with the habit-preserving copy.
    - SettingsView.swift links to DomainFocusPicker (Manage Domains) instead of CategoryManagerView.
  </acceptance_criteria>
  <done>Focus picker (toggle + New hint + safe delete) and custom-domain creation are wired into Settings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify focus toggle (no data loss), New hint, and custom-domain creation</name>
  <action>Build and run on iPhone 17 and verify focus + creation behavior per <how-to-verify>: focus toggle adds/removes a Hub tile without deleting the domain or habits; the New badge/hint shows for merge-added domains; custom-domain creation (gated name + curated symbol + 5-token color) persists and appears in the catalog; deleting a custom domain preserves its habits.</action>
  <what-built>
    DomainFocusPicker (per-row focus toggle + New badge/hint + safe custom-domain delete) and DomainCreateSheet (curated icon grid + closed 5-token swatch row), wired into Settings (DOM-04/DOM-05).
  </what-built>
  <how-to-verify>
    Build + run on iPhone 17. In Settings > Manage Domains:
    1. Toggle a domain's focus ON, return to Hub — a tile appears; toggle OFF — the tile disappears but the domain (and any habits filed under it) still exist in the picker (no data deleted).
    2. Confirm merge-added domains (Style/Diet/Money/Media) show a "New" badge and the hint caption; focusing one adds its Hub tile.
    3. Tap "New Domain": the "Add Domain" button is disabled until a name is typed; pick a curated symbol and one of the 5 swatches (no color wheel/hex); save — the new domain appears in the catalog and (created focused) in the Hub.
    4. Swipe-delete a custom domain: the confirmation states habits won't be deleted; confirm and verify any habit previously filed under it survives.
  </how-to-verify>
  <acceptance_criteria>
    - Focus toggle adds/removes a Hub tile without deleting the domain or its habits.
    - New badge + hint appear for merge-added domains.
    - Custom domain (name + curated symbol + 5-token color) persists, appears in the catalog, with Add Domain gated on a non-empty name.
    - Deleting a custom domain preserves its habits (nullify).
  </acceptance_criteria>
  <verify>
    <human-check>Focus toggle is non-destructive; New badge + hint appear; custom domain persists in catalog with Add Domain gated on a name; deleting a custom domain preserves its habits.</human-check>
  </verify>
  <done>Non-destructive focus toggling, the New hint, gated custom-domain creation, and habit-preserving delete all verified on device.</done>
  <resume-signal>Type "approved" if focus toggling is non-destructive, the New hint shows, and custom-domain creation/deletion behaves per spec, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text input (domain name) -> store | Free-form TextField is the only user-controlled string written this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering (input validation) | custom-domain name TextField | mitigate | Trim + length-guard before insert (mirrors CategoryManagerView); Add Domain disabled until non-empty trimmed name (ASVS V5 light) |
| T-01-04 | Tampering (input validation) | iconName / colorToken | accept | Valid-by-construction: iconName from a closed curated set (D-16), colorToken from a closed 5-swatch row (D-17) — user cannot inject an arbitrary glyph or off-palette color |
</threat_model>

<verification>
- Build: xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build.
- DomainCreateTests green: custom domain persists with a colorToken in the 5-token set.
- Manual: non-destructive focus toggle, New hint, gated creation, habit-preserving delete.
- Each new file under ~400 lines (§9.1); pickers data-driven (§9.2); empty states present (§9.3).
</verification>

<success_criteria>
- DOM-04: focus picker focuses/unfocuses domains; focusing adds a Hub tile, unfocusing hides it without deleting content; New hint surfaces merge-added domains.
- DOM-05: a custom domain (name + curated SF Symbol + 5-token color) persists and appears in the catalog; color is valid-by-construction.
</success_criteria>

<output>
Create `.planning/phases/01-domain-generalization-a/01-06-SUMMARY.md` when done.
</output>
