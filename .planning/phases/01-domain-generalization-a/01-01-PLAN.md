---
phase: 01-domain-generalization-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HabitsTrackerTests/DomainMigrationTests.swift
  - HabitsTrackerTests/BootstrapBackfillTests.swift
  - HabitsTrackerTests/DomainCreateTests.swift
  - HabitsTrackerTests/ExportImportTests.swift
  - Docs/UPGRADE_TEST_RUNBOOK.md
autonomous: true
requirements: [DOM-01, DOM-02, DOM-04, DOM-05]
must_haves:
  truths:
    - "A failing test exists for every Wave-0 behavior named in 01-VALIDATION.md before any production code is written"
    - "The DOM-01 upgrade-test procedure is captured as a committed, runnable runbook"
  artifacts:
    - path: "HabitsTrackerTests/DomainMigrationTests.swift"
      provides: "isFocused default + Domain field-shape assertions"
      contains: "DomainMigrationTests"
    - path: "HabitsTrackerTests/BootstrapBackfillTests.swift"
      provides: "version-gated backfill + idempotency + merge-add assertions"
      contains: "BootstrapBackfillTests"
    - path: "HabitsTrackerTests/DomainCreateTests.swift"
      provides: "custom-domain persistence assertion"
      contains: "DomainCreateTests"
    - path: "Docs/UPGRADE_TEST_RUNBOOK.md"
      provides: "DOM-01 manual upgrade-test merge-gate procedure"
      contains: "gn.HabitsTracker"
  key_links:
    - from: "HabitsTrackerTests/*"
      to: "Domain / BootstrapService / SeedDataService"
      via: "@testable import HabitsTracker"
      pattern: "@testable import HabitsTracker"
---

<objective>
Create the Wave-0 validation scaffold for Phase 1: failing unit tests covering every behavior in 01-VALIDATION.md (Domain field shape + isFocused default, version-gated focus backfill idempotency, merge-add, custom-domain persistence, Export/Import round-trip at schemaVersion 2) and a committed runbook for the DOM-01 manual upgrade test.

Purpose: Enforce the Nyquist rule — every downstream task has an automated verify that exists before its implementation, so a regression is caught within ~120s. The upgrade-test runbook makes the DOM-01 data-integrity merge gate a concrete, repeatable procedure rather than a checkbox.
Output: Three new test files, an updated ExportImportTests, and Docs/UPGRADE_TEST_RUNBOOK.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-generalization-a/01-VALIDATION.md
@.planning/phases/01-domain-generalization-a/01-RESEARCH.md
@Docs/SCHEMA_MIGRATION_PLAYBOOK.md

<interfaces>
<!-- Current model + service shapes the tests assert against. Domain/isFocused do NOT exist yet —
     these tests are RED until plan 01-02/01-03 implement them. -->

From HabitsTracker/Models/Category.swift (to become Domain in 01-02):
final class Category {
  var id: UUID; var name: String; var iconName: String; var colorToken: String
  var sortIndex: Int; var isSeeded: Bool; var seedVersion: Int
  @Relationship(deleteRule: .nullify, inverse: \Habit.category) var habits: [Habit]
}

From HabitsTracker/Services/BootstrapService.swift:
final class BootstrapService { func bootstrapIfNeeded(context: ModelContext) throws }
From HabitsTracker/Services/SeedDataService.swift:
final class SeedDataService { func seedIfNeeded(context: ModelContext) throws; func restoreMissingDefaults(context: ModelContext) throws }

Existing in-memory test pattern (ExportImportTests.swift, XCTest):
let schema = Schema([Category.self, Habit.self, DailyEntry.self, HabitState.self])
let config = ModelConfiguration(isStoredInMemoryOnly: true)
let container = try ModelContainer(for: schema, configurations: [config])
let context = container.mainContext
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author failing unit tests for Domain shape, backfill, and custom-domain persistence</name>
  <read_first>
    - HabitsTrackerTests/ExportImportTests.swift (in-memory ModelContainer + XCTest pattern to mirror)
    - .planning/phases/01-domain-generalization-a/01-VALIDATION.md (per-task verification map + Wave 0 list)
    - .planning/phases/01-domain-generalization-a/01-RESEARCH.md (Pattern 2 backfill semantics: previous>0 guard, two-run idempotency)
  </read_first>
  <files>HabitsTrackerTests/DomainMigrationTests.swift, HabitsTrackerTests/BootstrapBackfillTests.swift, HabitsTrackerTests/DomainCreateTests.swift</files>
  <action>
    Create three XCTest files (match ExportImportTests.swift style: `import XCTest`, `import SwiftData`, `@testable import HabitsTracker`, in-memory `ModelConfiguration(isStoredInMemoryOnly: true)`). These reference the not-yet-existing `Domain` type and the not-yet-existing `isFocused` field / `lastSeededVersion`-gated backfill, so they MUST fail to compile or fail assertions until 01-02 and 01-03 land — that is the intended RED state.

    DomainMigrationTests.swift — class `DomainMigrationTests: XCTestCase`:
    - `testIsFocusedDefaultsFalse`: insert a `Domain(name:iconName:colorToken:sortIndex:)` into an in-memory context using the designated initializer WITHOUT passing isFocused; fetch it back; assert `isFocused == false`.
    - `testDomainCarriesAllPriorFields`: construct a `Domain` and assert it exposes `name`, `iconName`, `colorToken`, `sortIndex`, `isSeeded`, `seedVersion`, `isFocused`, and a `habits` array (compile-level shape lock).

    BootstrapBackfillTests.swift — class `BootstrapBackfillTests: XCTestCase`. Use a per-test UserDefaults suite (`UserDefaults(suiteName:)` with a unique name, removePersistentDomain in setUp/tearDown) so the `lastSeededVersion` marker is isolated. Assert the behavior 01-03 will implement:
    - `testBackfillFlipsPreexistingDomainsWhenUpgrading`: simulate an upgrade by seeding domains with `isFocused == false` and a prior marker `> 0` and `< 2`; run the bootstrap focus-backfill; assert every pre-existing Domain is now `isFocused == true`.
    - `testBackfillRunsOnce`: run the bootstrap pass twice; after run 1, unfocus one Domain in the context; run 2 must NOT re-focus it (assert it stays `false`) — proves the `lastSeededVersion`-gated idempotency.
    - `testFreshInstallDoesNotBackfillExistingRows`: with marker `== 0` (fresh), run bootstrap; assert the merge-added new domains (Style/Diet/Money/Media) are `isFocused == false` (the `previous > 0` guard skips the existing-row flip).
    - `testMergeAddIsUnfocusedAndDedupesByName`: pre-seed a Domain named "Social"; run the merge-add; assert exactly one "Social" exists (name-keyed dedupe) and that newly added domains are `isFocused == false`.

    DomainCreateTests.swift — class `DomainCreateTests: XCTestCase`:
    - `testCustomDomainPersistsWithValidToken`: insert a `Domain` built from a name + a curated SF Symbol string + a colorToken drawn from the 5-token set `["forest","navy","maroon","walnut","stone"]`; save; fetch; assert it round-trips and `["forest","navy","maroon","walnut","stone"].contains(domain.colorToken)`.

    Do NOT stub out `Domain`/backfill to make these pass — they are deliberately RED. Use `os.Logger` if any logging is needed; never `print()`.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && git diff --stat HabitsTrackerTests/DomainMigrationTests.swift HabitsTrackerTests/BootstrapBackfillTests.swift HabitsTrackerTests/DomainCreateTests.swift | grep -q "3 files"</automated>
  </verify>
  <acceptance_criteria>
    - HabitsTrackerTests/DomainMigrationTests.swift contains `class DomainMigrationTests` and a `testIsFocusedDefaultsFalse` method asserting `isFocused == false`.
    - HabitsTrackerTests/BootstrapBackfillTests.swift contains `class BootstrapBackfillTests` with the four named methods (`testBackfillFlipsPreexistingDomainsWhenUpgrading`, `testBackfillRunsOnce`, `testFreshInstallDoesNotBackfillExistingRows`, `testMergeAddIsUnfocusedAndDedupesByName`).
    - HabitsTrackerTests/DomainCreateTests.swift contains `class DomainCreateTests` and asserts `colorToken` is one of the 5 accent tokens.
    - All three files use `@testable import HabitsTracker` and an in-memory `ModelConfiguration(isStoredInMemoryOnly: true)`.
    - No `print(` appears in any of the three files.
  </acceptance_criteria>
  <done>Three test files exist, are RED (reference Domain/isFocused/backfill that do not yet exist), and mirror the existing XCTest in-memory pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Update ExportImportTests to schemaVersion-2 Domain round-trip + write the DOM-01 upgrade runbook</name>
  <read_first>
    - HabitsTrackerTests/ExportImportTests.swift (current Category round-trip)
    - HabitsTracker/Services/ExportImportService.swift (schemaVersion=1, CategoryDTO shape)
    - Docs/SCHEMA_MIGRATION_PLAYBOOK.md (Step 4 upgrade-test command sequence, iPhone 17 / gn.HabitsTracker)
  </read_first>
  <files>HabitsTrackerTests/ExportImportTests.swift, Docs/UPGRADE_TEST_RUNBOOK.md</files>
  <action>
    Edit ExportImportTests.swift so its round-trip targets the post-rename shape (RED until 01-02 renames the type + bumps schemaVersion): replace `HabitsTracker.Category` / `Category(` references with `Domain` / `Domain(`, replace the schema array `Category.self` with `Domain.self`, pass `isFocused: true` when constructing the test Domain, and after import assert the fetched Domain's `isFocused == true` (proving isFocused survives the round-trip). Keep the existing habit + entry + state assertions. The test must continue to exercise `exportData(...)` / `importReplace(...)`; do not weaken the schema-version guard.

    Create Docs/UPGRADE_TEST_RUNBOOK.md by extracting the playbook Step-4 sequence into a standalone, runnable DOM-01 merge-gate runbook. It MUST contain: the last-shipped build SHA to check out (`f564d15`); the exact `xcrun simctl uninstall booted gn.HabitsTracker`, `xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath /tmp/htbuild_old build`, install, and `xcrun simctl launch booted gn.HabitsTracker` commands for the OLD build; an explicit "create a category + toggle habits + log a day, then terminate" step; the NEW-build rebuild/install/launch over the same store; the pass criterion ("app launches with PID>0 AND all prior categories→domains + their habits + logged day are visible"); and the FAIL action: "do NOT merge — pivot D-01 to relabel-only (keep @Model class named Category, label 'Domain' in UI) before any Hub UI." Reference Docs/SCHEMA_MIGRATION_PLAYBOOK.md as the source. State that this runbook is owner-side/manual (SPEC is planning-only) and is the DOM-01 hard gate consumed by plan 01-02's checkpoint.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && grep -q "gn.HabitsTracker" Docs/UPGRADE_TEST_RUNBOOK.md && grep -q "f564d15" Docs/UPGRADE_TEST_RUNBOOK.md && grep -vq "Category(" HabitsTrackerTests/ExportImportTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - HabitsTrackerTests/ExportImportTests.swift no longer constructs `Category(` and instead constructs `Domain(` with `isFocused:`, and asserts `isFocused == true` after import.
    - Docs/UPGRADE_TEST_RUNBOOK.md contains the literal strings `gn.HabitsTracker`, `iPhone 17`, the SHA `f564d15`, and the FAIL action mentioning `relabel-only`.
    - Docs/UPGRADE_TEST_RUNBOOK.md references `Docs/SCHEMA_MIGRATION_PLAYBOOK.md`.
  </acceptance_criteria>
  <done>ExportImportTests targets the Domain/schemaVersion-2 round-trip (RED until 01-02), and a standalone DOM-01 upgrade runbook is committed.</done>
</task>

</tasks>

<verification>
- The three new test files and the edited ExportImportTests compile-target `Domain`/`isFocused`, which do not exist yet — so the suite is expected RED at this point. That is correct for Wave 0; do NOT make them green by stubbing production types.
- Docs/UPGRADE_TEST_RUNBOOK.md is self-contained and runnable by the owner.
</verification>

<success_criteria>
- Every Wave-0 file listed in 01-VALIDATION.md exists (DomainMigrationTests, BootstrapBackfillTests, ExportImportTests updated, DomainCreateTests) plus the upgrade runbook.
- No `print(` introduced; tests use `@testable import HabitsTracker` + in-memory store.
- The DOM-01 manual upgrade-test procedure is a committed, concrete runbook with the FAIL→relabel-only fallback recorded.
</success_criteria>

<output>
Create `.planning/phases/01-domain-generalization-a/01-01-SUMMARY.md` when done.
</output>
