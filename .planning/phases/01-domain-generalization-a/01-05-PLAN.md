---
phase: 01-domain-generalization-a
plan: 05
type: execute
wave: 4
depends_on: [01-03, 01-04]
files_modified:
  - HabitsTracker/Features/Hub/HubView.swift
  - HabitsTracker/Features/Hub/DomainTile.swift
  - HabitsTracker/Features/Hub/DomainDetailView.swift
  - HabitsTracker/Features/RootTabView.swift
autonomous: false
requirements: [DOM-03, DOM-06]
must_haves:
  truths:
    - "The Hub tab shows focused domains as an icon+color grid"
    - "Tapping a tile opens DomainDetailView showing only non-empty sections (Phase 1: header + empty state)"
    - "When no domains are focused, the Hub shows its designed empty state with a link to the focus picker"
    - "The tab bar is exactly Today, Hub, Progress, Settings (4 tabs)"
  artifacts:
    - path: "HabitsTracker/Features/Hub/HubView.swift"
      provides: "grid of focused domains, owns the @Query, empty state"
      contains: "isFocused"
    - path: "HabitsTracker/Features/Hub/DomainTile.swift"
      provides: "data-driven tile (name/iconName/colorToken props, no query)"
      contains: "DomainTile"
    - path: "HabitsTracker/Features/Hub/DomainDetailView.swift"
      provides: "domain header + non-empty-sections-only detail with empty state"
      contains: "DomainDetailView"
  key_links:
    - from: "HabitsTracker/Features/Hub/HubView.swift"
      to: "isFocused query + DomainDetailView"
      via: "@Query filtered on isFocused; navigation to detail"
      pattern: "DomainDetailView"
    - from: "HabitsTracker/Features/Hub/DomainTile.swift"
      to: "accentColor(forToken:)"
      via: "tile tint resolved from stored colorToken"
      pattern: "accentColor"
    - from: "HabitsTracker/Features/RootTabView.swift"
      to: "HubView"
      via: "Hub tab inserted between Today and Progress"
      pattern: "HubView()"
---

<objective>
Build the Hub surface (DOM-03): HubView owns a `@Query` filtered on `isFocused == true` and lays out focused domains as an adaptive LazyVGrid of data-driven DomainTiles; tapping a tile pushes DomainDetailView, which renders the domain header plus only non-empty sections (Phase 1 has no offshoot content, so it shows the designed empty state). Add the Hub tab to RootTabView between Today and Progress to reach the final 4-tab IA. Every data-driven view ships an empty state (§9.3).

Purpose: DOM-03 + completes DOM-06's 4-tab target. The grid uses accentColor(forToken:) (from 01-03) so each tile carries its domain's accent identity; tiles are data-driven (§9.2 — parent owns the query).
Output: Features/Hub/{HubView,DomainTile,DomainDetailView}.swift and the Hub tab wired into RootTabView.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-generalization-a/01-UI-SPEC.md
@.planning/phases/01-domain-generalization-a/01-RESEARCH.md

<interfaces>
Domain @Model (from 01-02): id, name, iconName, colorToken, sortIndex, isSeeded, seedVersion, isFocused: Bool, habits: [Habit].
Resolver (from 01-03): accentColor(forToken token: String, scheme: ColorScheme) -> Color in HabitsTracker/Utilities/AccentTokenColor.swift.
Theme access pattern (every view in this app):
  @EnvironmentObject themeManager: ThemeManager; @Environment(\.colorScheme) colorScheme; let theme = themeManager.theme(for: colorScheme)
DesignKit components available: DKCard(theme:), DKSectionHeader(_:subtitle:theme:), DKBadge(_:theme:), DKButton.
RootTabView.swift (after 01-04): TabView with Today, Progress, Settings tabItems + .tint(theme.colors.accentPrimary). THIS PLAN inserts a Hub tabItem (Label "Hub", SF Symbol "square.grid.2x2") between Today and Progress.

UI-SPEC copy (binding):
  Hub empty heading: "Your Hub is empty"
  Hub empty body: "Focus a domain to pin it here. Open the focus picker to choose what belongs in your Hub." + button "Choose Domains"
  DomainDetail empty heading: "Nothing here yet"
  DomainDetail empty body: "Rules, collections, clips and ideas you file under this domain will show up here."
Tokens (UI-SPEC): tiles use DKCard / theme.radii.card; grid inter-item spacing theme.spacing.m, screen padding theme.spacing.l; tile glyph tinted via accentColor(forToken:); name in theme.typography.headline; tap target >= 44pt. Accent reserved for the glyph/tile tint and detail header glyph ONLY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DomainTile (data-driven) + HubView grid with @Query(isFocused) and empty state</name>
  <read_first>
    - .planning/phases/01-domain-generalization-a/01-UI-SPEC.md (S1 — tile grid, tile visual, empty state, accessibility labels)
    - HabitsTracker/Features/Today/TodayView.swift (existing theme/@Query patterns to mirror)
    - HabitsTracker/Utilities/AccentTokenColor.swift (accentColor signature from 01-03)
  </read_first>
  <files>HabitsTracker/Features/Hub/DomainTile.swift, HabitsTracker/Features/Hub/HubView.swift</files>
  <action>
    DomainTile.swift: a data-driven view (§9.2) taking props only — `name: String`, `iconName: String`, `colorToken: String`, plus `theme: Theme` and `scheme: ColorScheme` (so it can call accentColor) — and NO @Query. Render a DKCard (theme.radii.card) containing a centered SF Symbol `Image(systemName: iconName)` tinted with `accentColor(forToken: colorToken, scheme: scheme)`, with the domain name below in `theme.typography.headline` (theme.colors.textPrimary), 2-line cap. Whole tile is a >= 44pt tap target (`.frame(minWidth:44,minHeight:44)`). Add accessibility: `.accessibilityElement(children: .combine)` + `.accessibilityLabel("\(name), domain")`.

    HubView.swift: owns `@Query(filter: #Predicate<Domain> { $0.isFocused }, sort: \Domain.sortIndex) private var focusedDomains: [Domain]` (parent owns the query, §9.2). Inside a `NavigationStack`, if `focusedDomains.isEmpty` render the empty state (heading "Your Hub is empty" in theme.typography.titleLarge, body copy in theme.typography.body, and a "Choose Domains" button that navigates to the focus picker — for this plan the button may navigate via a NavigationLink to `DomainFocusPicker` which lands in 01-06; if DomainFocusPicker does not yet exist at build time, wire the button to a placeholder destination and leave a TODO comment referencing 01-06 — do NOT leave the empty state without the heading/body copy). Otherwise render a `LazyVGrid(columns: [GridItem(.adaptive(minimum: ...))], spacing: theme.spacing.m)` of `DomainTile(...)` inside a ScrollView, screen padding theme.spacing.l; each tile wrapped so tapping pushes `DomainDetailView(domain:)`. navigationTitle "Hub". Background theme.colors.background.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && grep -q "isFocused" HabitsTracker/Features/Hub/HubView.swift && grep -q "accentColor" HabitsTracker/Features/Hub/DomainTile.swift && grep -q "Your Hub is empty" HabitsTracker/Features/Hub/HubView.swift && grep -q "DomainDetailView" HabitsTracker/Features/Hub/HubView.swift && ! grep -q "@Query" HabitsTracker/Features/Hub/DomainTile.swift</automated>
  </verify>
  <acceptance_criteria>
    - DomainTile.swift takes name/iconName/colorToken props, calls `accentColor(forToken:`, and contains no `@Query`.
    - HubView.swift owns a `@Query` filtered on `isFocused`, renders a LazyVGrid of DomainTile, and has the "Your Hub is empty" empty state with the "Choose Domains" button.
    - DomainTile has an accessibility label of the form "<name>, domain".
    - No hard-coded colors/spacing (uses theme tokens + accentColor only).
  </acceptance_criteria>
  <done>Hub renders focused domains as an accent-tinted grid with a designed empty state; tile is data-driven.</done>
</task>

<task type="auto">
  <name>Task 2: DomainDetailView (non-empty sections only + empty state) and insert the Hub tab</name>
  <read_first>
    - .planning/phases/01-domain-generalization-a/01-UI-SPEC.md (S2 — detail header, non-empty sections, empty state copy; accent reserved for header glyph)
    - HabitsTracker/Features/RootTabView.swift (after 01-04 — Today/Progress/Settings tabItems)
    - HabitsTracker/Utilities/AccentTokenColor.swift
  </read_first>
  <files>HabitsTracker/Features/Hub/DomainDetailView.swift, HabitsTracker/Features/RootTabView.swift</files>
  <action>
    DomainDetailView.swift: `struct DomainDetailView: View` taking `let domain: Domain` (data-driven; no query for its own identity). Render a header with the domain glyph `Image(systemName: domain.iconName)` accent-tinted via `accentColor(forToken: domain.colorToken, scheme:)` and the domain name in theme.typography.title. Below it, render ONLY non-empty sections using DKSectionHeader per section. Phase 1 has no offshoot item types yet, so in practice this is the header plus the designed empty state: heading "Nothing here yet" (theme.typography.title) and body "Rules, collections, clips and ideas you file under this domain will show up here." (theme.typography.body). Do NOT push its own NavigationStack — it is pushed onto HubView's stack. Background theme.colors.background. Add VoiceOver: the header glyph + name as a combined element.

    RootTabView.swift: insert a `HubView().tabItem { Label("Hub", systemImage: "square.grid.2x2") }` between the Today and Progress tabItems, yielding the final order Today, Hub, Progress, Settings (4 tabs). Keep `.tint(theme.colors.accentPrimary)`. Remove the placeholder comment left by 01-04.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && grep -q "let domain: Domain" HabitsTracker/Features/Hub/DomainDetailView.swift && grep -q "Nothing here yet" HabitsTracker/Features/Hub/DomainDetailView.swift && grep -q "HubView()" HabitsTracker/Features/RootTabView.swift && grep -c tabItem HabitsTracker/Features/RootTabView.swift | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - DomainDetailView.swift takes `let domain: Domain`, accent-tints the header glyph via accentColor, and contains the "Nothing here yet" empty state copy.
    - DomainDetailView does not declare its own NavigationStack.
    - RootTabView.swift renders `HubView()` in a Hub tabItem and has exactly 4 `tabItem` occurrences in order Today, Hub, Progress, Settings.
  </acceptance_criteria>
  <done>DomainDetailView shows the header + empty state with no nested stack; the Hub tab completes the 4-tab IA.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the Hub grid, detail navigation, and final 4-tab IA</name>
  <action>Build and run on iPhone 17 and visually confirm the Hub surface per <how-to-verify>: 4-tab IA (Today/Hub/Progress/Settings), accent-tinted focused-domain grid, empty state when none focused, and DomainDetailView opening with header + empty state and no nested nav bar.</action>
  <what-built>
    The Hub tab with a focused-domain grid (accent-tinted tiles via accentColor), DomainDetailView with header + empty state, and the completed Today/Hub/Progress/Settings 4-tab bar (DOM-03/DOM-06).
  </what-built>
  <how-to-verify>
    Build + run on iPhone 17.
    1. Tab bar shows exactly Today, Hub, Progress, Settings (4 tabs).
    2. Open Hub: focused domains appear as a grid of tiles, each glyph tinted with its domain's accent color (5 distinct accents observable across domains). On a fresh/curated install, confirm the merge-added domains (Style/Diet/Money/Media) are NOT shown until focused.
    3. If no domains are focused, the "Your Hub is empty" state shows with the "Choose Domains" button.
    4. Tap a tile: DomainDetailView opens with the accent-tinted header glyph + name and the "Nothing here yet" empty state, with a single nav bar (no doubled title).
    5. Today is still visually unchanged.
  </how-to-verify>
  <acceptance_criteria>
    - 4 tabs: Today, Hub, Progress, Settings.
    - Hub grid renders accent-tinted tiles for focused domains; empty state shows when none are focused.
    - DomainDetailView opens from a tile with header + empty state and no nested nav bar.
  </acceptance_criteria>
  <verify>
    <human-check>4 tabs (Today/Hub/Progress/Settings); Hub grid shows accent-tinted tiles or the empty state; tapping a tile opens DomainDetailView with header + empty state and one nav bar.</human-check>
  </verify>
  <done>Hub grid, accent tints, empty state, detail navigation, and the 4-tab IA all verified on device.</done>
  <resume-signal>Type "approved" if the Hub grid, detail navigation, accent tints, and 4-tab bar all check out, or describe the issue.</resume-signal>
</task>

</tasks>

<verification>
- Build: xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build.
- Manual: Hub grid + accent tints + detail empty state + 4-tab IA.
- Each new file under the ~400-line cap (§9.1); tiles data-driven (§9.2); empty states present (§9.3).
</verification>

<success_criteria>
- DOM-03: Hub shows focused domains as an icon+color grid; tile opens DomainDetailView with non-empty sections only (header + empty state in Phase 1).
- DOM-06: final tab bar is Today/Hub/Progress/Settings (4 tabs), Today unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-domain-generalization-a/01-05-SUMMARY.md` when done.
</output>
