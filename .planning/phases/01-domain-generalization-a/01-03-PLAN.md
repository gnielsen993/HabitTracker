---
phase: 01-domain-generalization-a
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - HabitsTracker/Services/SeedDataService.swift
  - HabitsTracker/Services/BootstrapService.swift
  - HabitsTracker/Utilities/AccentTokenColor.swift
autonomous: true
requirements: [DOM-04]
must_haves:
  truths:
    - "On upgrade, every pre-existing Domain becomes isFocused == true exactly once; a domain the user later unfocuses stays unfocused across launches"
    - "New hub seed domains (Style, Diet, Money, Media) are merge-added as isFocused == false and an existing 'Social' is not duplicated"
    - "On a fresh install, the opinionated subset seeds pre-focused and the new domains seed unfocused"
    - "accentColor(forToken:) maps each of the 5 tokens to a Color and falls back safely for unknown tokens"
  artifacts:
    - path: "HabitsTracker/Services/BootstrapService.swift"
      provides: "version-gated focus backfill + merge-add invocation + lastSeededVersion marker"
      contains: "lastSeededVersion"
    - path: "HabitsTracker/Utilities/AccentTokenColor.swift"
      provides: "token string to Color resolver over the 5 accents"
      contains: "accentColor"
  key_links:
    - from: "HabitsTracker/Services/BootstrapService.swift"
      to: "SeedDataService.restoreMissingDefaults"
      via: "bootstrapIfNeeded calls merge-add then backfill, gated on marker"
      pattern: "restoreMissingDefaults"
    - from: "HabitsTracker/Utilities/AccentTokenColor.swift"
      to: "DesignKit PresetCatalog"
      via: "token -> ThemePreset -> anchors.accent"
      pattern: "PresetCatalog"
---

<objective>
Resolve open question Q3: add a persisted `lastSeededVersion` marker and wire the once-only, version-gated focus backfill + merge-add of new hub seed domains into BootstrapService; bump SeedDataService.seedVersion 1→2 and add the new hub domains (Style, Diet, Money, Media) to the defaults seeded fresh-installs-pre-focused / merge-added-unfocused. Also build the app-level `accentColor(forToken:)` resolver (the Hub grid and the 5-swatch row both consume it).

Purpose: Upgraders open the Hub to their familiar domains already focused (familiarity over an empty curated screen, D-07), the merge-add introduces new domains without flooding (D-08), and the backfill is idempotent so deliberate unfocusing sticks (D-11/Pitfall 2). The token->Color resolver is the single place a stored token becomes a Color, kept out of DesignKit per §9.14.
Output: backfill + marker + merge-add in BootstrapService/SeedDataService, new defaults, and AccentTokenColor.swift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-domain-generalization-a/01-RESEARCH.md
@.planning/phases/01-domain-generalization-a/01-CONTEXT.md

<interfaces>
Post-01-02 state (Domain-named, compiling, seedVersion still 1):

HabitsTracker/Services/BootstrapService.swift:
  final class BootstrapService {
    private let seedDataService = SeedDataService()
    func bootstrapIfNeeded(context: ModelContext) throws {
      try seedDataService.seedIfNeeded(context: context)
      _ = try ensureDailyEntryExists(for: .now, context: context)
    }
  }

HabitsTracker/Services/SeedDataService.swift (after 01-02):
  private let seedVersion = 1   (this plan bumps to 2)
  func seedIfNeeded(context:)            // early-returns if any Domain exists; seeds defaults
  func restoreMissingDefaults(context:)  // name-keyed merge-add; dedupes "Social" by name
  private func defaultDomains() -> [Domain]   // (currently named defaultCategories; rename optional)

DesignKit accent source (verified):
  PresetCatalog.theme(for: ThemePreset).anchors(for: scheme).accent  -> Color
  ThemePreset has cases .forest, .navy, .maroon, .walnut, .stone matching the 5 tokens.
  Theme is the app theme struct; DesignKit ThemeColors exposes accentPrimary as the safe fallback.

Backfill design (Research Pattern 2 — implement this exact shape):
  let previous = UserDefaults.standard.integer(forKey: "lastSeededVersion")  // 0 for everyone today
  if previous < 2 {
    try seedDataService.restoreMissingDefaults(context: context)   // D-08 merge-add unfocused
    if previous > 0 { try backfillFocusOnExistingDomains(context: context) }  // D-07 flip existing -> focused
    UserDefaults.standard.set(2, forKey: "lastSeededVersion")      // once-only (D-11)
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump seedVersion, add new hub domains, and wire the version-gated focus backfill + merge-add in BootstrapService</name>
  <read_first>
    - HabitsTracker/Services/SeedDataService.swift (seedVersion constant, defaultDomains list, restoreMissingDefaults name-keyed dedupe)
    - HabitsTracker/Services/BootstrapService.swift (bootstrapIfNeeded ordering)
    - .planning/phases/01-domain-generalization-a/01-RESEARCH.md (Pattern 2 — previous>0 guard, idempotency; Pitfalls 2 and 3)
    - HabitsTrackerTests/BootstrapBackfillTests.swift (the four assertions this task must satisfy)
  </read_first>
  <behavior>
    - Test: BootstrapBackfillTests.testBackfillFlipsPreexistingDomainsWhenUpgrading — marker between 0 and 2, existing domains end isFocused == true.
    - Test: BootstrapBackfillTests.testBackfillRunsOnce — second run does not re-focus a manually-unfocused domain.
    - Test: BootstrapBackfillTests.testFreshInstallDoesNotBackfillExistingRows — marker 0, merge-added new domains stay isFocused == false.
    - Test: BootstrapBackfillTests.testMergeAddIsUnfocusedAndDedupesByName — a pre-existing "Social" is not duplicated; new domains are unfocused.
  </behavior>
  <files>HabitsTracker/Services/SeedDataService.swift, HabitsTracker/Services/BootstrapService.swift</files>
  <action>
    SeedDataService.swift: bump `private let seedVersion = 1` to `= 2` (D-06). Add the new hub seed domains to the defaults list (D-08) — append Domain entries for "Style", "Diet", "Money", "Media" with curated SF Symbol iconNames consistent with the existing seeded glyphs (e.g. tshirt / fork.knife / banknote / play.rectangle — finalize from the curated set; Claude's discretion) and colorTokens drawn ONLY from the 5 accent tokens, with sortIndex values continuing after the existing 12. In `seedIfNeeded` (fresh-install path, D-09): seed the opinionated subset with `isFocused: true` and seed the newly added hub domains with `isFocused: false` (so a fresh curated install is not flooded — Pitfall 3). The existing 12 carried-over defaults keep `isFocused: true` on fresh install (they ARE the opinionated subset). `restoreMissingDefaults` stays name-keyed and inserts any missing default as `isFocused: false` (merge-add unfocused) — the existing "Social" dedupes by name automatically.

    BootstrapService.swift: rewrite `bootstrapIfNeeded` to implement Research Pattern 2 exactly. Read `UserDefaults.standard.integer(forKey: "lastSeededVersion")` into `previous`. Call `seedIfNeeded` first (fresh-install path). Then, gated `if previous < 2`: call `seedDataService.restoreMissingDefaults(context:)` (merge-add new domains, D-08); then `if previous > 0` call a new private method `backfillFocusOnExistingDomains(context:)` that fetches all Domains and sets `isFocused = true` on each, then saves (D-07 — flip pre-existing rows; the previous>0 guard ensures fresh installs are skipped, Pitfall 3). After the gated block, `UserDefaults.standard.set(2, forKey: "lastSeededVersion")` (once-only, D-11). Keep the existing `ensureDailyEntryExists(for: .now, context:)` call last. Inject the UserDefaults instance via an initializer parameter defaulting to `.standard` so BootstrapBackfillTests can pass an isolated suite. Use `os.Logger` for any diagnostics — never print().
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && grep -q "lastSeededVersion" HabitsTracker/Services/BootstrapService.swift && grep -q "restoreMissingDefaults" HabitsTracker/Services/BootstrapService.swift && grep -q "seedVersion = 2" HabitsTracker/Services/SeedDataService.swift && grep -q '"Style"' HabitsTracker/Services/SeedDataService.swift && ! grep -q "print(" HabitsTracker/Services/BootstrapService.swift</automated>
  </verify>
  <acceptance_criteria>
    - SeedDataService.swift contains `seedVersion = 2` and default domains for "Style", "Diet", "Money", "Media" with colorTokens in {forest,navy,maroon,walnut,stone}.
    - BootstrapService.swift reads and writes `lastSeededVersion`, calls `restoreMissingDefaults`, and only flips existing rows when `previous > 0`.
    - BootstrapService initializer accepts an injectable UserDefaults (default `.standard`).
    - No `print(` in BootstrapService.swift; logging uses os.Logger if present.
    - The four BootstrapBackfillTests assertions pass: xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:HabitsTrackerTests/BootstrapBackfillTests test -quiet.
  </acceptance_criteria>
  <done>Version-gated, idempotent focus backfill + merge-add wired; new hub domains seeded; BootstrapBackfillTests green.</done>
</task>

<task type="auto">
  <name>Task 2: App-level accentColor(forToken:) resolver over the 5 accent tokens</name>
  <read_first>
    - .planning/phases/01-domain-generalization-a/01-RESEARCH.md (Pattern 4 + A2 — PresetCatalog.theme(for:).anchors(for:).accent source)
    - .planning/phases/01-domain-generalization-a/01-UI-SPEC.md (Color section — resolver contract, fallback, valid-by-construction)
    - HabitsTracker/Features/Settings/ThemeManager.swift (how the app obtains the active Theme / colorScheme)
  </read_first>
  <files>HabitsTracker/Utilities/AccentTokenColor.swift</files>
  <action>
    Create HabitsTracker/Utilities/AccentTokenColor.swift (new app-level utility, NOT in DesignKit — §9.14). Provide a free function `func accentColor(forToken token: String, scheme: ColorScheme) -> Color` (or an extension on Theme — match how callers in 01-05/01-06 obtain colorScheme; prefer a signature that takes the scheme since the swatch source is scheme-dependent). Map the 5 token strings to DesignKit accent colors by resolving each token to its matching `ThemePreset` case and reading `PresetCatalog.theme(for: preset).anchors(for: scheme).accent`: "forest"->.forest, "navy"->.navy, "maroon" (alias "oxblood")->.maroon, "walnut"->.walnut, "stone"->.stone. For any unknown/legacy token, return a safe fallback (`theme.colors.accentPrimary` or the forest accent) — never crash, never go off-palette (UI-SPEC resolver contract). Confirm the exact public accessor at build time per Research A2; if `anchors(for:).accent` is not public, fall back to `PresetTheme.swatch(for:)` whose first element is the accent. Keep the file small and dependency-light (import SwiftUI + DesignKit only).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && grep -q "accentColor" HabitsTracker/Utilities/AccentTokenColor.swift && grep -q "PresetCatalog\|swatch" HabitsTracker/Utilities/AccentTokenColor.swift && grep -q "forest" HabitsTracker/Utilities/AccentTokenColor.swift && grep -q "stone" HabitsTracker/Utilities/AccentTokenColor.swift</automated>
  </verify>
  <acceptance_criteria>
    - HabitsTracker/Utilities/AccentTokenColor.swift defines an `accentColor(forToken:...)` function returning a `Color`.
    - It handles all 5 tokens (forest, navy, maroon/oxblood, walnut, stone) and a safe fallback for unknown tokens.
    - It sources colors from DesignKit's PresetCatalog/PresetTheme (no hard-coded Color(hex:) / Color(red:)).
    - File lives under HabitsTracker/Utilities/ (app target), not in DesignKit.
  </acceptance_criteria>
  <done>The single token-to-Color resolver exists, covers the 5 tokens with a safe fallback, and sources colors from DesignKit.</done>
</task>

</tasks>

<verification>
- Quick: xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:HabitsTrackerTests/BootstrapBackfillTests test -quiet (green).
- Full suite green after this plan.
- No hard-coded colors introduced (grep for Color(hex / Color(red returns nothing in AccentTokenColor.swift).
</verification>

<success_criteria>
- DOM-04 mechanics: upgraders see their existing domains focused once (idempotent); new domains merge-added unfocused; "Social" not duplicated.
- Fresh install: opinionated subset pre-focused, new domains unfocused.
- accentColor(forToken:) resolves the 5 tokens with a safe fallback, app-level only.
</success_criteria>

<output>
Create `.planning/phases/01-domain-generalization-a/01-03-SUMMARY.md` when done.
</output>
