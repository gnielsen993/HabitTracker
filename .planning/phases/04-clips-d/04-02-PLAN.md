---
phase: 04-clips-d
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - HabitsTracker/Utilities/ClipTitleSuggestion.swift
  - HabitsTrackerTests/ClipTitleSuggestionTests.swift
autonomous: true
requirements: [CLIP-01, CLIP-02]
decisions: [D-01, D-02]
must_haves:
  truths:
    - "ClipTitleSuggestion is a pure enum namespace with a single nonisolated static func suggest(from urlString: String) -> String, no SwiftData/model/network dependency (mirrors CollectionRollupEngine's pure-enum shape)"
    - "The parser is ZERO-NETWORK — it uses only URLComponents/String work, never URLSession/dataTask/any fetch (SC1 / D-01 offline gate)"
    - "A normal URL (https://tiktok.com/@user/video/123) yields a non-empty readable suggestion derived from host and/or last path slug (D-02)"
    - "A bare-host URL (https://example.com) yields the host-derived suggestion; a URL with a slug yields a humanized slug; a malformed/no-scheme string yields a graceful fallback (empty or the raw trimmed input) without crashing"
    - "ClipTitleSuggestionTests is a pure-function test suite (no ModelContainer) that RUNS AND PASSES on this simulator (§9.7), covering the 4 mandated cases"
    - "Traceability: implements CONTEXT decisions D-01, D-02 (see 04-CONTEXT.md)"
  artifacts:
    - path: "HabitsTracker/Utilities/ClipTitleSuggestion.swift"
      provides: "pure zero-network URL→title suggestion helper"
      contains: "enum ClipTitleSuggestion"
    - path: "HabitsTrackerTests/ClipTitleSuggestionTests.swift"
      provides: "runnable pure-function tests for the 4 mandated cases"
      contains: "final class ClipTitleSuggestionTests"
  key_links:
    - from: "HabitsTracker/Utilities/ClipTitleSuggestion.swift"
      to: "Foundation.URLComponents"
      via: "pure string parse, no network"
      pattern: "URLComponents"
    - from: "HabitsTrackerTests/ClipTitleSuggestionTests.swift"
      to: "ClipTitleSuggestion"
      via: "static suggest(from:) assertions"
      pattern: "ClipTitleSuggestion.suggest"
---

<objective>
Ship the D-02 zero-network title-suggestion helper: a pure function that derives an editable *suggested* clip title from a URL string using only `URLComponents`/string work — never a network fetch. This is the friction-reducing core of the Clips capture flow that stays fully inside the offline-only gate (SC1 / D-01), and it is a §9.5 pure helper so it ships with runnable unit tests in the same plan/commit.

Purpose: CLIP-01's recorded offline decision (D-01/D-02) is embodied here — a title suggestion with NO network. CLIP-02's editor consumes this to prefill the Title field. The parser is the one piece of genuinely testable logic in the phase and it RUNS on this simulator (unlike the @Model tests).
Output: One new pure utility + a runnable test suite covering the 4 mandated cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-clips-d/04-CONTEXT.md
@.planning/phases/04-clips-d/04-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase — use directly, no exploration needed. -->

Pure-enum service shape to mirror (HabitsTracker/Services/CollectionRollupEngine.swift):
```swift
import Foundation

enum CollectionRollupEngine {
    nonisolated static func rollup(collection: Collection, items: [CollectionItem]) -> Result { ... }
}
```

Pure-function test shape to mirror (HabitsTrackerTests/CollectionRollupEngineTests.swift — no ModelContainer, always runnable per §9.7):
```swift
import XCTest
@testable import HabitsTracker

final class CollectionRollupEngineTests: XCTestCase {
    func testCompletionistHappyPath() {
        let result = CollectionRollupEngine.rollup(collection: ..., items: [...])
        XCTAssertEqual(result, .count(x: 2, y: 5))
    }
}
```

Existing pure-transform utility neighbour (HabitsTracker/Utilities/AccentTokenColor.swift) — confirms `Utilities/` is the right home for a model-free pure transform.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing ClipTitleSuggestionTests for the 4 mandated cases (RED)</name>
  <files>HabitsTrackerTests/ClipTitleSuggestionTests.swift</files>
  <read_first>
    - HabitsTrackerTests/CollectionRollupEngineTests.swift (pure-function XCTest shape — no ModelContainer, no @MainActor needed; the assertion style to mirror)
    - .planning/phases/04-clips-d/04-CONTEXT.md decision D-02 (the 4 required cases: normal URL, bare-host URL, URL with a slug, malformed/no-scheme fallback) and Claude's Discretion (exact host-vs-slug-vs-both humanization rule)
    - .planning/phases/04-clips-d/04-PATTERNS.md section "HabitsTrackerTests/ClipTitleSuggestionTests.swift"
    - CLAUDE.md §9.7 (this pure suite RUNS and must be run — it creates no ModelContainer) and §9.5 (pure helpers ship with tests in the same commit)
  </read_first>
  <behavior>
    - testBareHost: `suggest(from: "https://example.com")` returns a non-empty string containing the host stem (e.g. "example.com" or "Example") — assert non-empty AND that it contains "example" (case-insensitive), not a hard-coded full string, so the executor keeps discretion over exact humanization.
    - testUrlWithSlug: `suggest(from: "https://www.nytimes.com/2026/01/how-to-make-sourdough")` returns a humanized slug (e.g. "How To Make Sourdough" or "how to make sourdough") — assert it contains "sourdough" (case-insensitive) and contains no hyphen ("-" replaced by spaces).
    - testNormalUrlNonEmpty: `suggest(from: "https://tiktok.com/@user/video/123")` returns a non-empty readable suggestion (host and/or slug) — assert non-empty.
    - testMalformedFallback: `suggest(from: "not a url ::: %%%")` and `suggest(from: "")` return a graceful fallback (empty string OR the trimmed raw input) WITHOUT crashing — assert the call returns (no trap) and equals the documented fallback the executor chooses (assert it does not throw and, for "", returns "").
  </behavior>
  <action>
    Create `ClipTitleSuggestionTests.swift` mirroring `CollectionRollupEngineTests` (plain `XCTestCase`, no ModelContainer, no `@MainActor`). Write the four tests above against `ClipTitleSuggestion.suggest(from:)`. Write assertions in terms of INVARIANTS (non-empty, contains-substring case-insensitive, no-hyphen, no-crash) rather than exact full strings, so the helper's precise humanization rule stays Claude's Discretion (D-02) while the contract stays enforced. This task lands the tests RED — `ClipTitleSuggestion` does not exist yet, so the test target will not compile; that is the expected RED state, resolved in Task 2.
  </action>
  <verify>
    <automated>test -f HabitsTracker/Utilities/ClipTitleSuggestion.swift || echo "RED: helper absent as expected"; grep -c "func testMalformedFallback" HabitsTrackerTests/ClipTitleSuggestionTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `ClipTitleSuggestionTests.swift` contains `final class ClipTitleSuggestionTests`, `testBareHost`, `testUrlWithSlug`, `testNormalUrlNonEmpty`, and `testMalformedFallback` (grep count for `func test` >= 4).
    - The suite references `ClipTitleSuggestion.suggest(from:)` and imports `@testable import HabitsTracker`; it creates NO `ModelContainer`/`ModelContext` (grep for `ModelContainer` returns 0).
    - Assertions use invariants (`XCTAssertFalse(... .isEmpty)`, `XCTAssertTrue(... .contains(...))`), not exact full-string equality on the humanized output.
  </acceptance_criteria>
  <done>The four mandated test cases exist as invariant-based assertions against a not-yet-existing helper (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ClipTitleSuggestion pure helper to green (GREEN)</name>
  <files>HabitsTracker/Utilities/ClipTitleSuggestion.swift</files>
  <read_first>
    - HabitsTracker/Services/CollectionRollupEngine.swift (the `enum Namespace { nonisolated static func ... }` pure-shape to mirror)
    - HabitsTracker/Utilities/AccentTokenColor.swift (confirms Utilities/ conventions for a pure model-free transform)
    - HabitsTrackerTests/ClipTitleSuggestionTests.swift (self — the contract just authored in Task 1)
    - .planning/phases/04-clips-d/04-CONTEXT.md D-02 + Claude's Discretion (host vs slug vs both, humanization) and D-01 (ZERO network, ever)
  </read_first>
  <behavior>
    - Same four behaviors as Task 1 — Task 2 makes them pass with a real implementation.
  </behavior>
  <action>
    Create `ClipTitleSuggestion.swift`: `import Foundation` + `enum ClipTitleSuggestion { nonisolated static func suggest(from urlString: String) -> String { ... } }`. Implementation (Claude's Discretion within the pure/zero-network constraint): trim the input; parse with `URLComponents` (prepend `https://` transiently only for parsing IF no scheme, so a bare `example.com` still resolves a host — this normalization is parse-local and never mutates stored data). Prefer the last non-empty path component (slug) when present and meaningful: strip a trailing file extension, replace `-`/`_` with spaces, collapse whitespace, and capitalize words for readability. Fall back to the host (optionally dropping a leading `www.`) when there is no useful slug. On a string that yields neither a host nor a slug (malformed/no-scheme garbage), return the documented graceful fallback (return `""` for empty/parse-failure — the editor then just leaves the Title field empty for the user to fill). ABSOLUTELY no `URLSession`, no `dataTask`, no `.load`, no network of any kind (D-01, SC1). Use `os.Logger` if any diagnostic is needed, never `print()` (§9.13) — though a pure transform should need none.
  </action>
  <verify>
    <automated>xcrun simctl shutdown all 2>/dev/null; xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet test -only-testing:HabitsTrackerTests/ClipTitleSuggestionTests -parallel-testing-enabled NO</automated>
  </verify>
  <acceptance_criteria>
    - `ClipTitleSuggestion.swift` contains `enum ClipTitleSuggestion` and `nonisolated static func suggest(from`.
    - The file uses `URLComponents` and contains NO network APIs: `grep -E "URLSession|dataTask|\.load\(|URLRequest" HabitsTracker/Utilities/ClipTitleSuggestion.swift` returns 0 matches (SC1 / D-01 offline gate).
    - No `print(` in the file (`grep -c 'print(' HabitsTracker/Utilities/ClipTitleSuggestion.swift` returns 0, §9.13).
    - The `-only-testing:HabitsTrackerTests/ClipTitleSuggestionTests` run EXITS 0 with all 4 tests passing (this is a pure suite that runs on this simulator per §9.7 — it must actually run green, not build-verify). If the host aborts on launch, clean the sim (`xcrun simctl shutdown all && xcrun simctl erase <device-id>`, §9.7) and retry — the pure suite itself creates no ModelContainer.
  </acceptance_criteria>
  <done>The pure zero-network suggestion helper exists and the 4 mandated cases pass green on this simulator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-entered URL string → ClipTitleSuggestion.suggest(from:) | Arbitrary/malformed user text; a parse crash or force-unwrap would take down the editor keystroke path |
| offline gate (SC1 / D-01) | The helper must never reach the network — a title suggestion is the tempting place to sneak a fetch; it must stay pure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | malformed/garbage URL string parsed by suggest(from:) | mitigate | Pure `URLComponents`/optional-chaining parse with a graceful `""` fallback and no force-unwraps; testMalformedFallback + empty-string case assert no crash |
| T-04-04 | Information Disclosure / offline-gate violation | any accidental network fetch from the suggestion path | mitigate | Implementation is pure string work only; acceptance grep asserts zero `URLSession`/`dataTask`/`.load`/`URLRequest` in the file (SC1 / D-01, constitution offline-only) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Foundation + XCTest only; no new third-party dependency |
</threat_model>

<verification>
- ClipTitleSuggestionTests runs green on iPhone 17 (pure suite, §9.7) covering all 4 mandated cases.
- The helper contains zero network APIs (grep == 0) — the offline gate holds.
- The helper is a pure enum namespace with a single nonisolated static func, no model/SwiftData dependency.
</verification>

<success_criteria>
- A zero-network, unit-tested title-suggestion helper exists (CLIP-01 offline decision embodied, D-02).
- The 4 mandated cases pass green on this simulator (not build-verify only).
</success_criteria>

<output>
Create `.planning/phases/04-clips-d/04-02-SUMMARY.md` when done.
</output>
