---
phase: 04-clips-d
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - HabitsTracker/Services/ExportImportService.swift
  - HabitsTracker/Features/Settings/SettingsView.swift
  - HabitsTrackerTests/ExportImportTests.swift
autonomous: true
requirements: [CLIP-02]
decisions: [D-03, D-13]
must_haves:
  truths:
    - "ExportImportService.schemaVersion is bumped 4 -> 5; HabitExportBundle gains clips: [ClipDTO]"
    - "ClipDTO is a scalar-only Codable { id, title, url, note?, tag?, status: String, isArchived, createdAt, domainID: UUID? } — status carried as the raw String (D-03/D-13)"
    - "exportData gains a clips: [Clip] parameter and maps each Clip into a ClipDTO (status: $0.statusRaw, domainID: $0.domain?.id)"
    - "importReplace reconstructs each Clip wired to categoryIndex[dto.domainID], defensively reading ClipStatus(rawValue: dto.status) ?? .saved; deleteAll deletes Clip.self BEFORE Domain.self (nullify ownership order)"
    - "The SettingsView export call site adds a @Query for clips and passes clips: clips, and compiles"
    - "An ExportImportTests v5 round-trip proves a Clip survives with title/url/note/tag/status/isArchived and its domain wiring intact (build-verify only per §9.7)"
    - "Traceability: implements CONTEXT decisions D-03, D-13 (see 04-CONTEXT.md)"
  artifacts:
    - path: "HabitsTracker/Services/ExportImportService.swift"
      provides: "schemaVersion 5 + ClipDTO round-trip + deleteAll ordering"
      contains: "struct ClipDTO"
    - path: "HabitsTracker/Features/Settings/SettingsView.swift"
      provides: "export call site passing clips"
      contains: "clips: clips"
    - path: "HabitsTrackerTests/ExportImportTests.swift"
      provides: "schemaVersion-5 round-trip test covering Clip"
      contains: "ClipDTO"
  key_links:
    - from: "HabitsTracker/Services/ExportImportService.swift"
      to: "Clip"
      via: "importReplace wires clip.domain via categoryIndex[dto.domainID]"
      pattern: "categoryIndex\\[.*domainID"
    - from: "HabitsTracker/Features/Settings/SettingsView.swift"
      to: "ExportImportService"
      via: "exportData call passing clips"
      pattern: "clips: clips"
    - from: "HabitsTracker/Services/ExportImportService.swift"
      to: "ClipStatus"
      via: "defensive rawValue reconstruction on import"
      pattern: "ClipStatus(rawValue:"
---

<objective>
Extend the Export/Import round-trip to `schemaVersion` 5 covering `Clip` (D-13), mirroring the existing `Rule`/`RuleDTO` block exactly. Full multi-type export completeness stays Phase 6; Phase 4 keeps the round-trip green for the one type it adds, satisfying the baseline DoD ("export/import round-trip green for any type touched").

Purpose: CLIP-02's clip data must survive a backup/restore. The clip's `status` serializes as its raw String (D-03) so it round-trips as more than a bool.
Output: Edited ExportImportService (schemaVersion 5 + ClipDTO + deleteAll ordering) + SettingsView call site + a v5 round-trip test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-clips-d/04-CONTEXT.md
@.planning/phases/04-clips-d/04-PATTERNS.md
@.planning/phases/04-clips-d/04-01-SUMMARY.md

<interfaces>
<!-- From 04-01 (Clip) + the codebase — use directly. -->

Clip (from 04-01): `init(id:, title:, url:, note:, tag:, status: ClipStatus = .saved, isArchived:, createdAt:, domain:)`; stored `var statusRaw: String`; computed `var status: ClipStatus`; `enum ClipStatus: String { case saved, acted }`.

ExportImportService (HabitsTracker/Services/ExportImportService.swift):
- `private let schemaVersion = 4` (line 98) → bump to 5.
- `struct HabitExportBundle { schemaVersion; ...; rules: [RuleDTO]; collections: [CollectionDTO]; collectionItems: [CollectionItemDTO] }` (line 4-13) → add `clips: [ClipDTO]`.
- `struct RuleDTO { id; title; body; sourceURL?; isArchived; createdAt; domainID: UUID? }` (line 26-34) — the scalar-DTO shape to mirror for ClipDTO.
- `func exportData(...)` (line 114) — extend signature with `clips: [Clip]`, add the `.map { ClipDTO(...) }`.
- `importReplace` (line 219): builds `categoryIndex [UUID: Domain]` (line 228), then `ruleIndex` (line 245) with `domain: dto.domainID.flatMap { categoryIndex[$0] }` (line 252) — mirror this exact wiring for a clips loop (no index map needed; nothing references clips by id).
- `deleteAll` (line 350): order today HabitState → DailyEntry → Habit → Rule → CollectionItem → Collection → Domain. Insert `Clip.self` before `Domain.self` (Clip.domain is .nullify, so it must go before Domain).
- `guard bundle.schemaVersion == schemaVersion` (line 221) — now gates 5.

SettingsView call site (HabitsTracker/Features/Settings/SettingsView.swift): the existing `exportData(categories:habits:entries:rules:collections:collectionItems:)` call — add a `@Query private var clips: [Clip]` and pass `clips: clips`.

ExportImportTests (HabitsTrackerTests/ExportImportTests.swift): `makeInMemoryContext()` Schema must add `Clip.self`; the existing v4 round-trip test is the template to extend to v5. NOTE §9.7: this is a SwiftData @Model persistence suite — it crashes the XCTest host at 0.000s on this simulator, so it is BUILD-VERIFY ONLY here (compile it; execute on device).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: schemaVersion 4->5 + ClipDTO round-trip + deleteAll ordering</name>
  <files>HabitsTracker/Services/ExportImportService.swift</files>
  <read_first>
    - HabitsTracker/Services/ExportImportService.swift (self — the RuleDTO scalar-DTO shape, exportData mapping, importReplace ruleIndex→domain wiring, deleteAll ordering, the schemaVersion guard)
    - .planning/phases/04-clips-d/04-PATTERNS.md section "ExportImportService.swift" (the ClipDTO field list, the map, the import loop, the deleteAll insertion point, the raw-status defensive fallback)
    - .planning/phases/04-clips-d/04-CONTEXT.md D-03 (status serialized as raw String) + D-13 (schemaVersion 4→5, round-trip covers Clip)
    - CLAUDE.md §9.16 (do not hand-edit frozen snapshots — none here; ExportImportService is normal source)
  </read_first>
  <behavior>
    - Exporting one Clip (status .acted, note+tag set, filed under a Domain) and re-importing yields exactly one Clip with title/url/note/tag/isArchived preserved, status == .acted, and clip.domain?.id == the domain's id.
    - A ClipDTO whose status raw String is unknown/garbage imports as .saved (defensive fallback), never crashes.
    - Importing a bundle whose schemaVersion != 5 throws ImportError.unsupportedSchema (guard unchanged, now gates 5).
    - deleteAll removes all Clips before Domains so no orphaned relationship state survives a replace-import.
  </behavior>
  <action>
    Edit `ExportImportService.swift`:
    1. Bump `private let schemaVersion = 4` → `5` (D-13).
    2. Add `struct ClipDTO: Codable { let id: UUID; let title: String; let url: String; let note: String?; let tag: String?; let status: String; let isArchived: Bool; let createdAt: Date; let domainID: UUID? }` — scalars only, `status` as raw String (D-03), the only FK id is `domainID`.
    3. Add `let clips: [ClipDTO]` to `HabitExportBundle`.
    4. Extend `exportData(...)`'s signature with `clips: [Clip]` (alongside `rules:`/`collections:`/`collectionItems:`) and add `clips: clips.map { ClipDTO(id: $0.id, title: $0.title, url: $0.url, note: $0.note, tag: $0.tag, status: $0.statusRaw, isArchived: $0.isArchived, createdAt: $0.createdAt, domainID: $0.domain?.id) }` into the bundle construction.
    5. In `importReplace`, after the rules block, add a `for dto in bundle.clips` loop inserting `Clip(id: dto.id, title: dto.title, url: dto.url, note: dto.note, tag: dto.tag, status: ClipStatus(rawValue: dto.status) ?? .saved, isArchived: dto.isArchived, createdAt: dto.createdAt, domain: dto.domainID.flatMap { categoryIndex[$0] })` then `context.insert(clip)`. No index map needed (nothing references clips by id). The defensive `?? .saved` mirrors the `HabitScheduleType(rawValue:) ?? .daily` idiom.
    6. Extend `deleteAll` to `try context.delete(model: Clip.self)` placed BEFORE `try context.delete(model: Domain.self)` (Clip.domain is .nullify — must delete clips before their domain; position relative to Rule/Collection does not matter).
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `ExportImportService.swift` contains `schemaVersion = 5`, `struct ClipDTO`, `status: $0.statusRaw` (export map), `ClipStatus(rawValue: dto.status) ?? .saved` (import), and `let clips: [ClipDTO]` in `HabitExportBundle`.
    - `context.delete(model: Clip.self)` appears BEFORE `context.delete(model: Domain.self)` in `deleteAll`.
    - The import clip loop wires the domain via `dto.domainID.flatMap { categoryIndex[$0] }` (never force-unwrap).
    - Build exits 0.
  </acceptance_criteria>
  <done>schemaVersion is 5, ClipDTO round-trips all Clip scalars + the domain FK with a defensive status fallback, and deleteAll respects nullify ownership order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the SettingsView export call site + v5 round-trip test</name>
  <files>HabitsTracker/Features/Settings/SettingsView.swift, HabitsTrackerTests/ExportImportTests.swift</files>
  <read_first>
    - HabitsTracker/Features/Settings/SettingsView.swift (self — the existing @Query set for rules/collections and the exportData(...) call site to extend)
    - HabitsTrackerTests/ExportImportTests.swift (self — makeInMemoryContext() Schema + the existing v4 round-trip test to adapt to v5)
    - .planning/phases/04-clips-d/04-PATTERNS.md section "SettingsView.swift" + section "ExportImportTests" (the round-trip test shape; §9.7 build-verify-only note)
    - CLAUDE.md §9.7 (ExportImportTests is a SwiftData @Model persistence suite — build-verify only on this simulator)
  </read_first>
  <behavior>
    - The Settings export button compiles against the new `clips:` parameter and includes the user's clips in the exported bundle.
    - testExportImportRoundTripV5 builds a Domain + a Clip (status .acted, note "read later", tag "recipe", url set), exports at v5, imports into a fresh in-memory context, and asserts: 1 Clip; title/url/note/tag preserved; status == .acted; clip.domain?.id == the domain id.
  </behavior>
  <action>
    Edit `SettingsView.swift`: add `@Query private var clips: [Clip]` (mirroring the existing `@Query` for rules/collections) and pass `clips: clips` into the `exportData(...)` call alongside the existing arguments. Update any existing v4 round-trip call in this file's flow if the signature changed (compile-driven).

    Edit `ExportImportTests.swift`: add `Clip.self` to the `makeInMemoryContext()` `Schema([...])` array. Update the existing v4 export/round-trip test call to the v5 signature (pass `clips: []` where clips are not under test) so it still compiles. Add `testExportImportRoundTripV5` that: inserts a Domain and a `Clip(title: "Sourdough", url: "https://example.com/sourdough", note: "read later", tag: "recipe", status: .acted, domain: domain)`, calls `exportData(... clips: [clip] ...)`, imports the Data into a fresh in-memory context, fetches `Clip`, and asserts count == 1, `first?.status == .acted`, `first?.tag == "recipe"`, `first?.note == "read later"`, `first?.url == "https://example.com/sourdough"`, and `first?.domain?.id == domain.id` (domain wiring survived). Per §9.7 this suite is BUILD-VERIFY ONLY here — it crashes the XCTest host at 0.000s on this simulator; author it correctly and confirm the test target COMPILES; it is actually executed on device via the 04-05 owner checkpoint (export → wipe → import).
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build-for-testing</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsView.swift` contains `@Query private var clips: [Clip]` and `clips: clips` in the exportData call.
    - `ExportImportTests.swift` contains `testExportImportRoundTripV5` and its Schema array lists `Clip.self`.
    - `build-for-testing` exits 0 (the test target compiles). The round-trip is NOT executed here per §9.7 (build-verify only); it is exercised on device via the 04-05 export/import checkpoint.
  </acceptance_criteria>
  <done>The Settings export includes clips and a v5 round-trip test is authored proving a Clip survives with status/tag/note/domain intact — execution deferred to device per §9.7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported backup JSON → ExportImportService.importReplace | Untrusted external file; schemaVersion mismatch, malformed payload, an unknown status raw String, or a dangling domainID must fail gracefully or default, never crash or silently corrupt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering / Spoofing | import of a backup with a wrong schemaVersion | mitigate | `guard bundle.schemaVersion == schemaVersion (5) else throw ImportError.unsupportedSchema` — unchanged guard rejects mismatched payloads at the boundary with a user-facing error (CLAUDE.md §4) |
| T-04-09 | Denial (data integrity) | dangling clip domainID or garbage status raw on import; deleteAll ordering | mitigate | `domainID.flatMap { categoryIndex[$0] }` resolves orphans to nil (never force-unwrap); `ClipStatus(rawValue:) ?? .saved` defends unknown status; deleteAll deletes Clip before Domain to avoid orphaned nullify state |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — Foundation + SwiftData + XCTest only; no new third-party dependency |
</threat_model>

<verification>
- App builds clean; the test target compiles (`build-for-testing` exits 0).
- schemaVersion == 5; deleteAll deletes Clip before Domain.
- ClipDTO round-trips title/url/note/tag/status(raw)/isArchived + domain FK; status defends unknown raw as .saved.
- SettingsView export compiles against the extended signature and includes clips.
</verification>

<success_criteria>
- Clip round-trips green at schemaVersion 5 (baseline DoD for the touched type, D-13).
- The Settings export includes clips; import defends malformed status/orphan domain refs.
</success_criteria>

<output>
Create `.planning/phases/04-clips-d/04-04-SUMMARY.md` when done.
</output>
