---
phase: 04-clips-d
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified:
  - HabitsTracker/Features/Hub/DomainDetailView.swift
autonomous: false
requirements: [CLIP-03]
decisions: [D-08, D-10]
must_haves:
  truths:
    - "DomainDetailView gains a Clips section, appended in nonEmptySections at the 'Phase D–E: append Clips / Ideas sections here' hook (~line 81) via a buildClipsSection/clipsSectionContent/clipsSectionHeader trio mirroring the Rules trio (D-10)"
    - "buildClipsSection returns nil unless the domain has ≥1 non-archived clip — it filters domain.clips on !isArchived and sorts by createdAt descending (recency-first, matches RuleRow), preserving the DOM-03 'only non-empty sections' contract (D-10)"
    - "The section header 'Clips' has a trailing '+' button (≥44pt, accentPrimary, accessibilityLabel 'Add clip to \\(domain.name)') presenting ClipEditorView(domain:) via a $creatingClip sheet"
    - "Each row is a NavigationLink { ClipDetailView(clip:) } label: { ClipRow(clip:) } .buttonStyle(.plain), matching the Rules/Collections row wiring"
    - "Owner visual verification (device) confirms the full Clips flow: create → row with status chip → detail Open Link (opens Safari, no fetch) → toggle saved↔acted → edit → delete-confirm → export/import round-trip → section hides when empty → offline (airplane mode) works"
    - "Traceability: implements CONTEXT decisions D-08, D-10 (see 04-CONTEXT.md)"
  artifacts:
    - path: "HabitsTracker/Features/Hub/DomainDetailView.swift"
      provides: "Clips section wired into the domain nav + creatingClip sheet"
      contains: "buildClipsSection"
  key_links:
    - from: "HabitsTracker/Features/Hub/DomainDetailView.swift"
      to: "ClipRow / ClipDetailView"
      via: "NavigationLink { ClipDetailView } label: { ClipRow } in the clips section"
      pattern: "ClipDetailView\\(clip:"
    - from: "HabitsTracker/Features/Hub/DomainDetailView.swift"
      to: "ClipEditorView"
      via: "creatingClip sheet presenting ClipEditorView(domain:)"
      pattern: "ClipEditorView\\(domain:"
    - from: "HabitsTracker/Features/Hub/DomainDetailView.swift"
      to: "Clip"
      via: "buildClipsSection filters non-archived clips, createdAt descending"
      pattern: "!\\$0.isArchived"
---

<objective>
Wire the three Clip surfaces (built in 04-03) into `DomainDetailView` so a clip is filed by domain and found in that domain's Clips section (CLIP-03) — the final connective step that makes Clips reachable — then run the blocking owner visual verification for the whole Clips flow (including the export/import round-trip and the offline gate that XCTest cannot execute here).

Purpose: CLIP-03's "found in that domain's Clips section." The section mirrors the locked Rules/Collections nav template exactly (D-10) and only renders when the domain has non-archived clips, preserving the DOM-03 "only non-empty sections" contract.
Output: One edited file (DomainDetailView) + a verified end-to-end Clips flow on device.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-clips-d/04-CONTEXT.md
@.planning/phases/04-clips-d/04-UI-SPEC.md
@.planning/phases/04-clips-d/04-PATTERNS.md
@.planning/phases/04-clips-d/04-03-SUMMARY.md
@.planning/phases/04-clips-d/04-04-SUMMARY.md

<interfaces>
<!-- From 04-03 (views) + the codebase — use directly. -->

From 04-03: `ClipRow(clip: Clip)`, `ClipDetailView(clip: Clip)`, `ClipEditorView(domain: Domain)` / `ClipEditorView(clip: Clip)`.

DomainDetailView existing structure (HabitsTracker/Features/Hub/DomainDetailView.swift):
- `@State private var creatingRule = false` (line 28), `@State private var creatingCollection = false` (line 29) — add `creatingClip` beside these.
- `.sheet(isPresented: $creatingRule) { ... }` (line 55), `.sheet(isPresented: $creatingCollection) { ... }` (line 58) — add a `$creatingClip` sheet presenting `ClipEditorView(domain: domain)`.
- `nonEmptySections(theme:)` (line 68) with the hook comment `// Phase D–E: append Clips / Ideas sections here.` (line 81).
- `buildRulesSection` (line 87-96): filters `.filter { !$0.isArchived }` + `.sorted { $0.createdAt > $1.createdAt }`, `guard !activeRules.isEmpty else { return nil }` — the CLOSEST analog (isArchived filter + createdAt-descending), mirror this shape for clips (Collections sorts by sortIndex and has no archive filter, so use the Rules shape).
- The Collections trio `buildCollectionsSection`/`collectionsSectionContent`/`collectionsSectionHeader` (line 140-186) is the structural template for the content + header ("+" button, ≥44pt, accentPrimary, accessibilityLabel "Add ... to \(domain.name)").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append the Clips section to DomainDetailView (buildClipsSection trio + creatingClip sheet)</name>
  <files>HabitsTracker/Features/Hub/DomainDetailView.swift</files>
  <read_first>
    - HabitsTracker/Features/Hub/DomainDetailView.swift (self — the creatingRule/creatingCollection state + sheets, nonEmptySections + the Phase D–E hook comment, the buildRulesSection isArchived-filter shape, the buildCollectionsSection/collectionsSectionContent/collectionsSectionHeader trio to mirror)
    - .planning/phases/04-clips-d/04-PATTERNS.md section "DomainDetailView.swift" (the 4-step apply list: state, sheet, append call, the trio + which filter shape to use)
    - .planning/phases/04-clips-d/04-UI-SPEC.md section "S1 — Clips section" (header copy "Clips", "+" accessibility "Add clip to \(domain.name)", visibility rule, row ordering)
    - .planning/phases/04-clips-d/04-CONTEXT.md D-10 (section visibility = has non-archived clips; append at the reserved hook) and §9.3 (the shared domain-level empty state already name-checks "clips" — no separate empty copy)
    - CLAUDE.md §9.1 (~400-line cap — if the file crosses it after this edit, extract the Clips trio to a private extension file in the same folder), §9.4 (tokens), §9.15 (accessibility)
  </read_first>
  <behavior>
    - A domain with ≥1 non-archived clip renders a "Clips" section listing those clips, most-recently-created first; each row taps through to ClipDetailView.
    - A domain with zero non-archived clips renders NO Clips section (falls through to the shared empty state — DOM-03 "only non-empty sections").
    - The section header "+" opens ClipEditorView pre-scoped to this domain; saving a clip makes the section appear (or a new row appear).
    - Adding the Clips section does not disturb the existing Rules/Collections sections or their ordering.
  </behavior>
  <action>
    Edit `DomainDetailView.swift` per the PATTERNS 4-step list:
    1. Add `@State private var creatingClip = false` beside `creatingCollection` (~line 29).
    2. Add `.sheet(isPresented: $creatingClip) { ClipEditorView(domain: domain) }` beside the existing `.sheet` modifiers (~line 58-60).
    3. In `nonEmptySections(theme:)`, at the `// Phase D–E: append Clips / Ideas sections here.` hook (~line 81), append `if let clipsSection = buildClipsSection(theme: theme) { sections.append(clipsSection) }`.
    4. Add the trio: `buildClipsSection(theme:) -> DomainSection?` mirroring `buildRulesSection` — `let activeClips = domain.clips.filter { !$0.isArchived }.sorted { $0.createdAt > $1.createdAt }; guard !activeClips.isEmpty else { return nil }` (D-10 — use the Rules isArchived+createdAt-descending shape, NOT the Collections sortIndex shape), returning `DomainSection(id: "clips", title: "Clips", content: AnyView(clipsSectionContent(clips: activeClips, theme: theme)))`. `clipsSectionContent`: `VStack(alignment:.leading, spacing: theme.spacing.m) { clipsSectionHeader(theme:); ForEach(activeClips, id: \.id) { clip in NavigationLink { ClipDetailView(clip: clip) } label: { ClipRow(clip: clip) }.buttonStyle(.plain) } }`. `clipsSectionHeader`: `HStack { Text("Clips").font(theme.typography.title).foregroundStyle(theme.colors.textPrimary).accessibilityAddTraits(.isHeader); Spacer(); Button { creatingClip = true } label: { Image(systemName:"plus").font(.system(size: 18, weight: .semibold)).foregroundStyle(theme.colors.accentPrimary).frame(minWidth:44, minHeight:44) }.accessibilityLabel("Add clip to \(domain.name)") }`. Do NOT add a Clips-specific empty state — the shared domain-level empty state already covers it (§9.3, D-10). If the file crosses ~400 lines after this edit, extract the Clips trio into a `DomainDetailView+Clips.swift` private extension in the same folder (§9.1) — the synchronized root group auto-registers it (§9.8), do not hand-edit project.pbxproj.
  </action>
  <verify>
    <automated>xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build</automated>
  </verify>
  <acceptance_criteria>
    - `DomainDetailView.swift` contains `creatingClip`, `.sheet(isPresented: $creatingClip)`, `ClipEditorView(domain: domain)`, `buildClipsSection`, `DomainSection(id: "clips"`, `ClipDetailView(clip:`, `ClipRow(clip:`, and `accessibilityLabel("Add clip to \(domain.name)")`.
    - `buildClipsSection` filters `!$0.isArchived` and sorts `createdAt` descending (grep for both) and `guard !activeClips.isEmpty else { return nil }` (visibility rule, D-10).
    - The Rules/Collections `buildRulesSection`/`buildCollectionsSection` functions are unchanged (grep still finds them intact).
    - No hard-coded colors (`grep -E "Color\(red:|Color\(hex:" DomainDetailView.swift` returns 0, §9.4).
    - Build exits 0. (If DomainDetailView.swift now exceeds ~400 lines, the Clips trio is extracted to a same-folder extension file per §9.1 and the build still exits 0.)
  </acceptance_criteria>
  <done>The Clips section renders in DomainDetailView only when the domain has non-archived clips, with a "+" that scopes a new clip to the domain and rows that push ClipDetailView — Rules/Collections untouched, builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner visual verification — full Clips flow, export/import round-trip, offline gate (device)</name>
  <read_first>
    - .planning/phases/04-clips-d/04-UI-SPEC.md (the full interaction/visual contract to verify against)
    - CLAUDE.md §6 (RC smoke test — export/import backup round-trip), §9.7 (XCTest UI host cannot launch here — owner device verification is the only path), §8 (build command; bundle id lauterstar.HabitsTracker)
  </read_first>
  <action>Run the blocking owner visual verification of the complete Clips flow on iPhone 17. XCTest cannot launch the UI host on this toolchain (§9.7), and the export/import round-trip + the offline gate (SC1/D-01) can only be truly confirmed by a human on device — so this is the gate that closes CLIP-02/CLIP-03/CLIP-04 end-to-end. Do not advance until the owner confirms.</action>
  <what-built>The Clips section is now wired into DomainDetailView, so ClipEditorView → ClipRow → ClipDetailView are reachable under a focused domain. The status chip toggle, the Open Link CTA (opens Safari, never fetches), the delete-confirm, the schemaVersion-5 export/import round-trip, and the offline gate all need on-device confirmation because the XCTest host cannot launch UI/persistence flows here (§9.7).</what-built>
  <how-to-verify>
    Build + run on iPhone 17 (bundle id `lauterstar.HabitsTracker`):
    1. Focus a domain (Hub → focus picker) and open its DomainDetailView. Confirm NO Clips section shows yet (zero clips → section absent, DOM-03).
    2. Tap the Clips "+" (or, if the section is absent because there are no clips, use the domain's add affordance) → ClipEditorView opens pre-scoped to the domain. Paste a URL (e.g. https://www.nytimes.com/2026/01/how-to-make-sourdough); confirm the Title field auto-fills a readable suggestion (D-02); then type your own Title and confirm the URL no longer overwrites it. Add a note + tag. Confirm "Add Clip" is disabled until both Title and URL are non-empty. Save.
    3. Confirm a Clips section now appears with the new row: title, tag caption, and a "Saved" status chip.
    4. Tap the status chip on the row → it flips to "Acted" with a haptic; tap again → back to "Saved" (D-05 two-way toggle).
    5. Tap the row → ClipDetailView: confirm the prominent full-width "Open Link" primary CTA, the status chip (tap-toggles), the tag pill, and the note block. Tap "Open Link" → Safari opens the URL. Tap "Edit" → editor reopens with fields prefilled; test the "Delete Clip" destructive row → confirm the "Delete this clip?" / "This can't be undone." dialog; cancel it (keep the clip).
    6. OFFLINE GATE (SC1/D-01): turn on Airplane Mode. Create a new clip and open its link — confirm saving works fully offline and NO spinner/preview/thumbnail ever appears (there is no network fetch, ever). Turn Airplane Mode off.
    7. EXPORT/IMPORT (RC smoke §6, D-13): Settings → export a backup (confirm it succeeds). Delete the clip(s) or erase+reinstall. Import the backup → confirm the clip returns with its title, url, note, tag, and status intact and still filed under the right domain.
    8. Accessibility spot-check: VoiceOver reads the row as "\(title), status: \(statusLabel)[, tag: ...]"; Dynamic Type at a large size does not clip the layout (row title caps at 2 lines).
    Expected: every step passes; Today is unchanged; the 4-tab structure holds. Report any visual/interaction deviation from 04-UI-SPEC.
  </how-to-verify>
  <resume-signal>Type "approved" once the full Clips flow, the offline gate, and the export/import round-trip are confirmed on device, or describe what deviated.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clip.url → openURL/Link from the reachable detail view | Now that clips are reachable, opening an arbitrary user URL must stay a Safari handoff (opened, never fetched) — the offline gate must hold end-to-end |
| domain.clips filtering → section visibility | An archived clip must not surface the section or a row; a wrong filter would leak archived content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure / offline-gate violation | end-to-end "open link" and clip capture reachable from the domain nav | mitigate | Owner offline-gate step (Airplane Mode) confirms no network fetch/preview ever occurs (SC1 / D-01); Features/Clips carries zero network APIs (asserted in 04-03) |
| T-04-11 | Denial (data integrity) | archived-clip leak or empty-section render | mitigate | `buildClipsSection` filters `!$0.isArchived` + `guard !activeClips.isEmpty else { return nil }`; owner step 1 confirms the section is absent with zero clips |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — DesignKit local path + SwiftUI + SF Symbols + Apple Link/openURL only; no new third-party dependency |
</threat_model>

<verification>
- App builds clean; the Clips section renders only for domains with non-archived clips.
- Rules/Collections sections and Today are unchanged; the 4-tab structure holds.
- Owner device verification green: create → toggle → open link → edit → delete-confirm → export/import round-trip → offline gate all pass.
</verification>

<success_criteria>
- A clip is filed by domain and found in that domain's Clips section (CLIP-03).
- The end-to-end Clips flow, the export/import round-trip, and the offline gate are owner-verified on device.
</success_criteria>

<output>
Create `.planning/phases/04-clips-d/04-05-SUMMARY.md` when done.
</output>
