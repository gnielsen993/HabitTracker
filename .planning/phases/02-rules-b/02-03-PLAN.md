---
phase: 02-rules-b
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - HabitsTracker/Features/Habits/HabitCreateSheet.swift
  - HabitsTracker/Features/Settings/HabitManagerView.swift
  - HabitsTracker/Features/Rules/RuleDetailView.swift
  - HabitsTracker/Features/Settings/HabitEditorView.swift
autonomous: true
requirements: [RULE-02, RULE-03, RULE-04]
decisions: [D-04, D-05, D-06, D-07, D-11]
must_haves:
  truths:
    - "A shared fill-then-commit HabitCreateSheet inserts a Habit into the modelContext only on Save; cancelling creates nothing (no orphan habit on Today)"
    - "The sheet is source-agnostic: it prefills title + domain from an optional source and both remain editable; launched from a rule it sets the new habit's originRule"
    - "Stem habit from a rule opens the prefilled sheet; on Save the new habit appears on Today and the rule is left untouched"
    - "The existing HabitManager 'Add Habit' button uses the same sheet (one create path); the insert-then-edit 'New Habit' placeholder is retired"
    - "A stemmed habit shows a read-only 'Stemmed from: {rule}' backref in HabitEditorView that navigates to the RuleDetailView"
    - "Traceability: implements CONTEXT decisions D-04, D-05, D-06, D-07, D-11 (see 02-CONTEXT.md)"
  artifacts:
    - path: "HabitsTracker/Features/Habits/HabitCreateSheet.swift"
      provides: "shared source-agnostic fill-then-commit habit create sheet"
      contains: "struct HabitCreateSheet"
    - path: "HabitsTracker/Features/Settings/HabitEditorView.swift"
      provides: "read-only 'Stemmed from' backref row"
      contains: "Stemmed from"
  key_links:
    - from: "HabitsTracker/Features/Rules/RuleDetailView.swift"
      to: "HabitCreateSheet"
      via: "Stem habit button presents the sheet with source = rule"
      pattern: "HabitCreateSheet"
    - from: "HabitsTracker/Features/Settings/HabitManagerView.swift"
      to: "HabitCreateSheet"
      via: "Add Habit presents the shared sheet"
      pattern: "HabitCreateSheet"
    - from: "HabitsTracker/Features/Settings/HabitEditorView.swift"
      to: "RuleDetailView"
      via: "backref row navigates to the origin rule"
      pattern: "RuleDetailView"
---

<objective>
Ship the load-bearing shared `HabitCreateSheet` (RULE-02): a fill-then-commit, source-agnostic habit-create sheet that inserts a `Habit` ONLY on Save (orphan-free cancel), prefilling title + domain from an optional source and keeping both editable. Wire it into three call sites: the "Stem habit" button in `RuleDetailView` (source = the rule; the saved habit gets `originRule` set and flows to Today — RULE-03), the migrated `HabitManager` "Add Habit" button (retiring the insert-then-edit "New Habit" placeholder — D-06), and — as the read side of RULE-04 — add a read-only "Stemmed from: {rule}" backref row in `HabitEditorView` that navigates to `RuleDetailView`.

Purpose: One create path everywhere (D-06) so the flow only has to be right once, and it is deliberately source-agnostic (D-07) so Phase 5 promote-to-habit reuses it verbatim with zero sheet changes. This closes the bidirectional link: 02-02 built the rule->habit read (stemmed list); this plan builds the create ACTION and the habit->rule backref.
Output: HabitCreateSheet.swift, the RuleDetailView Stem wiring, the HabitManager migration, and the HabitEditorView backref row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rules-b/02-CONTEXT.md
@.planning/phases/02-rules-b/02-UI-SPEC.md

<interfaces>
Current insert-then-edit flow to RETIRE — HabitsTracker/Features/Settings/HabitManagerView.swift:48-54:
  Button("Add Habit") { let habit = Habit(name: "New Habit", category: categories.first, scheduleType: .daily, mode: .required); modelContext.insert(habit); try? modelContext.save(); editingHabit = habit }
  REPLACE with a `.sheet` presenting HabitCreateSheet (no pre-insert).

Habit init — HabitsTracker/Models/Habit.swift: name, category: Domain?, scheduleType, scheduledDays, mode, weeklyTargetCount, isPinned, isArchived, ..., originRule: Rule? (added in 02-01). Existing controls for schedule + required/optional live in HabitEditorView — reuse the same control shapes.

Editor idiom — HabitsTracker/Features/Settings/HabitEditorView.swift: Form, @Environment(\.dismiss), @Environment(\.modelContext), @Query(sort: \Domain.sortIndex) categories, the schedule + mode controls.

Stem button wiring point left by 02-02 — HabitsTracker/Features/Rules/RuleDetailView.swift: the "Stem habit" button + `@State private var stemming` + the `// TODO(02-03)` marker. Flip it to `.sheet(isPresented: $stemming) { HabitCreateSheet(source: .rule(rule)) }`.

Tokens/copy (from 02-UI-SPEC S4/S5): sheet radii.sheet; save CTA "Add Habit" (accentPrimary, radii.button, >=44pt), disabled until trimmed title non-empty; "Cancel" dismisses inserting nothing; validation "Give this a name to continue."; backref caption "Stemmed from" + rule title (chip radius pill), VoiceOver "Stemmed from {rule title}, opens rule".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared fill-then-commit HabitCreateSheet (S4)</name>
  <read_first>
    - HabitsTracker/Features/Settings/HabitEditorView.swift (schedule + required/optional controls, domain picker, form idiom)
    - HabitsTracker/Models/Habit.swift (init incl. originRule; HabitMode, HabitScheduleType)
    - HabitsTracker/Models/Rule.swift (rule.title, rule.domain — the prefill source)
    - .planning/phases/02-rules-b/02-UI-SPEC.md (S4: fill-then-commit, source-agnostic, copy)
  </read_first>
  <files>HabitsTracker/Features/Habits/HabitCreateSheet.swift</files>
  <action>
    Create HabitsTracker/Features/Habits/HabitCreateSheet.swift: `struct HabitCreateSheet: View`. Define a source enum `enum HabitSource { case manual; case rule(Rule) }` (extensible — Phase 5 adds `.idea(Idea)` without touching the sheet, per D-07). Init `HabitCreateSheet(source: HabitSource = .manual)`.
    Hold the draft in @State (title: String, selectedDomain: Domain?, scheduleType, scheduledDays, mode) — NOT a persisted Habit. Seed the draft from the source on appear: `.rule(let r)` -> title = r.title, selectedDomain = r.domain; `.manual` -> empty title, selectedDomain = first domain. Both fields stay editable. NO chrome differs by source beyond the prefill (no "stemmed from" banner in the create sheet — the link is recorded on save).
    Form (theme.radii.sheet) with Title (TextField, prefilled+editable, trimmed non-empty guard), Domain (Picker over @Query(sort: \Domain.sortIndex)), Schedule (reuse the HabitEditorView schedule control shape), Required/Optional (HabitMode control). Labels theme.typography.headline, values theme.typography.body.
    Toolbar/footer: "Add Habit" (accentPrimary, radii.button, >=44pt), DISABLED until trimmed title is non-empty (inline "Give this a name to continue." otherwise); "Cancel" dismisses. On "Add Habit": construct `Habit(name: trimmedTitle, category: selectedDomain, scheduleType:, scheduledDays:, mode:, originRule: <the rule if source == .rule else nil>)`, modelContext.insert(habit), try? modelContext.save(), dismiss(). This is the ONLY insert — on Cancel nothing is inserted (fill-then-commit, D-04). Keep under ~400 lines (§9.1).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && test -f HabitsTracker/Features/Habits/HabitCreateSheet.swift && grep -q "struct HabitCreateSheet" HabitsTracker/Features/Habits/HabitCreateSheet.swift && grep -q "enum HabitSource" HabitsTracker/Features/Habits/HabitCreateSheet.swift && grep -q "modelContext.insert" HabitsTracker/Features/Habits/HabitCreateSheet.swift && grep -q "Add Habit" HabitsTracker/Features/Habits/HabitCreateSheet.swift</automated>
  </verify>
  <acceptance_criteria>
    - HabitCreateSheet inserts a Habit exactly once, inside the "Add Habit" save action — there is NO modelContext.insert before Save (grep shows a single insert call, in the save path).
    - A HabitSource enum drives prefill only; `.rule` sets originRule on the created habit, `.manual` leaves it nil; both prefilled fields remain editable.
    - "Add Habit" is disabled on empty title with the exact validation copy; "Cancel" inserts nothing.
    - Build green.
  </acceptance_criteria>
  <done>A single source-agnostic fill-then-commit sheet creates habits; cancel leaves no orphan; rule-sourced creates set originRule.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Stem action (RuleDetailView) + migrate HabitManager "Add Habit" (RULE-02/03, D-06)</name>
  <read_first>
    - HabitsTracker/Features/Rules/RuleDetailView.swift (the Stem button + stemming @State + TODO(02-03) marker from 02-02)
    - HabitsTracker/Features/Settings/HabitManagerView.swift (the insert-then-edit Add Habit button lines 48-54)
    - HabitsTracker/Features/Habits/HabitCreateSheet.swift (the sheet + HabitSource)
  </read_first>
  <files>HabitsTracker/Features/Rules/RuleDetailView.swift, HabitsTracker/Features/Settings/HabitManagerView.swift</files>
  <action>
    RuleDetailView.swift: replace the 02-02 wiring marker so the "Stem habit" button presents the sheet: `.sheet(isPresented: $stemming) { HabitCreateSheet(source: .rule(rule)) }`, and the button action sets `stemming = true`. REMOVE the `.disabled(true)` guard that 02-02 added to the button (it must now be tappable), and remove the `// TODO(02-03)` marker. The rule is NOT mutated by stemming (RULE-03: leave the rule untouched — the link is set on the Habit side via originRule). After the sheet saves, rule.stemmedHabits reflects the new habit (relationship auto-updates), so the "Stemmed: N" list grows on next render.

    HabitManagerView.swift: replace the Button("Add Habit") body (which currently inserts a "New Habit" placeholder then opens the editor) with a `@State private var creatingHabit = false`; the button sets `creatingHabit = true`; add `.sheet(isPresented: $creatingHabit) { HabitCreateSheet(source: .manual) }`. Delete the pre-insert of `Habit(name: "New Habit", ...)` entirely (D-06 — retire insert-then-edit). Keep the existing edit path (editingHabit -> HabitEditorView) for editing existing rows.
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && grep -q "HabitCreateSheet(source: .rule(rule))" HabitsTracker/Features/Rules/RuleDetailView.swift && grep -q "HabitCreateSheet(source: .manual)" HabitsTracker/Features/Settings/HabitManagerView.swift && ! grep -q "New Habit" HabitsTracker/Features/Settings/HabitManagerView.swift && ! grep -q "TODO(02-03)" HabitsTracker/Features/Rules/RuleDetailView.swift</automated>
  </verify>
  <acceptance_criteria>
    - RuleDetailView "Stem habit" presents HabitCreateSheet(source: .rule(rule)); the rule is not mutated by stemming.
    - HabitManagerView "Add Habit" presents HabitCreateSheet(source: .manual); the `Habit(name: "New Habit")` pre-insert is gone (no "New Habit" string remains).
    - Both call sites compile; build green.
  </acceptance_criteria>
  <done>Stem and manual-add both go through the one shared sheet; the orphan-prone placeholder path is retired.</done>
</task>

<task type="auto">
  <name>Task 3: Habit-side "Stemmed from" backref row in HabitEditorView (S5, RULE-04)</name>
  <read_first>
    - HabitsTracker/Features/Settings/HabitEditorView.swift (the Form + habit binding)
    - HabitsTracker/Features/Rules/RuleDetailView.swift (the navigation target)
    - HabitsTracker/Models/Habit.swift (habit.originRule)
    - .planning/phases/02-rules-b/02-UI-SPEC.md (S5 copy + a11y)
  </read_first>
  <files>HabitsTracker/Features/Settings/HabitEditorView.swift</files>
  <action>
    HabitEditorView.swift: add a read-only backref row using a GUARDED optional binding (NO force-unwrap): `if let originRule = habit.originRule { ... }`. Inside the binding, render a caption "Stemmed from" (theme.typography.caption / textSecondary) + `originRule.title` as a tappable pill/row (theme.typography.headline, theme.radii.chip) inside a `NavigationLink { RuleDetailView(rule: originRule) }`. Bind the rule once via `if let` — do NOT re-read `habit.originRule!` in the destination (avoids the TOCTOU force-unwrap crash if SwiftData faults the optional to nil after a delete). Editing or archiving the habit must NOT touch the rule (read-only backref — no writes to originRule from this row). >=44pt. VoiceOver: "Stemmed from {rule title}, opens rule". Keep HabitEditorView under ~400 lines (§9.1).
  </action>
  <verify>
    <automated>cd /Users/gabrielnielsen/Desktop/HabitsTracker && grep -q "Stemmed from" HabitsTracker/Features/Settings/HabitEditorView.swift && grep -q "originRule" HabitsTracker/Features/Settings/HabitEditorView.swift && grep -q "RuleDetailView" HabitsTracker/Features/Settings/HabitEditorView.swift</automated>
  </verify>
  <acceptance_criteria>
    - HabitEditorView shows a "Stemmed from: {rule title}" row only when habit.originRule != nil, navigating to RuleDetailView.
    - No force-unwrap of originRule in the backref row — a guarded `if let` binding is used and the bound value drives both the label and the NavigationLink destination (grep: no `originRule!` in HabitEditorView.swift).
    - The row is read-only — no code path writes habit.originRule from HabitEditorView.
    - Build green.
  </acceptance_criteria>
  <done>A stemmed habit surfaces its origin rule as a read-only, tappable backref — closing the bidirectional link (RULE-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| create-sheet Cancel -> store | Cancelling must not persist a partial/orphan Habit |
| stem action -> source rule | Stemming must create a Habit without mutating or endangering the rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0203-01 | Availability/Integrity | orphan Habit on cancel (old insert-then-edit bug) | mitigate | Fill-then-commit: the sole modelContext.insert lives in the Save action; Cancel returns without inserting (acceptance-tested by the single-insert grep + manual cancel check) |
| T-0203-02 | Tampering | stem action mutating the source rule | mitigate | Stem sets the link on the Habit side (originRule) only; RuleDetailView never writes to `rule` during stemming (RULE-03 "rule left untouched") |
| T-0203-03 | Integrity | backref row writing to originRule | mitigate | Backref is read-only navigation; no write path from HabitEditorView to habit.originRule |
| T-0203-SC | Supply chain | external package installs | accept | N/A — zero new dependencies; DesignKit + SF Symbols only |
</threat_model>

<verification>
- Build green: xcodebuild -scheme HabitsTracker -destination 'platform=iOS Simulator,name=iPhone 17' -quiet build.
- Manual RULE-03: open a rule -> "Stem habit" -> edit the prefilled title -> Add Habit -> the new habit appears on Today; the rule is unchanged; cancel a second stem -> NO new habit created (orphan-free).
- Manual RULE-02/D-06: HabitManager "Add Habit" opens the same sheet; cancel creates nothing.
- Manual RULE-04: the stemmed habit's editor shows "Stemmed from: {rule}" and tapping it opens the rule; the rule's "Stemmed: N" list includes the habit — bidirectional link both ways.
</verification>

<success_criteria>
- RULE-02: the shared habit-create-from-source sheet exists (prefilled title + domain, both editable; schedule + required/optional set by the user) and is the reused code path (Phase 5 promote-to-habit will reuse it).
- RULE-03: Stem opens the prefilled sheet, creates a new habit that appears on Today, and leaves the rule untouched.
- RULE-04: the habit shows a "from rule" backref that jumps to the rule (pairing with 02-02's rule-side "Stemmed: N" list).
- D-06: one create path — HabitManager "Add Habit" uses the same sheet; insert-then-edit retired.
</success_criteria>

<output>
Create `.planning/phases/02-rules-b/02-03-SUMMARY.md` when done.
</output>
