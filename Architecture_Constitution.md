# ARCHITECTURE_CONSTITUTION.md

> **Superseded — see `CLAUDE.md`.**
>
> The architecture rules (stack, persistence, state management, project structure, data
> safety, package strategy, simplicity/refactor discipline, naming, evolution policy) now
> live in `CLAUDE.md` as the single source of truth. `AGENTS.md` is a symlink to it, so
> Claude and Codex read identical rules.
>
> This file is kept only as a stable pointer. Do not add or fork rules here — edit `CLAUDE.md`.
